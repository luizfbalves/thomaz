---
phase: 06-strip-desktop-build-system
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: true
requirements: [BUILD-03]
must_haves:
  truths:
    - "A clean Switch build via scripts/build-switch.sh runs end-to-end without error"
    - "build_switch/thomaz.nro exists after the build completes"
    - "The build succeeds against the stripped (PLATFORM_DESKTOP-free) CMakeLists.txt"
  artifacts:
    - path: "build_switch/thomaz.nro"
      provides: "The shipped Switch homebrew binary, built from the Switch-only tree"
  key_links:
    - from: "scripts/build-switch.sh"
      to: "CMakeLists.txt"
      via: "cmake -DPLATFORM_SWITCH=ON configure inside devkitpro/devkita64"
      pattern: "PLATFORM_SWITCH"
---

<objective>
Prove the stripped build still works. Run a clean Switch build via
`scripts/build-switch.sh` (devkitPro Docker `devkitpro/devkita64`) against the
PLATFORM_DESKTOP-free CMakeLists.txt and confirm it produces
`build_switch/thomaz.nro` end-to-end.

This satisfies BUILD-03. It MUST run AFTER Plan 01 (Wave 1) so the build proves
the desktop-stripped tree is still buildable — that is the entire point of the
sequencing constraint.

Purpose: Verify zero regression in the shipped artifact after removing the
desktop build path. A green clean build is the post-removal compile gate for the
v1.1 milestone.
Output: `build_switch/thomaz.nro` produced from the stripped tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run a clean Switch build and confirm thomaz.nro is produced</name>
  <files></files>
  <read_first>
    - scripts/build-switch.sh — this is the canonical Switch build entrypoint.
      Note: with no DEVKITPRO env var set it builds inside the
      `devkitpro/devkita64` Docker image; it runs `git submodule update --init
      --recursive`, configures with `cmake -B build_switch -DPLATFORM_SWITCH=ON
      -DUSE_DEKO3D=ON`, auto-wipes a stale `build_switch` cache, then
      `make -C build_switch thomaz.nro`. Output: `build_switch/thomaz.nro`.
    - CMakeLists.txt — confirm Plan 01 left it PLATFORM_DESKTOP-free and the
      PLATFORM_SWITCH path intact before spending Docker build time.
  </read_first>
  <action>
    First do a fast sanity gate (cheap, no Docker): confirm Plan 01 actually
    stripped the build by asserting `grep -n 'PLATFORM_DESKTOP' CMakeLists.txt`
    returns nothing and both desktop scripts are gone. If any of those fail, stop
    and report — the Wave 1 removal did not complete; do not start a Docker build
    against an unstripped tree.

    Then run a CLEAN Switch build from the repo root: remove any existing
    `build_switch/` directory first so this is a from-scratch configure+compile
    (proving the stripped CMakeLists configures cleanly, not just incrementally),
    then invoke `scripts/build-switch.sh`. Let it run inside the
    `devkitpro/devkita64` Docker image (Docker is available in this environment).
    The build is long-running; allow it ample time and do not interrupt it.

    On success, confirm `build_switch/thomaz.nro` exists and is a non-empty file.

    If Docker is unavailable or the devkitPro image cannot be pulled in this
    environment, do NOT silently pass: record the failure in the SUMMARY, mark
    BUILD-03 as a deferred manual gate (per the Phase 6 blocker noted in STATE.md
    — "If unavailable, the nro-build criterion becomes a manual/deferred gate"),
    and surface it as a checkpoint for the operator to run on a Docker-capable
    machine. Do not fabricate a passing result.

    Do not modify any source or build file in this plan — this is a verification
    plan only. If the build fails on a real compile error, capture the error and
    report it as a gap (the stripped CMakeLists broke the Switch path); do not
    attempt edits here.
  </action>
  <verify>
    <automated>cd /home/solid/www/personal/playground/thomas && grep -q 'PLATFORM_DESKTOP' CMakeLists.txt && { echo "FAIL: PLATFORM_DESKTOP still present — Wave 1 incomplete"; exit 1; }; rm -rf build_switch && ./scripts/build-switch.sh && test -s build_switch/thomaz.nro && echo OK-NRO-BUILT</automated>
  </verify>
  <acceptance_criteria>
    - Pre-gate: `grep -n 'PLATFORM_DESKTOP' CMakeLists.txt` returns no matches
      (exit 1) AND `test ! -f scripts/build-desktop.sh` AND
      `test ! -f scripts/run-desktop.sh` — confirms Wave 1 removal landed before
      building.
    - `./scripts/build-switch.sh` runs to completion with exit code 0 (clean,
      from-scratch `build_switch` directory).
    - `test -s build_switch/thomaz.nro` succeeds — the .nro exists and is
      non-empty.
    - If Docker/devkitPro is unavailable: the SUMMARY records BUILD-03 as a
      deferred manual gate (operator runs it on a Docker-capable host); the result
      is NOT reported as an automated pass.
  </acceptance_criteria>
  <done>
    A clean, from-scratch Switch build via `scripts/build-switch.sh` succeeds
    end-to-end against the desktop-stripped CMakeLists.txt and produces a
    non-empty `build_switch/thomaz.nro` — OR, if Docker is unavailable in this
    environment, BUILD-03 is explicitly recorded as a deferred manual gate with
    operator instructions, not a fabricated pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Docker container | `scripts/build-switch.sh` bind-mounts the repo into the `devkitpro/devkita64` image and runs the build there. This boundary already exists and is unchanged by this phase; the image is the official devkitPro container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | Switch build of the stripped tree producing a different/broken .nro | mitigate | A clean from-scratch build is required (build_switch wiped first); a non-empty thomaz.nro must be produced. A compile failure is reported as a gap, never passed silently. |
| T-06-03 | Elevation of Privilege | devkitpro/devkita64 Docker image provenance | accept | Pre-existing, unchanged dependency: build-switch.sh already used this official devkitPro image before this phase. This phase introduces no new image or registry; risk is pre-accepted by the existing build flow. |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this phase; no legitimacy checkpoint required. |

**No high-severity threats:** This phase only verifies a build of an already-
desktop-stripped tree. No new inputs, network surface, dependencies, or privilege
boundaries are introduced; the Docker build boundary pre-dates this phase.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

**Build artifact (BUILD-03):**
- `build_switch/thomaz.nro` — the shipped Switch homebrew, built clean from the
  desktop-stripped tree, proving zero-regression removal.

**No source/build-file changes:** this plan is verification-only; it modifies no
tracked files (the `build_switch/` directory is a build output, not committed).
</artifacts_produced>

<verification>
- Pre-gate confirms Wave 1 stripped the build (no PLATFORM_DESKTOP, scripts gone).
- `./scripts/build-switch.sh` exits 0 from a clean `build_switch` directory.
- `test -s build_switch/thomaz.nro` succeeds.
- Fallback: if Docker/devkitPro is unavailable, BUILD-03 is recorded as a deferred
  manual gate rather than reported as passing.
</verification>

<success_criteria>
- BUILD-03: A clean Switch build via `scripts/build-switch.sh` succeeds
  end-to-end against the stripped CMakeLists.txt and produces a non-empty
  `build_switch/thomaz.nro` (or is explicitly deferred to a manual gate if Docker
  is unavailable in this environment).
</success_criteria>

<output>
Create `.planning/phases/06-strip-desktop-build-system/06-02-SUMMARY.md` when done.
</output>
