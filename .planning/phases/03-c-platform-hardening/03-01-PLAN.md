---
phase: 03-c-platform-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/platform/fs_util.hpp
  - source/platform/fs_util.cpp
  - source/platform/cheat_store.cpp
  - source/platform/themes/theme_install.cpp
  - source/platform/mods/libarchive_extractor.cpp
  - source/platform/mods/mod_download.cpp
  - source/platform/mods/mod_store.cpp
  - source/platform/mods/mod_store.hpp
  - source/platform/mods/mod_actions.cpp
  - source/platform/save_service_switch.cpp
  - tests/Makefile
  - tests/test_fs_util.cpp
autonomous: false
requirements: [DEBT-01, DEBT-02]
must_haves:
  truths:
    - "ensure_parent_dirs and copy_tree are defined in exactly one place (fs_util.cpp)"
    - "All 7 previously-duplicated call-sites compile against the shared fs_util helpers"
    - "make test links and passes including a doctest that proves the canonical ensure_parent_dirs equals the char-by-char (theme_install) variant"
    - "A clean desktop build with -DUSE_SDL2=ON compiles fs_util.cpp with zero new warnings"
  artifacts:
    - path: "source/platform/fs_util.hpp"
      provides: "thomaz::ensure_parent_dirs and thomaz::copy_tree declarations"
      contains: "namespace thomaz"
    - path: "source/platform/fs_util.cpp"
      provides: "single definition of both helpers"
      contains: "copy_tree"
    - path: "tests/test_fs_util.cpp"
      provides: "D-05 equivalence gate doctest"
      contains: "TEST_CASE"
  key_links:
    - from: "source/platform/cheat_store.cpp"
      to: "thomaz::ensure_parent_dirs"
      via: "include platform/fs_util.hpp"
      pattern: "fs_util\\.hpp"
    - from: "tests/Makefile"
      to: "source/platform/fs_util.cpp"
      via: "explicit SRCS entry"
      pattern: "fs_util\\.cpp"
---

<objective>
Consolidate the four duplicated `ensure_parent_dirs` definitions and the two duplicated `copy_tree` definitions into a single shared `source/platform/fs_util.{hpp,cpp}` platform utility under the `thomaz::` namespace (DEBT-01, DEBT-02 — per D-04, ALL 7 call-sites, not just the roadmap-flagged pair). Per S1, DEBT-01 and DEBT-02 share `fs_util.{hpp,cpp}` and MUST land in this single plan/commit.

Per D-05, the two `ensure_parent_dirs` variants (substring-at-slash in cheat_store.cpp vs char-by-char in theme_install.cpp) must be proven behaviorally equivalent by a doctest BEFORE the char-by-char copy is removed. The canonical form is the substring-at-slash variant.

Purpose: Stop hand-rolling six copies of two filesystem helpers; establish one tested source of truth.
Output: New fs_util.{hpp,cpp}; 7 migrated call-sites; updated tests/Makefile SRCS; new test_fs_util.cpp.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-c-platform-hardening/03-CONTEXT.md
@.planning/phases/03-c-platform-hardening/03-RESEARCH.md
@.planning/phases/03-c-platform-hardening/03-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan CREATES the following symbols/files — the source-grounding/drift pass MUST treat these as newly-created (not pre-existing drift):
- New file `source/platform/fs_util.hpp` — declares `thomaz::ensure_parent_dirs(const std::string&)` and `thomaz::copy_tree(const std::string&, const std::string&, std::string*)`.
- New file `source/platform/fs_util.cpp` — single definition of both helpers plus the file-local `is_dir`/`copy_file` helpers pulled from mod_store.cpp.
- New file `tests/test_fs_util.cpp` — D-05 equivalence gate doctest.
- New SRCS entry `../source/platform/fs_util.cpp` in tests/Makefile.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create fs_util with canonical helpers + D-05 equivalence gate doctest</name>
  <files>source/platform/fs_util.hpp, source/platform/fs_util.cpp, tests/test_fs_util.cpp, tests/Makefile</files>
  <read_first>
    - source/platform/cheat_store.cpp:11-21 (CANONICAL ensure_parent_dirs: substring-at-slash, i=1..size(), !dir.empty() guard, void return)
    - source/platform/themes/theme_install.cpp:38-46 (char-by-char variant whose equivalence D-05 must prove before removal)
    - source/platform/mods/mod_store.cpp:11-74 (is_dir helper :11-15, copy_file helper :16-38, CANONICAL copy_tree :42-74 — 3-arg std::string* err form)
    - source/platform/mods/mod_store.hpp:1-26 (header layout: pragma once, thomaz:: namespace, doc-comment-above-declaration; copy_tree declaration at :11)
    - source/platform/save_service_switch.cpp:55-90 (2-arg copy_tree; fold the ghost-file-removal-on-open-fail behavior into canonical copy_file so no behavior is lost — RESEARCH line 205)
    - tests/test_cfw_paths.cpp (doctest precedent: include doctest.h, the platform header, using namespace thomaz, TEST_CASE + CHECK; std::filesystem + temp-dir usage)
    - tests/Makefile:3 (explicit SRCS list — NOT a platform glob; cheat_store.cpp and mod_store.cpp are ALREADY in SRCS)
  </read_first>
  <behavior>
    - test_fs_util.cpp: under a std::filesystem temp root, fs_util::ensure_parent_dirs of "<tmp>/themes/a/b/c.bin" creates exactly themes, themes/a, themes/a/b and does NOT create the final segment c.bin.
    - Trailing-slash case: fs_util::ensure_parent_dirs of "<tmp>/x/y/" creates x and x/y (matching libarchive_extractor's deliberate trailing-slash usage).
    - Equivalence (D-05): a reference char-by-char implementation (copied inline into the test from theme_install.cpp:38-46, run against a sibling temp root) produces the SAME set of created directories as fs_util::ensure_parent_dirs for "themes/a/b/c" and the trailing-slash case.
  </behavior>
  <action>
    Create source/platform/fs_util.hpp following the mod_store.hpp header layout (pragma once; include the string header; namespace thomaz; doc-comment above each declaration). Declare ensure_parent_dirs taking a const std::string& path returning void, and copy_tree taking (const std::string& src_dir, const std::string& dst_dir, std::string* err) returning bool.

    Create source/platform/fs_util.cpp: include cstdio, dirent.h, sys/stat.h, unistd.h, and platform/fs_util.hpp. Define thomaz::ensure_parent_dirs using the CANONICAL substring-at-slash form from cheat_store.cpp:11-21 verbatim (loop i=1..size(), dir = path.substr(0,i), !dir.empty() guard, mkdir ignoring EEXIST, void return) — promoted to external linkage in thomaz:: (NOT anonymous). Define thomaz::copy_tree using the mod_store.cpp:42-74 3-arg form; pull the file-local is_dir and copy_file helpers (mod_store.cpp:11-38) into fs_util.cpp's anonymous namespace. Fold save_service_switch's ghost-file-removal-on-open-fail nicety into copy_file so no behavior is lost. Use POSIX + std::string ONLY — no std::format, no spaceship operator, no std::span (must compile under BOTH C++20 app and C++17 tests — Pitfall 5).

    Create tests/test_fs_util.cpp mirroring test_cfw_paths.cpp: include doctest.h and platform/fs_util.hpp; using namespace thomaz. Implement the three behaviors above using std::filesystem temp dirs (the test_cfw_paths.cpp temp-dir precedent). Inline a copy of theme_install.cpp's char-by-char loop as the reference oracle for the equivalence assertion (D-05 gate). Clean up the temp tree at the end.

    Edit tests/Makefile line 3: append ../source/platform/fs_util.cpp to the SRCS list. MANDATORY in THIS change (build asymmetry): cheat_store.cpp and mod_store.cpp are already in SRCS and will reference thomaz::ensure_parent_dirs/copy_tree once Task 2 migrates them — without fs_util.cpp in SRCS the test link fails with undefined reference (Pitfall 1). test_fs_util.cpp itself is auto-collected by the *.cpp wildcard — no extra registration.
  </action>
  <verify>
    <automated>cd tests && make clean && make test</automated>
  </verify>
  <acceptance_criteria>
    - fs_util.hpp contains namespace thomaz and declarations for both helpers: grep -q ensure_parent_dirs source/platform/fs_util.hpp AND grep -q copy_tree source/platform/fs_util.hpp
    - fs_util.cpp defines both helpers in thomaz:: : grep -q thomaz source/platform/fs_util.cpp
    - tests/Makefile SRCS contains ../source/platform/fs_util.cpp: grep -q fs_util.cpp tests/Makefile
    - tests/test_fs_util.cpp has at least 2 TEST_CASE proving the equivalence gate: grep -c TEST_CASE tests/test_fs_util.cpp returns 2 or more
    - fs_util uses no C++20-only constructs: grep for std::format and std::span in fs_util.cpp/.hpp returns nothing
    - cd tests && make clean && make test exits 0 (the equivalence doctest passes BEFORE any local copy is removed)
  </acceptance_criteria>
  <done>fs_util.{hpp,cpp} exist with the canonical helpers; test_fs_util.cpp proves D-05 equivalence; tests/Makefile lists fs_util.cpp; make test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 7 call-sites to fs_util and remove every local copy</name>
  <files>source/platform/cheat_store.cpp, source/platform/themes/theme_install.cpp, source/platform/mods/libarchive_extractor.cpp, source/platform/mods/mod_download.cpp, source/platform/mods/mod_store.cpp, source/platform/mods/mod_store.hpp, source/platform/mods/mod_actions.cpp, source/platform/save_service_switch.cpp</files>
  <read_first>
    - source/platform/fs_util.hpp (the header created in Task 1 — the include target)
    - source/platform/cheat_store.cpp:9-23 (anon-namespace ensure_parent_dirs to delete; call at :41)
    - source/platform/themes/theme_install.cpp:14-46 (anon-namespace char-by-char ensure_parent_dirs to delete; calls at :98, :111)
    - source/platform/mods/libarchive_extractor.cpp:11-41 (anon-namespace ensure_parent_dirs to delete; calls at :106 trailing-slash, :111)
    - source/platform/mods/mod_download.cpp:10-34 (anon-namespace ensure_parent_dirs to delete; call at :39)
    - source/platform/mods/mod_store.cpp:11-74 (is_dir/copy_file/copy_tree to delete — they move to fs_util.cpp; recursive self-call at :60)
    - source/platform/mods/mod_store.hpp:11 (copy_tree declaration to remove — it now lives in fs_util.hpp)
    - source/platform/mods/mod_actions.cpp:82 (already 3-arg copy_tree(src,dst,&err) — only add the include, no signature change)
    - source/platform/save_service_switch.cpp:55-90 (2-arg copy_tree definition to delete; recursive self-call :71; callers :206, :256 — switch to 3-arg, pass nullptr for err)
  </read_first>
  <action>
    In EACH of the 8 files: add include platform/fs_util.hpp and replace the local helper with a qualified call. Per D-04 and Pitfall 4, delete EVERY local definition in this SAME change — after editing, only fs_util.{hpp,cpp} may define ensure_parent_dirs/copy_tree, or you get redefinition/ambiguity.

    ensure_parent_dirs sites (delete the anon-namespace definition, keep the call):
    - cheat_store.cpp: remove :11-21 definition (and the now-empty anon namespace if nothing else remains); call at :41 resolves to thomaz::ensure_parent_dirs.
    - theme_install.cpp: remove the char-by-char :38-46 definition (D-05 already proved equivalent in Task 1); calls at :98, :111 resolve to canonical.
    - libarchive_extractor.cpp: remove :13-40 definition; calls at :106 (trailing-slash equivalence confirmed) and :111.
    - mod_download.cpp: remove :12-33 definition; call at :39.

    copy_tree sites:
    - mod_store.cpp: remove is_dir/copy_file/copy_tree (:11-74); recursive call at :60 now resolves to thomaz::copy_tree.
    - mod_store.hpp: remove the copy_tree declaration at :11 (now in fs_util.hpp). If any external TU includes mod_store.hpp expecting copy_tree, ensure it includes fs_util.hpp instead.
    - mod_actions.cpp:82: add the include only — call is already the 3-arg form.
    - save_service_switch.cpp: remove the 2-arg copy_tree :55-90; recursive self-call :71 and callers :206, :256 become the 3-arg form passing nullptr for err (these callers ignore error detail and rely on both dirs pre-existing; canonical copy_tree's top-level mkdir is harmless EEXIST).
  </action>
  <verify>
    <automated>cd tests && make clean && make test</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one definition of each helper remains: grep -rn 'void ensure_parent_dirs' source/ piped to grep -vc fs_util returns 0, and grep -rn 'bool copy_tree' source/ piped to grep -vc fs_util returns 0
    - Every former call-site now includes the shared header: grep -lr fs_util.hpp across the 7 migrated files lists all 7
    - mod_store.hpp no longer declares copy_tree: grep -c copy_tree source/platform/mods/mod_store.hpp returns 0
    - cd tests && make clean && make test exits 0 (proves no undefined-reference from the build-asymmetry break; cheat_store.cpp + mod_store.cpp link against fs_util.cpp)
    - No redefinition/ambiguity: the test link produces no 'redefinition of' or 'call is ambiguous' errors
  </acceptance_criteria>
  <done>All 7 call-sites call the shared helpers; every local copy is deleted; mod_store.hpp no longer declares copy_tree; make test is green with no undefined references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify clean desktop build (fresh CMake configure picks up fs_util.cpp)</name>
  <action>Human verifies the desktop app build after fs_util consolidation. Claude has already run the host test suite (make test) which proves the test-build linkage. This checkpoint covers the APP build, which uses CMake GLOB_RECURSE (configure-time) and must be reconfigured to pick up the new fs_util.cpp.</action>
  <what-built>fs_util consolidation complete: shared helpers, 7 migrated call-sites, tests/Makefile SRCS updated, D-05 equivalence doctest green. The host test suite (make test) verifies the test-build linkage. This checkpoint verifies the APP (desktop) build, which uses CMake GLOB_RECURSE (configure-time) and must be reconfigured to pick up the new fs_util.cpp.</what-built>
  <how-to-verify>
    1. Reconfigure CMake fresh (GLOB is evaluated at configure time — a stale build dir will NOT see fs_util.cpp; Pitfall 2): cmake -DUSE_SDL2=ON -S . -B build_desktop (or your existing desktop build dir name).
    2. Build and capture warnings: cmake --build build_desktop 2>&1 | tee /tmp/p3-01-build.log
    3. Confirm zero ERRORS and zero NEW warnings vs the pre-phase baseline: grep -i warning /tmp/p3-01-build.log — expect no new lines attributable to fs_util.cpp or the 7 migrated files (the toolchain uses -Wall, no -Werror, so warnings do not fail the build automatically — they must be eyeballed).
    4. Confirm fs_util.cpp actually appears in the build output (proves the fresh configure picked it up).
  </how-to-verify>
  <verify><human-check>Desktop build compiles clean with zero errors and zero new warnings; fs_util.cpp appears in the build output.</human-check></verify>
  <done>Desktop build is clean with zero new warnings; fs_util.cpp compiled by the fresh configure.</done>
  <resume-signal>Type "approved" if the desktop build is clean with zero new warnings, or describe the warnings/errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive contents → filesystem | extracted mod/theme paths reach ensure_parent_dirs/copy_tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | path traversal via fs_util helpers (`../` in archive entry names) | accept | Paths are internally constructed or already validated upstream by `core::is_safe_archive_path` in libarchive_extractor.cpp BEFORE reaching ensure_parent_dirs; consolidation does not change which paths arrive. No new attack surface — same inputs, same POSIX mkdir/copy behavior, now de-duplicated. |
| T-03-02 | Tampering | behavioral drift between old char-by-char and new canonical ensure_parent_dirs | mitigate | D-05 equivalence doctest (test_fs_util.cpp) proves identical mkdir set for representative + trailing-slash + leading-prefix paths BEFORE any local copy is removed. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — internal C++ refactor only; RESEARCH Package Legitimacy Audit confirms zero external packages this phase. |
</threat_model>

<verification>
- cd tests && make test green (D-05 doctest + all existing 37 tests).
- grep -rn 'void ensure_parent_dirs|bool copy_tree' source/ piped to grep -v fs_util returns nothing (exactly-one definition).
- Fresh cmake -DUSE_SDL2=ON desktop build compiles fs_util.cpp with zero new warnings (checkpoint).
</verification>

<success_criteria>
- source/platform/fs_util.{hpp,cpp} exist; ensure_parent_dirs + copy_tree defined there and nowhere else (ROADMAP SC-1).
- All 7 duplicated copies removed from their original sites (D-04).
- tests/Makefile SRCS includes fs_util.cpp; make test links and passes (build asymmetry resolved).
- D-05 equivalence proven by doctest before theme_install's variant removed.
- Desktop build clean, zero new warnings.
</success_criteria>

<output>
Create `.planning/phases/03-c-platform-hardening/03-01-SUMMARY.md` when done.
</output>
