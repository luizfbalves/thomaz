---
phase: 05-collapse-source-seams-switch-only
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified: []
autonomous: true
requirements:
  - SIMPL-01
  - SIMPL-02
  - SIMPL-03

must_haves:
  truths:
    - "find source/platform -name '*_fake*' returns exactly saves/fake_cloud_save_client.cpp and saves/fake_cloud_save_client.hpp"
    - "grep -rn 'ifndef __SWITCH__\\|#else' source/ shows no *_fake-vs-*_switch seam"
    - "grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/ returns nothing except fake_cloud_save_client references"
    - "make -C tests test passes unchanged"
    - "saves/fake_cloud_save_client.cpp and saves/fake_cloud_save_client.hpp still exist and the test suite still compiles them"
  artifacts:
    - path: source/platform/saves/fake_cloud_save_client.cpp
      provides: "Retained doctest double — must exist and compile"
    - path: source/platform/saves/fake_cloud_save_client.hpp
      provides: "Retained doctest double header — must exist"
  key_links:
    - from: tests/Makefile
      to: source/platform/saves/fake_cloud_save_client.cpp
      via: explicit SRCS
      pattern: "fake_cloud_save_client"
---

<objective>
Run all four Phase 5 success-criteria verification commands and confirm the host doctest suite passes unchanged.

Purpose: This is the phase gate. All four roadmap success criteria must be verified as concrete pass/fail assertions — not inspected visually. The test suite run confirms that seam collapsing in the platform files compiled by tests/ caused no regressions, and that fake_cloud_save_client.* is still reachable and compiled.

Output: Green make -C tests test; all four success-criterion greps returning expected output; phase confirmed complete.
</objective>

<execution_context>
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md
</execution_context>

<context>
@/home/solid/www/personal/playground/thomas/.planning/ROADMAP.md
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-02-SUMMARY.md
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 4 roadmap success-criterion checks (SIMPL-01, SIMPL-02, SIMPL-03)</name>
  <files></files>

  <read_first>
    - .planning/phases/05-collapse-source-seams-switch-only/05-02-SUMMARY.md — confirms 9 stub files deleted
    - .planning/phases/05-collapse-source-seams-switch-only/05-03-SUMMARY.md — confirms seam collapse and comment cleanup complete
    - .planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md — Section 6 post-step checkpoints and Section 5 test suite contract
  </read_first>

  <action>
    Run each of the four roadmap success-criterion commands in order. For each, record the output. If any command does not produce the expected result, diagnose the gap before proceeding to the test run.

    Success Criterion 1 (SIMPL-01):
      find source/platform -name '*_fake*' | sort

    Expected: exactly two lines —
      source/platform/saves/fake_cloud_save_client.cpp
      source/platform/saves/fake_cloud_save_client.hpp
    Failure condition: any other line (e.g. a stub that was not deleted, or fake_cloud_save_client missing).

    Success Criterion 2 (SIMPL-02):
      grep -rn 'ifndef __SWITCH__\|#else' source/

    Expected: only lines from source/app/game_panel.cpp and source/platform/saves/save_backup_io.cpp (Category C _WIN32 seams). No #else inside any #ifdef __SWITCH__ block, no #ifndef __SWITCH__ anywhere.
    Failure condition: any hit in any other file.

    Success Criterion 3 (SIMPL-03):
      grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/

    Expected: only lines containing `fake_cloud_save_client`. Zero PLATFORM_DESKTOP, SDL2, GLFW, or other _fake references.
    Failure condition: any hit not containing `fake_cloud_save_client`.

    Success Criterion 4 (VERIF-01 — host doctest suite, full run in Task 2).

    If criteria 1-3 all pass, proceed to Task 2. If any criterion fails, identify which plan's output is incomplete, fix the gap, and re-run before continuing.
  </action>

  <acceptance_criteria>
    - `find source/platform -name '*_fake*' | sort` output is exactly the two retained doctest double files
    - `grep -rn 'ifndef __SWITCH__\|#else' source/ | grep -v 'game_panel\|save_backup_io'` returns nothing
    - `grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/ | grep -v 'fake_cloud_save_client'` returns nothing
  </acceptance_criteria>

  <verify>
    <automated>find source/platform -name '*_fake*' | sort && grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/ | grep -v 'fake_cloud_save_client' | wc -l | grep -x 0</automated>
  </verify>

  <done>All three source-side success criteria produce expected output; no residual desktop references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Run host doctest suite — make -C tests test (SIMPL-01 / VERIF-01, per RESEARCH.md Section 5)</name>
  <files></files>

  <read_first>
    - .planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md — Section 5 (host doctest suite contract), Section 4 tests/Makefile analysis
  </read_first>

  <action>
    Run the host doctest suite from the repo root:

      make -C tests test

    Per RESEARCH.md Section 5: the suite compiles an explicit source list that includes `source/platform/saves/fake_cloud_save_client.cpp` (the retained doctest double) and several platform files whose path-string seams were collapsed in Plan 03 (app_settings.cpp, cheat_store.cpp, cfw_paths.cpp, theme_paths.cpp). None of the 5 deleted stub files were ever in the compile list — their removal does not affect compilation.

    Expected outcome: all tests pass (green), no compile errors. The test suite exercises the retained fake_cloud_save_client test double as before.

    If the suite fails:
    - Compile error referencing a deleted stub → a consumer reference was missed; inspect the error, identify the file, and patch it.
    - Compile error in a seam-collapsed file → the collapse in Plan 03 introduced a syntax error; read the failing file and correct it.
    - Test assertion failure → a seam collapse changed a path string that a test asserts on; review RESEARCH.md Section 4 warning about cfw_paths.cpp::base_present_for() returning false on host paths (this is expected and not a regression — the prior desktop paths also returned false on the test host).

    Do not edit tests/Makefile. Per RESEARCH.md Section 4, the Makefile needs zero changes.

    After the suite passes, confirm the retained test double was compiled:
      grep 'fake_cloud_save_client' tests/Makefile
    This should return the explicit SRCS entry confirming it is still part of the compile list.
  </action>

  <acceptance_criteria>
    - `make -C tests test` exits 0 (all tests pass)
    - `grep 'fake_cloud_save_client' tests/Makefile` returns the SRCS line (the retained double is still compiled by the suite)
    - No compile errors referencing any of the 9 deleted stub files
    - Test output shows zero failures
  </acceptance_criteria>

  <verify>
    <automated>make -C tests test</automated>
  </verify>

  <done>make -C tests test exits 0; fake_cloud_save_client is still compiled by the suite; no regressions from seam collapse or stub deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification commands → source tree | Grep/find commands confirm the tree state; a false negative (e.g. wrong working dir) could mask a remaining seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | fake_cloud_save_client deleted by Plan 02 (regression catch) | mitigate | success criterion 1 find command explicitly checks the retained files are present; test suite compile confirms they are reachable |
| T-05-12 | Tampering | Seam collapse in Plan 03 broke a host-compiled file (syntax error or wrong path) | mitigate | make -C tests test compile gate catches any syntax error introduced by Plan 03 edits; action provides diagnosis guidance |
| T-05-13 | Information Disclosure | Dangling #else desktop branch leaves a dead code path that could be confused with active logic in future phases | mitigate | SIMPL-02 grep gate (criterion 2) asserts zero remaining seams before test run |
</threat_model>

<verification>
Phase 5 is complete when all of the following pass in a single shell session from the repo root:

```bash
# Success Criterion 1 — SIMPL-01
find source/platform -name '*_fake*' | sort
# Expected output:
#   source/platform/saves/fake_cloud_save_client.cpp
#   source/platform/saves/fake_cloud_save_client.hpp

# Success Criterion 2 — SIMPL-02
grep -rn 'ifndef __SWITCH__\|#else' source/ | grep -v 'game_panel\|save_backup_io'
# Expected output: (empty)

# Success Criterion 3 — SIMPL-03
grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/ | grep -v 'fake_cloud_save_client'
# Expected output: (empty)

# Success Criterion 4 — VERIF-01
make -C tests test
# Expected: all tests pass, exit 0
```
</verification>

<success_criteria>
- All 4 success-criterion commands produce expected output
- make -C tests test passes with zero failures
- fake_cloud_save_client.{cpp,hpp} still exist and are still compiled by the suite
- Phase 5 gate confirmed; Phase 6 (Strip Desktop from Build System) may begin
</success_criteria>

## Artifacts This Phase Produces (Full Phase 5 Summary)

Files intentionally removed across this phase (all plans combined). Downstream drift verification should treat their absence as correct:

| Deleted File | Responsible Plan |
|---|---|
| source/platform/save_service_fake.cpp | Plan 02 |
| source/platform/save_service_fake.hpp | Plan 02 |
| source/platform/title_service_fake.cpp | Plan 02 |
| source/platform/title_service_fake.hpp | Plan 02 |
| source/platform/fake_auth_client.cpp | Plan 02 |
| source/platform/fake_auth_client.hpp | Plan 02 |
| source/platform/themes/firmware_extract_fake.cpp | Plan 02 |
| source/platform/sysmod/sysmod_store_fake.cpp | Plan 02 |
| source/platform/sysmod/sysmod_store_fake.hpp | Plan 02 |

Symbols no longer defined after this phase:
- `thomaz::FakeSaveService`
- `thomaz::FakeTitleService`
- `thomaz::FakeAuthClient`
- `thomaz::FakeSysmoduleStore`
- The fake `extract_all_base_layouts()` overload from firmware_extract_fake.cpp

Desktop path strings removed (no longer defined in any source/ file):
- `"thomaz-saves"` (was the desktop saves root)
- `"thomaz-mods"` (was the desktop mods root)
- `"thomaz-cache/locale.txt"`, `"thomaz-cache/api_url.txt"`, `"thomaz-cache/session.txt"`, `"thomaz-cache/save_sync.txt"`, `"thomaz-cache/versions.json"` (desktop cache paths)
- `"themes-out/contents"`, `"themes/systemData"` (desktop theme paths — latter shared string value with Switch but was inside a seam)
- `"themes"`, `"themes-out/exefs_patches/thomaz_themes"` (desktop theme paths)
- `"resources/theme-patches"` (desktop patch source)

<output>
Create `.planning/phases/05-collapse-source-seams-switch-only/05-04-SUMMARY.md` when done.
</output>
