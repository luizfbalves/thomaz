---
phase: 05-collapse-source-seams-switch-only
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - source/platform/save_service_fake.cpp
  - source/platform/save_service_fake.hpp
  - source/platform/title_service_fake.cpp
  - source/platform/title_service_fake.hpp
  - source/platform/fake_auth_client.cpp
  - source/platform/fake_auth_client.hpp
  - source/platform/themes/firmware_extract_fake.cpp
  - source/platform/sysmod/sysmod_store_fake.cpp
  - source/platform/sysmod/sysmod_store_fake.hpp
autonomous: true
requirements:
  - SIMPL-01

must_haves:
  truths:
    - "The 9 stub files no longer exist in the repository"
    - "find source/platform -name '*_fake*' returns only saves/fake_cloud_save_client.cpp and saves/fake_cloud_save_client.hpp"
    - "saves/fake_cloud_save_client.cpp and saves/fake_cloud_save_client.hpp still exist and are unmodified"
  artifacts:
    - path: source/platform/saves/fake_cloud_save_client.cpp
      provides: "Retained doctest double — must survive this plan"
    - path: source/platform/saves/fake_cloud_save_client.hpp
      provides: "Retained doctest double header — must survive this plan"
  key_links:
    - from: tests/Makefile
      to: source/platform/saves/fake_cloud_save_client.cpp
      via: explicit SRCS entry
      pattern: "fake_cloud_save_client"
---

<objective>
Delete the 9 desktop stub files that are no longer referenced after Plan 01 cleaned the consumer files.

Purpose: Completing SIMPL-01 — remove all five desktop stub pairs from source/platform/. After Plan 01 eliminated every include and factory reference to these files, deletion is now safe (no dangling references remain).

Output: 9 files removed from the repository. The retained doctest double (saves/fake_cloud_save_client.*) is explicitly NOT deleted.
</objective>

<execution_context>
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md
</execution_context>

<context>
@/home/solid/www/personal/playground/thomas/.planning/ROADMAP.md
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Plan 01 consumer references are clear, then delete 9 stub files (SIMPL-01, per RESEARCH.md Section 1 and Section 6 Step 3)</name>
  <files>
    source/platform/save_service_fake.cpp,
    source/platform/save_service_fake.hpp,
    source/platform/title_service_fake.cpp,
    source/platform/title_service_fake.hpp,
    source/platform/fake_auth_client.cpp,
    source/platform/fake_auth_client.hpp,
    source/platform/themes/firmware_extract_fake.cpp,
    source/platform/sysmod/sysmod_store_fake.cpp,
    source/platform/sysmod/sysmod_store_fake.hpp
  </files>

  <read_first>
    - .planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md — Section 1 (all five stub pair entries), Section 7 Risk 1, Section 6 Step 3, Section 3 (retained doctest double)
    - .planning/phases/05-collapse-source-seams-switch-only/05-01-SUMMARY.md — confirms Plan 01 consumer edits are complete
  </read_first>

  <action>
    Before deleting any file, run the pre-deletion safety check:

      grep -rn 'save_service_fake\|title_service_fake\|fake_auth_client\|sysmod_store_fake\|firmware_extract_fake\|FakeTitleService\|FakeSaveService\|FakeAuthClient\|FakeSysmoduleStore' source/

    If this returns any result outside the stub files themselves, STOP — Plan 01 did not complete correctly. Do not proceed until the consumer references are zero.

    If the check passes (zero results), delete the following 9 files using git rm or plain rm — both are acceptable; use git rm so the removals are staged:

      git -C /path/to/repo rm source/platform/save_service_fake.cpp
      git -C /path/to/repo rm source/platform/save_service_fake.hpp
      git -C /path/to/repo rm source/platform/title_service_fake.cpp
      git -C /path/to/repo rm source/platform/title_service_fake.hpp
      git -C /path/to/repo rm source/platform/fake_auth_client.cpp
      git -C /path/to/repo rm source/platform/fake_auth_client.hpp
      git -C /path/to/repo rm source/platform/themes/firmware_extract_fake.cpp
      git -C /path/to/repo rm source/platform/sysmod/sysmod_store_fake.cpp
      git -C /path/to/repo rm source/platform/sysmod/sysmod_store_fake.hpp

    CRITICAL — DO NOT delete:
      - source/platform/saves/fake_cloud_save_client.cpp
      - source/platform/saves/fake_cloud_save_client.hpp

    These two files are the retained doctest double (RESEARCH.md Section 3). They are in source/platform/saves/ (the saves/ subdirectory), not in source/platform/ root. They must continue to exist after this plan.

    Note per RESEARCH.md Section 1C: fake_auth_client.{cpp,hpp} has no #ifndef __SWITCH__ guard — it compiled unconditionally but was never instantiated on Switch (dead code). Deleting it removes dead code only; the IAuthClient interface and HttpAuthClient implementation are unaffected.

    Note per RESEARCH.md Section 1D: firmware_extract_fake.cpp has no corresponding .hpp to delete — only the .cpp file exists.

    Do not edit CMakeLists.txt or tests/Makefile. Per RESEARCH.md Section 4: CMake uses GLOB_RECURSE so the deleted files are automatically excluded on next configure; tests/Makefile never listed any of the 5 stubs.
  </action>

  <acceptance_criteria>
    - `find source/platform -name '*_fake*'` returns exactly two lines:
        source/platform/saves/fake_cloud_save_client.cpp
        source/platform/saves/fake_cloud_save_client.hpp
      (and nothing else)
    - `ls source/platform/save_service_fake.cpp 2>&1` exits non-zero (file does not exist)
    - `ls source/platform/title_service_fake.cpp 2>&1` exits non-zero
    - `ls source/platform/fake_auth_client.cpp 2>&1` exits non-zero
    - `ls source/platform/themes/firmware_extract_fake.cpp 2>&1` exits non-zero
    - `ls source/platform/sysmod/sysmod_store_fake.cpp 2>&1` exits non-zero
    - `ls source/platform/saves/fake_cloud_save_client.cpp` exits zero (retained file still exists)
    - `ls source/platform/saves/fake_cloud_save_client.hpp` exits zero (retained file still exists)
    - `grep -rn 'save_service_fake\|title_service_fake\|fake_auth_client\|sysmod_store_fake\|firmware_extract_fake' source/` returns nothing
  </acceptance_criteria>

  <verify>
    <automated>find source/platform -name '*_fake*' | sort</automated>
  </verify>

  <done>Exactly 9 stub files removed; find source/platform -name '*_fake*' returns only saves/fake_cloud_save_client.cpp and saves/fake_cloud_save_client.hpp; tests/Makefile and CMakeLists.txt untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion command → source tree | rm/git rm is irreversible; deleting fake_cloud_save_client.* would break the host test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Accidental deletion of saves/fake_cloud_save_client.* | mitigate | pre-deletion find check enumerates exact target list; action explicitly names the 9 files; acceptance_criteria asserts retained files still exist after deletion |
| T-05-05 | Tampering | Deleting files while consumer references still exist | mitigate | mandatory pre-deletion grep gate — STOP if any consumer reference is found; action requires Plan 01 SUMMARY to be read before proceeding |
| T-05-06 | Tampering | CMakeLists.txt or tests/Makefile inadvertent edit | accept | action explicitly instructs DO NOT edit either file; GLOB_RECURSE handles the deletion implicitly (per RESEARCH.md Section 4) |
</threat_model>

<verification>
After deletion:

```
find source/platform -name '*_fake*' | sort
```
Expected output (exactly two lines):
```
source/platform/saves/fake_cloud_save_client.cpp
source/platform/saves/fake_cloud_save_client.hpp
```

Confirm retained test double compiles with the suite (full gate in Plan 04):
```
ls source/platform/saves/fake_cloud_save_client.cpp source/platform/saves/fake_cloud_save_client.hpp
```
Both must exist.
</verification>

<success_criteria>
- 9 stub files removed; only fake_cloud_save_client.{cpp,hpp} remain in source/platform/ tree matching '*_fake*'
- No source/ file references any of the deleted stub class names
- CMakeLists.txt and tests/Makefile unchanged
</success_criteria>

## Artifacts This Phase Produces (Deletions)

Files intentionally removed by this plan (SIMPL-01). Downstream drift verification should treat absence of these files as correct, not as gaps:

| Deleted File | Reason |
|---|---|
| source/platform/save_service_fake.cpp | Desktop stub for FakeSaveService — deleted per SIMPL-01 |
| source/platform/save_service_fake.hpp | Desktop stub header — deleted per SIMPL-01 |
| source/platform/title_service_fake.cpp | Desktop stub for FakeTitleService — deleted per SIMPL-01 |
| source/platform/title_service_fake.hpp | Desktop stub header — deleted per SIMPL-01 |
| source/platform/fake_auth_client.cpp | Desktop dead-code stub for FakeAuthClient — deleted per SIMPL-01 |
| source/platform/fake_auth_client.hpp | Desktop dead-code stub header — deleted per SIMPL-01 |
| source/platform/themes/firmware_extract_fake.cpp | Desktop stub for firmware_extract_fake — deleted per SIMPL-01 |
| source/platform/sysmod/sysmod_store_fake.cpp | Desktop stub for FakeSysmoduleStore — deleted per SIMPL-01 |
| source/platform/sysmod/sysmod_store_fake.hpp | Desktop stub header — deleted per SIMPL-01 |

Symbols no longer defined after this plan:
- `thomaz::FakeSaveService`
- `thomaz::FakeTitleService`
- `thomaz::FakeAuthClient`
- `thomaz::FakeSysmoduleStore`
- `firmware_extract_fake` translation unit (no named class; the fake `extract_all_base_layouts()` overload)

<output>
Create `.planning/phases/05-collapse-source-seams-switch-only/05-02-SUMMARY.md` when done.
</output>
