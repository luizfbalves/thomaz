---
phase: 05-collapse-source-seams-switch-only
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - source/platform/game_stats.cpp
  - source/platform/app_settings.cpp
  - source/core/backup_store.cpp
  - source/core/mods/mod_paths.cpp
  - source/platform/feed/auth_store.cpp
  - source/platform/saves/sync_store.cpp
  - source/platform/cheat_store.cpp
  - source/platform/themes/cfw_paths.cpp
  - source/platform/themes/theme_paths.cpp
  - source/platform/themes/qlaunch_patches.cpp
  - source/platform/http_client_curl.cpp
  - source/platform/themes/theme_install.cpp
  - source/platform/sysmod/system_reboot.cpp
  - source/platform/curl_tls.hpp
  - source/platform/save_service.hpp
  - source/platform/auth_client.hpp
  - source/platform/sysmod/sysmod_store.hpp
  - source/platform/themes/firmware_extract.hpp
autonomous: true
requirements:
  - SIMPL-02
  - SIMPL-03

must_haves:
  truths:
    - "No #else desktop branch (path string or no-op body) survives in any of the 12 seam files"
    - "grep -rn '#ifndef __SWITCH__' source/ returns nothing"
    - "grep -rn '#else' source/ returns only _WIN32/localtime_r lines in game_panel.cpp and save_backup_io.cpp (Category C, untouched)"
    - "The 4 interface header comments no longer mention deleted fake files or desktop behaviour"
    - "grep -rnE '_fake' source/ returns only fake_cloud_save_client references"
  artifacts:
    - path: source/platform/game_stats.cpp
      provides: "Unconditional Switch pdmqry stats implementation"
    - path: source/platform/app_settings.cpp
      provides: "Unconditional Switch /switch/thomaz/config/ paths"
    - path: source/platform/curl_tls.hpp
      provides: "Unconditional Switch romfs CA bundle path"
    - path: source/platform/sysmod/system_reboot.cpp
      provides: "Unconditional Switch spsmShutdown implementation"
  key_links:
    - from: source/platform/curl_tls.hpp
      to: source/platform/http_client_curl.cpp
      via: "#include"
      pattern: "curl_tls"
---

<objective>
Collapse the 22 remaining #else desktop branches across 12 source files (path-string seams, no-op bodies, identity assignments, and the curl CA-bundle path), then remove the 4 stale desktop references from interface/header comments.

Purpose: SIMPL-02 requires no #else/#ifndef __SWITCH__ desktop branch to remain in any source/ file. SIMPL-03 requires no source/ file to reference a deleted stub or desktop-only symbol. This plan handles all seams that are NOT factory/include seams (those were Plan 01), completing the source-layer simplification alongside Plan 02's stub deletion.

Output: 12 source files with unconditional Switch paths/bodies; 4 headers with clean comments; zero #else desktop branches in source/.
</objective>

<execution_context>
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md
</execution_context>

<context>
@/home/solid/www/personal/playground/thomas/.planning/ROADMAP.md
@/home/solid/www/personal/playground/thomas/.planning/REQUIREMENTS.md
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse #else path-string and logic seams in 10 files (SIMPL-02, per RESEARCH.md Section 2 Category A and Section 6 Step 4)</name>
  <files>
    source/platform/game_stats.cpp,
    source/platform/app_settings.cpp,
    source/core/backup_store.cpp,
    source/core/mods/mod_paths.cpp,
    source/platform/feed/auth_store.cpp,
    source/platform/saves/sync_store.cpp,
    source/platform/cheat_store.cpp,
    source/platform/themes/cfw_paths.cpp,
    source/platform/themes/theme_paths.cpp,
    source/platform/themes/qlaunch_patches.cpp,
    source/platform/http_client_curl.cpp,
    source/platform/themes/theme_install.cpp,
    source/platform/sysmod/system_reboot.cpp,
    source/platform/curl_tls.hpp
  </files>

  <read_first>
    - .planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md — Section 2 Category A table (all rows except main.cpp and home_activity.cpp which are Plan 01), Section 6 Step 4 checklist, Section 2 Category C (DO NOT TOUCH these)
    - Each file being edited — read before touching; exact line numbers in RESEARCH.md are guidance but confirm actual content first since settings_activity.cpp and self_update.cpp are already modified (though those files are not in this list)
  </read_first>

  <action>
    Read each file before editing. RESEARCH.md Section 2 Category A lists the exact seams. Apply the collapse rule uniformly: when an `#ifdef __SWITCH__` block has an `#else` desktop block, remove the `#ifdef __SWITCH__` wrapper line, the `#else` line, the entire desktop block, and the `#endif` line — leaving only the Switch code unconditionally.

    Work through the 14 files in order:

    1. source/platform/game_stats.cpp (lines 10-56): The entire function body of `query_game_stats` is wrapped `#ifdef __SWITCH__` (Switch: pdmqry implementation) `#else` (desktop: deterministic fake stats) `#endif`. Remove the `#ifdef __SWITCH__` at line 10, the `#else` + fake stats block (approx lines 39-55), and the `#endif` at line 56. The pdmqry implementation becomes the unconditional body.

    2. source/platform/app_settings.cpp (lines 9-13 and 25-29): Two helper functions each have a `#ifdef __SWITCH__` path (Switch SD path) and `#else` path (desktop cache path). For each function: remove the guard wrapper and `#else` + desktop path, keep the Switch path string unconditionally.

    3. source/core/backup_store.cpp (lines 46-50): `saves_root()` return value — remove `#ifdef __SWITCH__` wrapper, `#else`, desktop `"thomaz-saves"` path, `#endif`; keep `/switch/thomaz/saves` unconditionally.

    4. source/core/mods/mod_paths.cpp (lines 7-10): `mod_staging_root()` — same pattern, keep `/switch/thomaz/mods` unconditionally.

    5. source/platform/feed/auth_store.cpp (lines 10-14): `session_file()` — keep `/switch/thomaz/config/session.txt` unconditionally.

    6. source/platform/saves/sync_store.cpp (lines 11-14): `sync_file()` — keep `/switch/thomaz/config/save_sync.txt` unconditionally.

    7. source/platform/cheat_store.cpp (lines 50-54): `index_cache_path()` — keep `/switch/thomaz/cache/versions.json` unconditionally.

    8. source/platform/themes/cfw_paths.cpp (lines 7-10 and 15-18): `layeredfs_root()` keeps `/atmosphere/contents`; `base_layout_dir()` keeps `/themes/systemData`. Two seams, same collapse pattern.

    9. source/platform/themes/theme_paths.cpp (lines 7-10): `themes_root()` keeps `/themes` unconditionally.

    10. source/platform/themes/qlaunch_patches.cpp: Two seams — `patch_src_dir()` (lines 16-19) keeps `romfs:/theme-patches`; `exefs_patches_dir()` (lines 26-29) keeps `/atmosphere/exefs_patches/thomaz_themes`. Per RESEARCH.md Section 6 Step 4 note 10: the `#ifdef __SWITCH__` warning block in `install_theme` (the `patches == 0` warning push, with only a `(void)patches` `#else`) keeps the Switch warning branch and removes the `#else (void)patches` branch.

    11. source/platform/http_client_curl.cpp (lines 32-42): Both Switch and desktop branches assign `networkReady = true` — this is an identity seam (RESEARCH.md Section 2 Category A). Remove the entire `#ifdef __SWITCH__ … networkReady = true; … #else … networkReady = true; … #endif` block and replace with a single `networkReady = true;` statement.

    12. source/platform/themes/theme_install.cpp (lines 63-69): Remove the `#else (void)patches; #endif` (the desktop no-op branch); keep the Switch warning-push branch unconditionally.

    13. source/platform/sysmod/system_reboot.cpp (lines 3-31): The entire function is `#ifdef __SWITCH__` (Switch: spsmShutdown impl) `#else` (desktop: `return false;`) `#endif`. Remove the guard wrapper; keep the Switch spsmShutdown implementation as the unconditional body.

    14. source/platform/curl_tls.hpp (lines 38-70): `#ifdef __SWITCH__` block uses romfs CA bundle path; `#else` block uses system CA store. Remove `#ifdef __SWITCH__` wrapper, `#else` + system-CA block, `#endif`; keep the romfs CA bundle path block unconditionally.

    DO NOT touch:
    - source/app/game_panel.cpp line 71 (`#if defined(_WIN32)` / `localtime_s` / `#else` / `localtime_r`) — Category C, unrelated to Switch
    - source/platform/saves/save_backup_io.cpp line 24 (same `_WIN32`/`localtime_r` pattern) — Category C
    - Any `#ifdef __SWITCH__` block that has NO `#else` (Category B) — per RESEARCH.md Section 2 Category B
    - source/platform/themes/firmware_extract_switch.cpp, key_loader_switch.cpp, nca_extract_switch.cpp — the `#ifdef __SWITCH__` wrapping entire file bodies must stay (prevents host-build compile errors)
    - CMakeLists.txt and tests/Makefile — zero changes in Phase 5
  </action>

  <acceptance_criteria>
    - `grep -rn '#ifndef __SWITCH__' source/` returns nothing
    - `grep -rn '#else' source/` returns only lines in source/app/game_panel.cpp and source/platform/saves/save_backup_io.cpp (the _WIN32/localtime_r Category C seams) — no other file
    - `grep -rn 'FakeTitleService\|FakeSaveService\|FakeSysmoduleStore\|FakeAuthClient\|thomaz-saves\|thomaz-mods\|thomaz-cache\|thomaz-cache' source/` returns nothing (desktop path strings gone)
    - `grep -c 'networkReady = true' source/platform/http_client_curl.cpp` returns 1 (single unconditional assignment, not 2 inside a seam)
    - `grep -n 'return false' source/platform/sysmod/system_reboot.cpp` returns nothing (desktop no-op gone)
  </acceptance_criteria>

  <verify>
    <automated>grep -rn '#ifndef __SWITCH__' source/ && echo "FAIL: #ifndef __SWITCH__ still present" || echo "PASS: no #ifndef __SWITCH__ seams"</automated>
  </verify>

  <done>All 22 Category A #else desktop branches are removed; Switch paths are unconditional in all 14 files; Category B guards and Category C _WIN32 guards are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Clean 4 interface/header comments that reference deleted stubs (SIMPL-03, per RESEARCH.md Section 6 Step 5)</name>
  <files>
    source/platform/save_service.hpp,
    source/platform/auth_client.hpp,
    source/platform/sysmod/sysmod_store.hpp,
    source/platform/themes/firmware_extract.hpp
  </files>

  <read_first>
    - .planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md — Section 6 Step 5 (exact locations of each comment)
    - Section 1A (save_service.hpp lines 11-12 comment)
    - Section 1C (auth_client.hpp line 12 comment)
    - Section 1E (sysmod_store.hpp line 20 comment)
    - Section 1D (firmware_extract.hpp lines 45-47 comment)
    - Each header file — read before editing
  </read_first>

  <action>
    Read each header file before editing. Apply comment-only edits (no logic changes) to remove stale desktop references:

    1. source/platform/save_service.hpp lines 11-12: The comment says something like "Switch impl uses libnx; the fake impl lets the full UI flow run on desktop without a console." Remove the "the fake impl lets …" half. Keep the part describing the Switch impl or simplify to just describe the Switch-only impl. Do not alter any code, only the comment text.

    2. source/platform/auth_client.hpp line 12: The comment includes "FakeAuthClient runs on the desktop;" (or similar phrasing). Remove that clause. The remaining comment should describe IAuthClient and HttpAuthClient only.

    3. source/platform/sysmod/sysmod_store.hpp line 20: The comment includes "the desktop fake (sysmod_store_fake) is in-memory" (or similar). Remove the desktop fake mention. The remaining comment should describe only the real SysmoduleStore.

    4. source/platform/themes/firmware_extract.hpp lines 45-47: A comment block that describes "Desktop behaviour (firmware_extract_fake.cpp): Returns {false, …}". Remove this entire comment block (the desktop behaviour description). The interface comment should describe only the Switch extraction behaviour.

    These are comment-only edits. Do not alter any declarations, definitions, include directives, or macro guards in these headers.
  </action>

  <acceptance_criteria>
    - `grep -n 'fake_impl\|fake impl\|desktop.*fake\|fake.*desktop\|firmware_extract_fake\|sysmod_store_fake\|FakeAuthClient.*desktop\|desktop.*FakeAuthClient' source/platform/save_service.hpp source/platform/auth_client.hpp source/platform/sysmod/sysmod_store.hpp source/platform/themes/firmware_extract.hpp` returns nothing
    - `grep -rnE '_fake' source/` returns only lines containing `fake_cloud_save_client` (the retained doctest double) — no other `_fake` reference
    - `grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/` returns only `fake_cloud_save_client` references (the full SIMPL-03 verification command from ROADMAP success criterion 3)
  </acceptance_criteria>

  <verify>
    <automated>grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/ | grep -v 'fake_cloud_save_client'</automated>
  </verify>

  <done>All 4 interface headers have no stale desktop or fake references in comments; the full SIMPL-03 grep returns only fake_cloud_save_client lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → source tree | Incorrect seam collapse could remove Switch-only code (Category B guards) instead of desktop branches (Category A guards), silently disabling Switch functionality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | Collapsing Category B (#ifdef no #else) guards | mitigate | action explicitly lists Category B guards to leave intact; acceptance_criteria grep for #else confirms only _WIN32 seams remain |
| T-05-08 | Tampering | Removing localtime_r _WIN32 seam (Category C) | mitigate | action explicitly instructs DO NOT TOUCH game_panel.cpp and save_backup_io.cpp _WIN32 seams |
| T-05-09 | Tampering | firmware_extract_switch.cpp #ifdef __SWITCH__ body deleted | mitigate | action names the three _switch.cpp files whose whole-file #ifdef guards must stay; these protect the host build from libnx compile errors |
| T-05-10 | Tampering | Comment edit accidentally alters a declaration in a header | mitigate | action states these are comment-only edits; acceptance_criteria asserts no functional symbol is removed (grep on class/function names would still match) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan |
</threat_model>

<verification>
After both tasks complete, run the full SIMPL-02 and SIMPL-03 verification sweep:

SIMPL-02 gate:
```
grep -rn 'ifndef __SWITCH__\|#else' source/
```
Expected: only lines from source/app/game_panel.cpp and source/platform/saves/save_backup_io.cpp (the _WIN32/localtime_r Category C patterns). Zero #else inside any #ifdef __SWITCH__ block.

SIMPL-03 gate:
```
grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/
```
Expected: only lines containing `fake_cloud_save_client`. No other `_fake` reference, no PLATFORM_DESKTOP, no SDL2, no GLFW.
</verification>

<success_criteria>
- 22 Category A #else desktop branches removed across 14 files
- 4 interface header comments cleaned of stale fake/desktop references
- grep -rn '#ifndef __SWITCH__' source/ returns nothing
- grep -rnE 'PLATFORM_DESKTOP|SDL2|GLFW|_fake' source/ returns only fake_cloud_save_client lines
- Category B guards (#ifdef __SWITCH__ with no #else) are untouched
- Category C _WIN32 seams are untouched
</success_criteria>

## Artifacts this phase produces

This is an edit-only plan — it creates **no new symbols, files, classes, functions, or flags**. Intentional removals (so downstream drift verification treats them as deletions, not gaps):
- Removed: 22 Category A `#else` / `#ifndef __SWITCH__` desktop branches across 14 `source/` files (the enclosing `#ifdef __SWITCH__` Switch path becomes unconditional in each).
- Removed: stale `*_fake` / desktop references in 4 interface-header comments (comment text only — no declarations changed).
- Untouched (NOT removed): Category B `#ifdef __SWITCH__` guards with no `#else`, and Category C `_WIN32`/`localtime_r` seams in `game_panel.cpp` and `save_backup_io.cpp`.

<output>
Create `.planning/phases/05-collapse-source-seams-switch-only/05-03-SUMMARY.md` when done.
</output>
