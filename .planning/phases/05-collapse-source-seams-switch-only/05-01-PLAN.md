---
phase: 05-collapse-source-seams-switch-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/main.cpp
  - source/app/home_activity.cpp
autonomous: true
requirements:
  - SIMPL-02
  - SIMPL-03

must_haves:
  truths:
    - "source/main.cpp contains no #ifdef __SWITCH__ / #else block selecting fake vs switch service implementations"
    - "source/main.cpp unconditionally includes title_service_switch.hpp, save_service_switch.hpp, and <switch.h>"
    - "source/main.cpp unconditionally instantiates NsTitleService and NsSaveService (no FakeTitleService / FakeSaveService)"
    - "source/app/home_activity.cpp contains no #ifndef __SWITCH__ include of sysmod_store_fake.hpp"
    - "source/app/home_activity.cpp unconditionally instantiates SysmoduleStore (no FakeSysmoduleStore)"
    - "No reference to save_service_fake, title_service_fake, or sysmod_store_fake remains in any source/ file"
  artifacts:
    - path: source/main.cpp
      provides: "Switch-only service factory — no desktop branch"
    - path: source/app/home_activity.cpp
      provides: "Switch-only sysmod store factory — no desktop branch"
  key_links:
    - from: source/main.cpp
      to: source/platform/title_service_switch.hpp
      via: unconditional #include
      pattern: "include.*title_service_switch"
    - from: source/main.cpp
      to: source/platform/save_service_switch.hpp
      via: unconditional #include
      pattern: "include.*save_service_switch"
    - from: source/app/home_activity.cpp
      to: source/platform/sysmod/sysmod_store.hpp
      via: unconditional factory
      pattern: "SysmoduleStore"
---

<objective>
Remove the three factory/include selection seams in main.cpp and home_activity.cpp that chose between *_fake and *_switch implementations, leaving only the Switch path unconditional.

Purpose: Consumer files must not reference stub headers before the stubs are deleted in Plan 02. This plan eliminates all consumer references so the tree never has a dangling #include mid-execution.

Output: main.cpp and home_activity.cpp with unconditional Switch factories; zero references to the three fake classes they previously selected.
</objective>

<execution_context>
@/home/solid/www/personal/playground/thomas/.planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md
</execution_context>

<context>
@/home/solid/www/personal/playground/thomas/.planning/ROADMAP.md
@/home/solid/www/personal/playground/thomas/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse main.cpp service-factory seams (SIMPL-02, per RESEARCH.md Section 6 Step 1)</name>
  <files>source/main.cpp</files>

  <read_first>
    - source/main.cpp — read in full before touching it; the seams are at lines 7-14, 70-79, 82-86, and 131-133 per RESEARCH.md
    - .planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md — Section 6 Step 1 and Section 2 Category A row for main.cpp
  </read_first>

  <action>
    Read source/main.cpp before making any edit.

    Apply three seam-collapse edits:

    1. Lines 7-14 (include seam): The block reads `#ifdef __SWITCH__` … `#include <switch.h>`, `#include "platform/title_service_switch.hpp"`, `#include "platform/save_service_switch.hpp"` … `#else` … `#include "platform/title_service_fake.hpp"`, `#include "platform/save_service_fake.hpp"` … `#endif`. Remove the `#ifdef __SWITCH__`, the `#else`, the two fake includes, and the `#endif`. Keep only the three Switch includes unconditionally.

    2. Lines 70-79 (title service factory): The block reads `#ifdef __SWITCH__` … `auto titleService = std::make_unique<thomaz::NsTitleService>();` + `init()` call … `#else` … `auto titleService = std::make_unique<thomaz::FakeTitleService>();` … `#endif`. Remove the `#ifdef __SWITCH__`, the `#else`, the fake factory line, and the `#endif`. Keep the NsTitleService factory + init() call unconditionally.

    3. Lines 82-86 (save service factory): Same pattern — `#ifdef __SWITCH__` / `NsSaveService` / `#else` / `FakeSaveService` / `#endif`. Remove the guard; keep `NsSaveService` unconditionally.

    Do NOT touch lines 131-133 (`#ifdef __SWITCH__ titleService->exit(); #endif`). That guard has no `#else` and is not a SIMPL-02 target per RESEARCH.md Section 2 Category B.

    Do NOT place fenced code blocks in this action — the current file content is in the source file itself. Read it first.
  </action>

  <acceptance_criteria>
    - `grep -n 'FakeTitleService\|FakeSaveService\|title_service_fake\|save_service_fake' source/main.cpp` returns nothing
    - `grep -n '#ifdef __SWITCH__' source/main.cpp` returns only the line 131-133 `titleService->exit()` block (one match)
    - `grep -n '#else' source/main.cpp` returns nothing
    - `grep -n 'NsTitleService\|NsSaveService' source/main.cpp` returns at least 2 matches (the factory lines)
    - `grep -n 'title_service_switch.hpp\|save_service_switch.hpp' source/main.cpp` returns 2 matches (unconditional includes)
  </acceptance_criteria>

  <verify>
    <automated>grep -n 'FakeTitleService\|FakeSaveService\|title_service_fake\|save_service_fake' source/main.cpp | wc -l | grep -x 0 && grep -n 'NsTitleService\|NsSaveService' source/main.cpp | wc -l</automated>
  </verify>

  <done>main.cpp has zero references to FakeTitleService/FakeSaveService; NsTitleService and NsSaveService are instantiated unconditionally.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse home_activity.cpp sysmod store seam (SIMPL-02, per RESEARCH.md Section 6 Step 2)</name>
  <files>source/app/home_activity.cpp</files>

  <read_first>
    - source/app/home_activity.cpp — read in full before touching it; the seams are at lines 14-16 and 94-98 per RESEARCH.md
    - .planning/phases/05-collapse-source-seams-switch-only/05-RESEARCH.md — Section 6 Step 2 and Section 1E
  </read_first>

  <action>
    Read source/app/home_activity.cpp before making any edit.

    Apply two seam-collapse edits:

    1. Lines 14-16 (include seam): A 3-line block: `#ifndef __SWITCH__` / `#include "platform/sysmod/sysmod_store_fake.hpp"` / `#endif`. Delete all three lines entirely. There is no Switch counterpart here — the real header (`sysmod_store.hpp`) is included unconditionally elsewhere in the file.

    2. Lines 94-98 (factory seam): The block reads `#ifdef __SWITCH__` / `auto store = std::make_shared<SysmoduleStore>();` / `#else` / `auto store = std::make_shared<FakeSysmoduleStore>();` / `#endif`. Remove the `#ifdef __SWITCH__`, the `#else`, the `FakeSysmoduleStore` factory line, and the `#endif`. Keep only `auto store = std::make_shared<SysmoduleStore>();` unconditionally.

    Do NOT touch any other #ifdef in this file. The note in RESEARCH.md Section 7 Risk 4 confirms settings_activity.cpp and self_update.cpp (different files, both in working tree with modifications) are not touched in this phase.
  </action>

  <acceptance_criteria>
    - `grep -n 'FakeSysmoduleStore\|sysmod_store_fake' source/app/home_activity.cpp` returns nothing
    - `grep -n '#ifndef __SWITCH__' source/app/home_activity.cpp` returns nothing
    - `grep -n 'SysmoduleStore' source/app/home_activity.cpp` returns at least 1 match (the unconditional factory)
    - `grep -rn 'save_service_fake\|title_service_fake\|sysmod_store_fake' source/` returns nothing (confirms both tasks together leave no consumer references)
  </acceptance_criteria>

  <verify>
    <automated>grep -rn 'save_service_fake\|title_service_fake\|sysmod_store_fake\|FakeTitleService\|FakeSaveService\|FakeSysmoduleStore' source/</automated>
  </verify>

  <done>home_activity.cpp has zero references to FakeSysmoduleStore or sysmod_store_fake; SysmoduleStore is instantiated unconditionally. No source/ file references any of the three fake classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → source tree | This plan modifies two source files; incorrect edits could silence a real Switch code path or introduce a compile error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | source/main.cpp seam collapse | mitigate | acceptance_criteria grep asserts exact presence of Switch factories and absence of fake references before proceeding to Plan 02 |
| T-05-02 | Tampering | Category B guard removal (lines 131-133) | mitigate | action explicitly instructs executor NOT to touch the no-#else guard; acceptance_criteria asserts exactly one #ifdef __SWITCH__ remains in main.cpp |
| T-05-03 | Tampering | Accidental deletion of fake_cloud_save_client | accept | this plan touches only main.cpp and home_activity.cpp; fake_cloud_save_client is in source/platform/saves/ and is not referenced by either file |
</threat_model>

<verification>
After both tasks complete, confirm the consumer seams are clear before Plan 02 proceeds to delete the stub files:

```
grep -rn 'save_service_fake\|title_service_fake\|fake_auth_client\|sysmod_store_fake' source/
```

Expected: zero output.

```
grep -rn 'FakeTitleService\|FakeSaveService\|FakeSysmoduleStore\|FakeAuthClient' source/
```

Expected: zero output (FakeAuthClient was never referenced outside its own files per RESEARCH.md Section 1C).
</verification>

<success_criteria>
- main.cpp: NsTitleService and NsSaveService factories are unconditional; no fake includes or factories remain
- home_activity.cpp: SysmoduleStore factory is unconditional; no fake include block remains
- grep across all source/ for the three fake class/file names returns zero results
- The retained fake_cloud_save_client is NOT touched by this plan
</success_criteria>

<output>
Create `.planning/phases/05-collapse-source-seams-switch-only/05-01-SUMMARY.md` when done.
</output>
