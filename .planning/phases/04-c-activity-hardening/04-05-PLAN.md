---
phase: 04-c-activity-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_save_sync.cpp
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "A host doctest covers the classify->plan_push decision composition that the cloud-save doUpload path relies on (TEST-04a)"
    - "The conflict case (cloud advanced since last sync) asserts SyncSituation::CloudAhead, isConflict==true, and the correct PUT revision"
    - "The clean-push case (in sync) asserts isConflict==false and the correct revision"
    - "The host doctest suite builds clean under C++17 and the new cases pass"
  artifacts:
    - path: "tests/test_save_sync.cpp"
      provides: "TEST-04a classify->plan_push composition cases"
      contains: "plan_push"
  key_links:
    - from: "tests/test_save_sync.cpp"
      to: "source/core/saves/save_sync.hpp"
      via: "calls classify() then plan_push() and asserts the composed decision"
      pattern: "classify"
---

<objective>
Close the TEST-04a gap: a host doctest for the cloud-save conflict-resolution / `plan_push` branch (D-05a). The genuine gap is NOT `plan_push` alone — `tests/test_save_sync.cpp` already covers `plan_push` per `SyncSituation` (RESEARCH Pitfall 3). The untested part is the DECISION COMPOSITION `classify(...) → plan_push(...)` that the `doUpload` path computes (`save_detail_activity.cpp` upload flow). Add cases that drive a realistic `(cloudExists, cloudRev, syncedRev)` triple through BOTH functions and assert the resulting `(revision, isConflict)` — the regression guard for the combined decision the UI relies on.

Purpose: TEST-04 (D-05) requires covering both branches: (a) this conflict/plan_push composition, and (b) the runAsync dropped-callback semantics (delivered as TEST-04b in Plan 01). This plan delivers (a). It is pure-core and Borealis-free (`classify`/`plan_push` live in `source/core/saves/save_sync.{hpp,cpp}`, compiled by the host suite), so it has no dependency on the activity migration and runs in Wave 1 parallel with Plan 01.

Output: extended `tests/test_save_sync.cpp` with the composition cases; host suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-c-activity-hardening/04-CONTEXT.md
@.planning/phases/04-c-activity-hardening/04-RESEARCH.md
@.planning/phases/04-c-activity-hardening/04-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_produced>
## Artifacts This Phase Produces (this plan's contribution)

- **Test cases** in `tests/test_save_sync.cpp` covering the `classify(...) → plan_push(...)` composition (TEST-04a) — at minimum a conflict case (CloudAhead → isConflict) and a clean-push case (InSync → no conflict), driven from a `(cloudExists, cloudRev, syncedRev)` triple.

(TEST-04b — the runAsync dropped-callback test — is delivered as `tests/test_async_guard.cpp` in Plan 01.)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the classify->plan_push composition doctest cases (TEST-04a)</name>
  <files>tests/test_save_sync.cpp</files>
  <behavior>
    - Conflict: classify(cloudExists=true, cloudRev=5, syncedRev=3) == SyncSituation::CloudAhead; plan_push(that, 5) has isConflict==true and revision==5.
    - Clean push (in sync): classify(true, 3, 3) == SyncSituation::InSync; plan_push(that, 3) has isConflict==false and revision==3.
    - (Optional third) New slot / no cloud yet: classify(cloudExists=false, ...) follows the existing enum semantics; plan_push reflects a non-conflict create. Use the actual enum values present in save_sync.hpp.
  </behavior>
  <read_first>
    - tests/test_save_sync.cpp (the existing file to extend — its includes, `using namespace thomaz::core;`, the existing `classify` cases 6-21 and `plan_push` cases 23-35 that confirm plan_push-alone is ALREADY covered)
    - source/core/saves/save_sync.hpp (the exact signatures: `SyncSituation classify(bool cloudExists, int cloudRevision, int syncedRevision);`, `struct PushPlan { int revision; bool isConflict; };`, `PushPlan plan_push(SyncSituation situation, int cloudRevision);`, and the `SyncSituation` enum values: InSync, CloudAhead, ...)
    - source/core/saves/save_sync.cpp (the actual classify/plan_push logic — so the asserted (revision,isConflict) match the implementation, not an assumption)
    - .planning/phases/04-c-activity-hardening/04-RESEARCH.md Pitfall 3 + §"TEST-04a" (target the COMPOSITION, not plan_push alone) and §"TEST-04a — decision-composition test"
    - .planning/phases/04-c-activity-hardening/04-PATTERNS.md §"tests/test_save_sync.cpp" (match existing doctest style: `#include "doctest.h"`, `#include "core/saves/save_sync.hpp"`, plain CHECK)
    - tests/Makefile (-std=c++17; tests/*.cpp auto-globbed; save_sync.cpp already in SRCS via the source/core/saves wildcard — no Makefile edit)
  </read_first>
  <action>
    Append new `TEST_CASE`s to `tests/test_save_sync.cpp` (do NOT duplicate the existing plan_push-per-situation cases — Pitfall 3). Each new case computes `auto sit = classify(cloudExists, cloudRev, syncedRev);` then `auto plan = plan_push(sit, cloudRev);` and asserts the composed `(sit, plan.isConflict, plan.revision)` with `CHECK`. Cover at least the conflict triple `(true, 5, 3)` → CloudAhead/isConflict/revision==5 and the in-sync triple `(true, 3, 3)` → InSync/!isConflict/revision==3. Read `save_sync.cpp` first and assert the values the implementation actually returns (do not hardcode an assumed revision). Match the existing file's include/namespace/style exactly. No Borealis, no `source/app/*` — this is pure core. The file is auto-globbed; no Makefile change.
  </action>
  <verify>
    <automated>make -C tests >/dev/null 2>&1; rc=$?; grep -q "classify" tests/test_save_sync.cpp && grep -q "plan_push" tests/test_save_sync.cpp && grep -q "isConflict" tests/test_save_sync.cpp && ! grep -qi "borealis" tests/test_save_sync.cpp && echo "GREP_OK build_rc=$rc"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_save_sync.cpp` contains new cases that call `classify(` and then `plan_push(` on its result and assert `isConflict` and `revision` (the COMPOSITION, not plan_push with a hand-set SyncSituation — Pitfall 3).
    - At least the conflict (CloudAhead) and clean-push (InSync) triples are covered, with asserted values matching `save_sync.cpp`'s actual implementation.
    - No `borealis` include and no `source/app/*` reference (pure core).
    - `make -C tests` builds with zero errors under `-std=c++17 -Wall -Wextra` (no new warnings).
    - The compiled host test binary runs and ALL save_sync cases pass (exit 0). (Use the run target named in tests/Makefile.)
  </acceptance_criteria>
  <done>The cloud-save upload decision composition (`classify → plan_push`) is regression-guarded by host doctest cases that assert the conflict and clean-push outcomes against the real implementation, and the host suite passes — satisfying TEST-04a.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only change; no runtime trust boundary is added or crossed. The guarded logic (`classify`/`plan_push`) is pure in-process decision code over integers. |

## STRIDE Threat Register

Test-only addition. No production code path, network surface, auth, or input parsing changes.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering (silent regression) | the cloud-save conflict decision could regress unnoticed, allowing a stale-revision overwrite of cloud data | mitigate | This plan IS the mitigation: a host doctest asserts the `classify → plan_push` composition so a regression that drops the conflict flag (and would let `doUpload` overwrite newer cloud data) fails CI. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure in-repo test addition (RESEARCH: zero new dependencies). N/A. |

No high-severity (remotely exploitable) threat in this plan; it is a regression guard. Stated explicitly per the security contract.
</threat_model>

<verification>
- `tests/test_save_sync.cpp` contains the `classify → plan_push` composition cases (conflict + clean push), distinct from the existing plan_push-per-situation cases.
- Host suite builds clean under C++17 and all save_sync cases pass.
- No Borealis / app-layer dependency leaks into the test.
</verification>

<success_criteria>
- A host doctest covers the cloud-save conflict-resolution / `plan_push` branch as the composition `doUpload` relies on (ROADMAP Phase 4 success criterion 4; TEST-04a / D-05a).
- The asserted outcomes match the real `save_sync.cpp` implementation (not assumptions).
</success_criteria>

<output>
Create `.planning/phases/04-c-activity-hardening/04-05-SUMMARY.md` when done. Record: the exact triples tested, the asserted (revision, isConflict) values, and confirmation that the host suite passed under C++17.
</output>
