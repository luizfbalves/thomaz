---
phase: 02-full-extraction-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/platform/themes/cfw_paths.cpp
  - source/platform/themes/cfw_paths.hpp
  - source/platform/themes/firmware_extract.hpp
  - source/platform/themes/firmware_extract_fake.cpp
  - tests/test_cfw_paths.cpp
autonomous: true
requirements:
  - EXTRACT-01
must_haves:
  truths:
    - "target_map(\"common\") returns {\"0100000000001000\", \"common.szs\"} (D-01a)"
    - "base_szs_path(\"common\") returns base_layout_dir() + \"/common.szs\""
    - "ExtractAllResult struct is declared in firmware_extract.hpp with ok, systemic_error, failed_parts, written_parts fields"
    - "extract_all_base_layouts() is declared in firmware_extract.hpp"
    - "firmware_extract_fake.cpp stub compiles on desktop with the new signature"
    - "make test in tests/ passes with all cfw_paths doctest cases including common"
  artifacts:
    - path: "source/platform/themes/cfw_paths.cpp"
      provides: "target_map with 8 entries including common"
      contains: "if (target == \"common\")"
    - path: "source/platform/themes/firmware_extract.hpp"
      provides: "ExtractAllResult struct + extract_all_base_layouts declaration"
      exports: ["ExtractAllResult", "extract_all_base_layouts"]
    - path: "source/platform/themes/firmware_extract_fake.cpp"
      provides: "Desktop no-op for extract_all_base_layouts"
      contains: "extract_all_base_layouts"
    - path: "tests/test_cfw_paths.cpp"
      provides: "Doctest coverage for common target"
      contains: "common"
  key_links:
    - from: "source/platform/themes/cfw_paths.cpp"
      to: "lib/switchthemes/Common.cpp"
      via: "reconciliation — ThemeTargetInfo::QlaunchCommon confirms title 0100000000001000"
      pattern: "target == \"common\""
    - from: "source/platform/themes/firmware_extract_fake.cpp"
      to: "source/platform/themes/firmware_extract.hpp"
      via: "must implement extract_all_base_layouts() with correct return type"
      pattern: "extract_all_base_layouts"
---

<objective>
Add `common` to `target_map()` (D-01a), declare the Phase 2 multi-title result type and entry point
in the platform-neutral header, and update the desktop no-op stub — all host-testable, no BIS/SPL
symbols.

Purpose: Establishes the interface contract and data-model that Plan 02-02 implements, and adds the
missing `common.szs` entry that the extraction engine (D-01) will produce but the apply path also
needs. Host doctest coverage for the pure mapping logic satisfies success criterion 4.

Output: Updated cfw_paths.cpp (+common entry), updated firmware_extract.hpp (+ExtractAllResult,
+extract_all_base_layouts), updated firmware_extract_fake.cpp (stub for new entry point),
extended test_cfw_paths.cpp (common target assertions).

## Artifacts This Phase Produces

New symbols introduced by this plan (executor: reference these in implementations):

| Symbol | File | Kind |
|--------|------|------|
| `thomaz::ExtractAllResult` | `firmware_extract.hpp` | struct — `bool ok; std::string systemic_error; std::vector<std::string> failed_parts; std::vector<std::string> written_parts;` |
| `thomaz::extract_all_base_layouts()` | `firmware_extract.hpp` | function declaration (returns `ExtractAllResult`) |
| `target_map("common")` | `cfw_paths.cpp` | new arm — returns `TargetMap{"0100000000001000", "common.szs"}` |
</objective>

<execution_context>
@/home/solid/.claude/gsd-core/workflows/execute-plan.md
@/home/solid/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-full-extraction-engine/02-CONTEXT.md
@.planning/phases/02-full-extraction-engine/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `common` to target_map() and extend doctest coverage</name>
  <files>source/platform/themes/cfw_paths.cpp, tests/test_cfw_paths.cpp</files>
  <read_first>
    - source/platform/themes/cfw_paths.cpp — read lines 22-32 (target_map), line 34-37 (base_szs_path), lines 46-55 (base_present_for); the `common` entry goes before `return std::nullopt` (after the MyPage arm)
    - lib/switchthemes/Common.cpp — scan for `ThemeTargetInfo::QlaunchCommon` and `ThemeTargetList6` to confirm title_id 0x0100000000001000 and szs "/lyt/common.szs" (reconciliation check per PATTERNS.md)
    - source/platform/themes/cfw_paths.hpp — read in full to understand TargetMap struct; do NOT modify this file in this task (no new declarations needed)
    - tests/test_cfw_paths.cpp — read in full; extend existing TEST_CASE("target_map covers the known applet targets") with common assertions
  </read_first>
  <behavior>
    - target_map("common") returns TargetMap{"0100000000001000", "common.szs"}
    - base_szs_path("common") returns base_layout_dir() + "/common.szs"
    - target_map("Bogus") still returns nullopt (regression)
    - target_map("ResidentMenu")->title_id still == "0100000000001000" (regression)
    - base_present_for({"common"}) returns false when /themes/systemData/common.szs does not exist (exercised via existing test pattern)
  </behavior>
  <action>
    In cfw_paths.cpp: insert one line before `return std::nullopt;` — the new arm is:
    `if (target == "common") return TargetMap{"0100000000001000", "common.szs"};`
    No other lines change. The `base_present_for` and `base_szs_path` functions derive from `target_map` automatically — no changes needed there.

    In tests/test_cfw_paths.cpp: add the following assertions to the existing
    TEST_CASE("target_map covers the known applet targets"):
    - CHECK(target_map("common")->title_id == "0100000000001000") — confirms qlaunch title per ThemeTargetInfo::QlaunchCommon
    - CHECK(target_map("common")->szs == "common.szs")

    Add a new TEST_CASE("base_szs_path for common resolves correctly"):
    - CHECK(base_szs_path("common") == base_layout_dir() + "/common.szs")

    Run `make test` from the tests/ directory to confirm all cases pass (including the new ones).
    The Makefile already includes ../source/platform/themes/cfw_paths.cpp in SRCS — no Makefile change needed.
  </action>
  <verify>
    <automated>cd /home/solid/gsd-workspaces/temas/thomas/tests && make test 2>&1 | tail -20</automated>
  </verify>
  <done>
    `make test` exits 0; output shows "TEST SUITE PASSED" or equivalent doctest summary with zero failures.
    test_cfw_paths.cpp assertions for target_map("common") and base_szs_path("common") pass.
    No regression in existing target_map / base_present_for / output_szs_path cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Declare ExtractAllResult and extract_all_base_layouts in the platform-neutral header + desktop stub</name>
  <files>source/platform/themes/firmware_extract.hpp, source/platform/themes/firmware_extract_fake.cpp</files>
  <read_first>
    - source/platform/themes/firmware_extract.hpp — read in full (54 lines); current state: ExtractResult struct + extract_base_layout() declaration only; must NOT remove ExtractResult or extract_base_layout (spike callers may still reference them — keep both)
    - source/platform/themes/firmware_extract_fake.cpp — read in full (13 lines); must add a matching stub for extract_all_base_layouts() under #ifndef __SWITCH__
    - .planning/phases/02-full-extraction-engine/02-PATTERNS.md — section "firmware_extract.hpp (interface header)" shows the exact struct shape to add; section "firmware_extract_fake.cpp (platform service, no-op)" shows the stub return value
  </read_first>
  <action>
    In firmware_extract.hpp:
    1. Add `#include <vector>` after `#include <string>` (ExtractAllResult uses std::vector<std::string>)
    2. Add the ExtractAllResult struct inside namespace thomaz:
       - `bool ok;` — false only on systemic abort (D-02a: applet gate failed, BIS mount failed, key derivation failed)
       - `std::string systemic_error;` — non-empty only on systemic abort
       - `std::vector<std::string> failed_parts;` — per-part failure messages (D-02)
       - `std::vector<std::string> written_parts;` — canonical paths successfully written
    3. Declare: `ExtractAllResult extract_all_base_layouts();`
    Keep ExtractResult and extract_base_layout() as-is — do not remove or rename them.

    In firmware_extract_fake.cpp:
    Add a second function body inside the #ifndef __SWITCH__ block:
    `ExtractAllResult extract_all_base_layouts() { return {false, "Firmware extraction is only available on Switch.", {}, {}}; }`
    No libnx headers may be added. The file keeps its #ifndef __SWITCH__ / #endif guards unchanged.
    Verify the pattern mirrors firmware_extract_fake.cpp's existing style exactly.
  </action>
  <verify>
    <automated>cd /home/solid/gsd-workspaces/temas/thomas && grep -c "ExtractAllResult" source/platform/themes/firmware_extract.hpp && grep -c "extract_all_base_layouts" source/platform/themes/firmware_extract.hpp && grep -c "extract_all_base_layouts" source/platform/themes/firmware_extract_fake.cpp</automated>
  </verify>
  <done>
    firmware_extract.hpp contains the ExtractAllResult struct with the four fields (ok, systemic_error, failed_parts, written_parts) and the extract_all_base_layouts() declaration.
    firmware_extract_fake.cpp contains the desktop stub for extract_all_base_layouts() returning {false, "...", {}, {}} with zero libnx includes.
    grep commands each return count >= 1.
    No libnx/hactool/mbedtls type appears in either file (D-08 compliance preserved).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host test → cfw_paths.cpp | The doctest invokes target_map / base_szs_path directly on desktop; no privileged services involved |
| firmware_extract.hpp → callers | Interface header must remain type-neutral; any libnx type leaking here breaks the desktop build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | cfw_paths.cpp target_map() | mitigate | New `common` arm is additive — existing callers that never ask for "common" are unaffected; add regression tests to confirm existing targets unchanged |
| T-02-02 | Tampering | firmware_extract.hpp | mitigate | No libnx types in ExtractAllResult or its declaration; only std::string + std::vector<std::string>; verified by grep: no `<switch.h>` or libnx type names in the header |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — pure C++ source modifications only |
</threat_model>

<verification>
1. `make test` from tests/ exits 0 with zero test failures — covers cfw_paths doctest regressions + new common assertions.
2. `grep -c "if (target == \"common\")" source/platform/themes/cfw_paths.cpp` returns 1.
3. `grep -Eq 'switch\.h|nca_ctx_t|hactool|mbedtls' source/platform/themes/firmware_extract.hpp && echo FAIL || echo OK` outputs OK.
4. `grep -c "extract_all_base_layouts" source/platform/themes/firmware_extract_fake.cpp` returns at least 1.
</verification>

<success_criteria>
- target_map("common") returns TargetMap{"0100000000001000", "common.szs"} (verifiable via doctest)
- ExtractAllResult is declared in firmware_extract.hpp with fields: ok (bool), systemic_error (string), failed_parts (vector<string>), written_parts (vector<string>)
- extract_all_base_layouts() is declared in firmware_extract.hpp
- Desktop stub in firmware_extract_fake.cpp returns {false, "extraction is Switch-only", {}, {}} for extract_all_base_layouts()
- All host tests pass (make test in tests/)
- No libnx types in firmware_extract.hpp (D-08 invariant maintained)
</success_criteria>

<output>
Create `.planning/phases/02-full-extraction-engine/02-01-SUMMARY.md` when done.
</output>
