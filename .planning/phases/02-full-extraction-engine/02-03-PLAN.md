---
phase: 02-full-extraction-engine
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03
must_haves:
  truths:
    - "On hardware, extract_all_base_layouts() produces at minimum: ResidentMenu.szs, Entrance.szs, Flaunch.szs, Set.szs, Notification.szs, common.szs (qlaunch — EXTRACT-01)"
    - "On hardware, Psl.szs is present in /themes/systemData/ after the run (EXTRACT-02)"
    - "On hardware, MyPage.szs is present in /themes/systemData/ after the run (EXTRACT-03)"
    - "All extracted files land directly in /themes/systemData/ with no subdirectory nesting (D-03 flat layout)"
    - "base_present_for({\"ResidentMenu\",\"Entrance\",\"Flaunch\",\"Set\",\"Notification\",\"common\",\"Psl\",\"MyPage\"}) returns true after extraction"
  artifacts:
    - path: "/themes/systemData/ResidentMenu.szs"
      provides: "Hardware-extracted qlaunch base layout"
    - path: "/themes/systemData/common.szs"
      provides: "Hardware-extracted qlaunch common layout (D-01a)"
    - path: "/themes/systemData/Psl.szs"
      provides: "Hardware-extracted Psl base layout"
    - path: "/themes/systemData/MyPage.szs"
      provides: "Hardware-extracted MyPage base layout"
  key_links:
    - from: "/themes/systemData/*.szs"
      to: "cfw_paths::base_present_for()"
      via: "stat() check — each szs must be a regular file"
      pattern: "S_ISREG"
---

<objective>
Hardware validation checkpoint: run extract_all_base_layouts() on a real Nintendo Switch launched via
title takeover and confirm all three titles extract their layouts into /themes/systemData/.

Purpose: This is the only validation that can prove EXTRACT-01, EXTRACT-02, and EXTRACT-03. Code
correctness (plans 02-01 and 02-02) is necessary but not sufficient — actual BIS/SPL/lr behaviour on
hardware is required. This checkpoint closes Phase 2.

Output: Human-observed confirmation that all required szs files exist in /themes/systemData/ on the SD
card, with no systemic errors reported by the app. No new code is written by this plan.

## Artifacts This Phase Produces

This plan produces no code artifacts. It validates the artifacts produced by plans 02-01 and 02-02 on
hardware. The output is the human-verify approval that Phase 2 is complete.
</objective>

<execution_context>
@/home/solid/.claude/gsd-core/workflows/execute-plan.md
@/home/solid/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-full-extraction-engine/02-CONTEXT.md
@.planning/phases/02-full-extraction-engine/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure the build is committed and deployed</name>
  <files></files>
  <read_first>
    - .planning/phases/02-full-extraction-engine/02-02-SUMMARY.md — confirm plans 02-01 and 02-02 completed successfully
  </read_first>
  <action>
    Confirm the following before the hardware-verify checkpoint:
    1. Plans 02-01 and 02-02 SUMMARY files exist and show self-check PASSED.
    2. The Switch build produces a fresh thomaz.nro (CI or local `cmake --build build --target thomaz.nro`).
    3. Copy the fresh thomaz.nro to the Switch SD card at `/switch/thomaz/thomaz.nro` (or the standard hbmenu path).
    4. Ensure Atmosphère + hbloader is present on the target Switch (required for title takeover).

    If the build cannot be run locally (no devkitPro), confirm that CI has produced a green artifact
    from the commits produced by plans 02-01 and 02-02, download the artifact, and copy it to the SD.
  </action>
  <verify>
    <automated>git log --oneline -5</automated>
  </verify>
  <done>
    Commits from plans 02-01 and 02-02 are present in git log.
    thomaz.nro is on the Switch SD card.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 02-01 and 02-02 implemented the full extraction engine:
    - target_map() now includes common (D-01a)
    - nca_romfs_filter widened to /lyt/ prefix — captures all layouts from each title
    - extract_all_base_layouts() drives three titles (qlaunch/Psl/MyPage), opens one SPL session,
      collects per-part failures without aborting (D-02), aborts on systemic BIS/SPL failure (D-02a),
      validates each szs with Yaz0-decompress + SARC-unpack (D-04), writes flat to /themes/systemData/ (D-03)
  </what-built>
  <how-to-verify>
    1. Launch thomaz via title takeover:
       - From the Switch Home menu, HOLD the R button
       - While holding R, launch any installed GAME (not the Album/Gallery)
       - Keep holding R through the Nintendo logo → Homebrew menu opens in Application mode
       - Launch thomaz from the Homebrew menu

    2. Run the full extraction. In Phase 2 the entry point is the raw extract_all_base_layouts() call —
       it may be triggered via the existing "spike" hook or a temporary test button depending on how plans
       02-01/02-02 wired it. Check the app's current UI for an extraction trigger; if none exists yet
       (Phase 3 wires the UI action), the call may need to be invoked from a temporary debug path or the
       existing spike entry point.

       If thomaz.nro does not yet have a user-visible trigger for extract_all_base_layouts(), trigger
       it by calling the function from the existing single-target debug path with a temporary wrapper,
       or confirm with the developer the correct debug invocation for this build.

    3. After the run completes (expect several seconds for three NCA decryptions), check the SD card:
       Mount the SD card on a PC or use the Switch's album/file access path. Navigate to:
         /themes/systemData/

       Confirm the following files exist and are non-zero:
         [ ] ResidentMenu.szs  (EXTRACT-01 — qlaunch)
         [ ] Entrance.szs      (EXTRACT-01 — qlaunch)
         [ ] Flaunch.szs       (EXTRACT-01 — qlaunch)
         [ ] Set.szs           (EXTRACT-01 — qlaunch)
         [ ] Notification.szs  (EXTRACT-01 — qlaunch)
         [ ] common.szs        (EXTRACT-01 — qlaunch, D-01a)
         [ ] Psl.szs           (EXTRACT-02)
         [ ] MyPage.szs        (EXTRACT-03)

       Additional /lyt/*.szs files (DataTransfer, Eula, Gift, etc.) extracted from qlaunch are
       acceptable and expected (D-01 "all /lyt/*.szs") — do not fail on their presence.

    4. Confirm the app shows no systemic error ("SPL key derivation failed", "BIS mount failed", etc.).
       Per-part warnings for absent optional layouts are acceptable — note them if they appear.

    5. Optional but recommended: check /switch/thomaz/hactool.log on the SD for the per-title
       "diag:" lines; confirm header_key all_zero=0 (key derived successfully) and no "Invalid NCA header" lines.

    Record the firmware version shown in the log (format: major.minor.micro) — needed for THIRD_PARTY.md
    provenance. The firmware version was already recorded in Phase 1; if this run is on a different
    firmware, record the new version as well.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 8 szs files are present and non-zero.
    Or describe any failures (which titles failed, error message shown, hactool.log contents).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hardware NCA → extracted szs | Actual on-device BIS/SPL/lr/hactool execution; validation gate (D-04) was already applied in plan 02-02 before write |
| SD card → human-visible files | Human reads file list; no new code surfaces in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Info Disclosure | Extracted Nintendo .szs on SD card | accept | Files are copyrighted Nintendo layout data written to the user's own SD only; thomaz never uploads or distributes them; this is the intended use of the extraction engine |
| T-02-10 | Tampering | Stale szs from prior extraction run | mitigate | D-03 overwrite-in-place ensures a fresh run replaces any stale file; validated by the checkpoint: re-running should produce the same file sizes |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
Hardware verification is the only valid check for EXTRACT-01, EXTRACT-02, EXTRACT-03 success criteria 1-3.
The checkpoint approval signal is the acceptance gate for this plan.

Additional confirmation available from hactool.log:
- `grep "diag: header_key all_zero=0" /switch/thomaz/hactool.log` — confirms key derivation succeeded for each title
- `ls -la /themes/systemData/*.szs` — file sizes should be non-zero (typical: 50 KB – 2 MB per szs)
</verification>

<success_criteria>
- On hardware: /themes/systemData/ contains at minimum ResidentMenu.szs, Entrance.szs, Flaunch.szs, Set.szs, Notification.szs, common.szs, Psl.szs, MyPage.szs — all non-zero (EXTRACT-01, EXTRACT-02, EXTRACT-03)
- No systemic error from the extraction run (SPL key derivation succeeded, BIS mounted, all three NCAs resolved)
- All files are in /themes/systemData/ directly — no subdirectory layout (D-03 flat confirmed)
- Human approves the checkpoint with "approved" signal
</success_criteria>

<output>
Create `.planning/phases/02-full-extraction-engine/02-03-SUMMARY.md` when done.
</output>
