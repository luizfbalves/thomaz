---
phase: 02-api-security-regression-tests
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - api/test/api.test.ts
autonomous: true
requirements: [SEC-01, TEST-01, TEST-02]
must_haves:
  truths:
    - "A direct path to a save blob (GET /uploads/saves/<userId>/<titleId>.bin) returns 404 — no static route serves it"
    - "A PUT to an existing slot without a revision field returns 400 {ok:false,error:'revision_required'}"
    - "All four saves PUT revision branches are asserted: new-slot 200, matching-revision 200, revision_conflict 409, revision_required 400"
    - "A token reused after POST /auth/logout returns 401 {ok:false,error:'unauthorized'}; a sibling valid token for the same user returns 200"
    - "The full Vitest suite passes with no spurious log output"
  artifacts:
    - path: "api/test/api.test.ts"
      provides: "TEST-01 (save-blob 404 guard), TEST-02 (revision_required branch), SEC-02 (revoked-token rejection) regression tests"
      contains: "uploads/saves"
  key_links:
    - from: "api/test/api.test.ts TEST-01"
      to: "app.inject GET /uploads/saves/..."
      via: "status assertion 404"
      pattern: "uploads/saves"
    - from: "api/test/api.test.ts SEC-02"
      to: "POST /auth/logout then GET /saves"
      via: "revoked token 401, sibling token 200"
      pattern: "/auth/logout"
---

<objective>
Lock every Phase 2 security fix behind regression tests by extending `api/test/api.test.ts`: TEST-01 asserts no static route serves a save blob (SEC-01 regression guard), TEST-02 fills the one missing saves-PUT revision branch (`revision_required` 400), and a SEC-02 test proves a logged-out access token is rejected while a sibling token still works.

Purpose: SEC-01's fix (removing `@fastify/static`) landed in Phase 1; Phase 2 delivers TEST-01 as the binding behavioral guard so a future re-introduction of static serving fails CI. TEST-02 completes the four revision branches (three already exist). The SEC-02 test verifies the revocation behavior built in Plan 02 end-to-end against the real test Postgres.
Output: three new test cases extending the existing `app.inject()` suite, all green.
</objective>

<artifacts_this_phase_produces>
This plan introduces:
- TEST-01: an `it("TEST-01: direct save-blob path is not publicly accessible", ...)` asserting `GET /uploads/saves/<userId>/<titleId>.bin` → 404.
- TEST-02: an `it(...)` asserting a PUT to an existing slot WITHOUT a `revision` field → 400 `{ ok:false, error:"revision_required" }` (the only previously-uncovered branch; the other three exist in the current "saves list, upload, download with revision" test).
- SEC-02: an `it(...)` minting two tokens for one user, logging out with the first (bearer in `Authorization` + its refreshToken in body), asserting the first → `GET /saves` 401 `{ok:false,error:"unauthorized"}` and the second → 200.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-security-regression-tests/02-CONTEXT.md
@.planning/phases/02-api-security-regression-tests/02-RESEARCH.md
@.planning/phases/02-api-security-regression-tests/02-PATTERNS.md
@.planning/phases/02-api-security-regression-tests/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TEST-01 (save-blob 404 guard) and TEST-02 (revision_required 400 branch)</name>
  <files>api/test/api.test.ts</files>
  <read_first>
    - api/test/api.test.ts (plain inject pattern lines 39-43 for TEST-01; the multipart FormData PUT pattern lines 186-203 and 241-281 for TEST-02; error-envelope assertion lines 277-281; the register→token flow lines 161-184)
    - api/src/routes/saves.ts (lines 144-154 — the four revision branches; `revision_required` at 146-148 is the only one not yet covered)
    - .planning/phases/02-api-security-regression-tests/02-PATTERNS.md §"api/test/api.test.ts" (TEST-01 + TEST-02 excerpts) and §"Shared Patterns" (error envelope, FormData)
    - .planning/phases/02-api-security-regression-tests/02-RESEARCH.md §"TEST-01" and §"TEST-02" code examples + completeness check
  </read_first>
  <action>
    TEST-01 (SEC-01 guard): add an `it("TEST-01: direct save-blob path is not publicly accessible", ...)` inside the existing `describe("thomaz-api", ...)`. Use the plain `app.inject({ method: "GET", url: "/uploads/saves/some-user-id/01008BB901469000.bin" })` style (no auth header needed) and assert `res.statusCode === 404` — no static route serves `uploads/`. The exact userId/titleId fixture in the URL is the executor's call; any well-formed path under `/uploads/saves/` works because nothing serves it.

    TEST-02 (revision_required branch): add an `it(...)` that registers a fresh user, PUTs once to create a slot (revision 1) using the existing FormData multipart pattern (`form.append("data", Buffer.from(...), { filename, contentType: "application/octet-stream" })` + `headers: { authorization: \`Bearer ${token}\`, ...form.getHeaders() }`), then PUTs again to the SAME titleId WITHOUT appending a `revision` field. Assert `res.statusCode === 400` and `res.json()` equals `{ ok: false, error: "revision_required" }` — this exercises saves.ts line 146-148. Use a unique titleId/username (e.g. derived from `Date.now()`) to avoid collisions with the shared app's other tests. Confirm the four-branch coverage is now complete: new-slot 200 + matching-revision 200 + revision_conflict 409 already live in "saves list, upload, download with revision"; this adds the fourth.
  </action>
  <verify>
    <automated>cd api && grep -q "uploads/saves" test/api.test.ts && grep -q "revision_required" test/api.test.ts && npx vitest run test/api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `api/test/api.test.ts` contains a test injecting `GET /uploads/saves/<userId>/<titleId>.bin` and asserting `statusCode === 404`
    - `api/test/api.test.ts` contains a test that PUTs to an existing slot with no `revision` field and asserts `statusCode === 400` and `json()` equals `{ ok: false, error: "revision_required" }`
    - The new TEST-02 case uses the existing FormData + `form.getHeaders()` multipart pattern (not hand-built boundaries)
    - All four saves-PUT revision branches are now asserted across the suite (new-slot 200, matching 200, conflict 409, required 400)
    - `npx vitest run test/api.test.ts` exits 0
  </acceptance_criteria>
  <done>TEST-01 guards that no save-blob path is publicly served (404) and TEST-02 covers the previously-untested revision_required (400) branch; the saves PUT revision matrix is fully asserted and the suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add SEC-02 revoked-token regression test</name>
  <files>api/test/api.test.ts</files>
  <read_first>
    - api/test/api.test.ts (the register→login→token flow lines 88-153 "auth refresh and logout" as the closest analog; the authenticated `GET /saves` with `Bearer` header lines 128-133 and 168-176)
    - .planning/phases/02-api-security-regression-tests/02-RESEARCH.md §"Revoked-token regression coverage (SEC-02)" and §"Test-env caveat"
    - .planning/phases/02-api-security-regression-tests/02-PATTERNS.md §"api/test/api.test.ts" SEC-02 paragraph
    - .planning/phases/02-api-security-regression-tests/02-02-SUMMARY.md (confirms logout best-effort revoke + blocklist enforcement are implemented)
  </read_first>
  <action>
    Add an `it("SEC-02: logged-out access token is rejected; other valid tokens still work", ...)` inside `describe("thomaz-api", ...)`. Register a fresh user (unique username from `Date.now()`), then `POST /auth/login` TWICE to mint two independent sessions: tokenA1 + refreshA1 (the session to be logged out) and tokenA2 + refreshA2 (the session kept alive). Since both tokens are minted by the new code, each carries a `jti`. Call `POST /auth/logout` with `headers: { authorization: \`Bearer ${tokenA1}\` }` and `payload: { refreshToken: refreshA1 }`; assert it returns 200 `{ ok: true }`. Then assert `GET /saves` with `authorization: Bearer ${tokenA1}` returns `statusCode === 401` and `json()` equals `{ ok: false, error: "unauthorized" }` (D-03 generic shape). Then assert `GET /saves` with `authorization: Bearer ${tokenA2}` returns `statusCode === 200` (sibling token unaffected — only the logged-out jti is blocklisted).

    No logging setup is needed: with `NODE_ENV=test` the logger is `false`, so the blocklist `findUnique` and any fail-open warn path run silently — the suite stays clean (Success Criterion 4). The test relies on the `RevokedToken` migration already applied to the test Postgres by Plan 01.
  </action>
  <verify>
    <automated>cd api && grep -q "SEC-02" test/api.test.ts && grep -q "/auth/logout" test/api.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `api/test/api.test.ts` contains a SEC-02 test that mints two tokens for one user, logs out the first (bearer header + refreshToken body), and asserts the logout returns 200
    - The logged-out token's subsequent `GET /saves` returns `statusCode === 401` with `json()` equal to `{ ok: false, error: "unauthorized" }`
    - The sibling (non-logged-out) token's `GET /saves` returns `statusCode === 200`
    - The full suite `npm test` (vitest run) exits 0 with no spurious log output
  </acceptance_criteria>
  <done>The SEC-02 test proves a logged-out access token is rejected with the generic 401 while a sibling token for the same user still authenticates; the full suite passes silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /uploads/saves/* | A predictable save-blob URL must not be served by any static route |
| client → protected /saves | A revoked token must be rejected; a valid sibling token must pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | Predictable save-blob URL enumeration | mitigate | TEST-01 is the binding regression guard asserting `GET /uploads/saves/<userId>/<titleId>.bin` → 404; if static serving is ever re-introduced, this test fails CI (SEC-01) |
| T-02-10 | Spoofing | Regression that silently un-revokes logged-out tokens | mitigate | SEC-02 test asserts the logged-out token → 401 and the sibling token → 200, locking the Plan 02 blocklist behavior against future regressions |
| T-02-11 | Tampering | Saves revision-check regression (missing-revision accepted) | mitigate | TEST-02 asserts the `revision_required` 400 branch, completing the four-branch matrix so a weakened revision check fails CI |
</threat_model>

<verification>
- `cd api && npm test` exits 0 (full suite green, no spurious output — NODE_ENV=test → logger false).
- `grep -c "uploads/saves" api/test/api.test.ts` is ≥ 1 (TEST-01 present).
- `grep -c "revision_required" api/test/api.test.ts` is ≥ 1 (TEST-02 present).
- `grep -c "SEC-02" api/test/api.test.ts` is ≥ 1 (revoked-token test present).
- Precondition: the test Postgres at `localhost:5433` has the `RevokedToken` migration applied (delivered by Plan 01) — else the SEC-02 test fails at runtime on the missing table.
</verification>

<success_criteria>
- TEST-01: `GET /uploads/saves/...` → 404 (SEC-01 regression guard in place).
- TEST-02: the `revision_required` (400) branch is covered; all four PUT revision branches asserted.
- SEC-02: logged-out token → 401; sibling token → 200.
- The full Vitest suite passes silently.
</success_criteria>

<output>
Create `.planning/phases/02-api-security-regression-tests/02-03-SUMMARY.md` when done. List the three new test names, confirm `npm test` is green, and note the four-branch revision coverage is complete.
</output>
