---
phase: 07-docs-cleanup-final-verification-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: true
requirements:
  - VERIF-01
must_haves:
  truths:
    - "The host doctest suite builds and passes: make -C tests test is green"
    - "The host doctest suite still compiles the retained source/platform/saves/fake_cloud_save_client.* test double"
    - "The Switch build via scripts/build-switch.sh produces build_switch/thomaz.nro"
    - "Both single-target gates (host doctest + Switch build) are confirmed green together as the v1.1 verification flow"
  artifacts:
    - path: "build_switch/thomaz.nro"
      provides: "Clean Switch build output proving the stripped tree still compiles"
  key_links:
    - from: "tests/Makefile"
      to: "source/platform/saves/fake_cloud_save_client.cpp"
      via: "host doctest suite compiles the retained test double"
      pattern: "fake_cloud_save_client"
    - from: "scripts/build-switch.sh"
      to: "build_switch/thomaz.nro"
      via: "Switch build output"
      pattern: "build_switch/thomaz\\.nro"
---

<objective>
Run the milestone's two single-target verification gates together and confirm
both are green: the host doctest suite (make -C tests test) and the Switch build
(scripts/build-switch.sh -> build_switch/thomaz.nro). This is the final combined
gate of milestone v1.1, run after the docs cleanup in Plan 07-01.

Purpose: Satisfies VERIF-01 and Phase 7 success criteria #2 and #3 — the host
doctest suite passes unchanged (still compiling the retained
saves/fake_cloud_save_client.* test double), and the Switch build is clean,
standing the two single-target gates up as the documented verification flow.
No application behavior or source code is changed by this plan.

Output: Confirmed-green run records of both gates captured in the SUMMARY (host
doctest pass count; presence of build_switch/thomaz.nro).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-docs-cleanup-final-verification-gate/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@scripts/build-switch.sh
@tests/Makefile
</context>

<artifacts_this_phase_produces>
This is a verification plan. No new code symbols, types, or source files are
created. It produces a build artifact and run records only:
- `build_switch/thomaz.nro` — clean Switch build output (regenerated, not committed).
- Run records captured in `07-02-SUMMARY.md`: host doctest pass count and the
  Switch build result. No source files are modified.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the host doctest gate and confirm green (with retained test double)</name>
  <files>tests/Makefile (read-only verification target; no files modified)</files>
  <read_first>
    - tests/Makefile (the gate: `make -C tests test`; confirm the SRCS list includes `../source/platform/saves/fake_cloud_save_client.cpp`, the retained doctest double)
    - .planning/phases/07-docs-cleanup-final-verification-gate/07-CONTEXT.md (D-06 gate 1; code_context note: this suite passed 209/209 earlier this session)
    Environment note for THIS machine (not for any doc): the host doctest gate runs in the devkitPro MSYS2 login shell with msys2 gcc; if a POSIX-signal compile error appears, build with `CXXFLAGS+=' -DDOCTEST_CONFIG_NO_POSIX_SIGNALS'` (e.g. invoke via `/c/devkitPro/msys2/usr/bin/bash.exe -lc 'cd <repo> && make -C tests test CXXFLAGS="... -DDOCTEST_CONFIG_NO_POSIX_SIGNALS"'`). Prefer the plain `make -C tests test` first; only add the flag if the host gcc requires it.
  </read_first>
  <action>
    Run the host doctest gate: `make -C tests test`. Confirm it builds and all test
    cases pass (expected ~209/209 from this session; the exact count may shift, but
    the run MUST be all-green with zero failures). Confirm from tests/Makefile that
    the SRCS list still includes `../source/platform/saves/fake_cloud_save_client.cpp`
    so the retained test double is compiled by the suite (VERIF-01). Do NOT modify
    any source or test file — this is a verification run. If the host gcc needs the
    DOCTEST_CONFIG_NO_POSIX_SIGNALS define (machine quirk), apply it via CXXFLAGS at
    invocation only; do not edit tests/Makefile.
  </action>
  <verify>
    <automated>make -C tests test</automated>
  </verify>
  <acceptance_criteria>
    - `make -C tests test` exits 0 with all doctest cases passing (zero failures); record the pass count in the SUMMARY.
    - tests/Makefile SRCS includes `../source/platform/saves/fake_cloud_save_client.cpp` (the retained test double is compiled by the suite).
    - No source or test file is modified by this task (`git status` shows no changes under source/ or tests/).
  </acceptance_criteria>
  <done>The host doctest suite is green and still compiles the retained fake_cloud_save_client test double.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Switch build gate and confirm build_switch/thomaz.nro is produced</name>
  <files>build_switch/thomaz.nro (build output; no source files modified)</files>
  <read_first>
    - scripts/build-switch.sh (the gate: Docker `devkitpro/devkita64` default, native when `DEVKITPRO` is set; output `build_switch/thomaz.nro`)
    - .planning/phases/07-docs-cleanup-final-verification-gate/07-CONTEXT.md (D-06 gate 2; code_context note: build-switch.sh produced a working thomaz.nro this session that ran on hardware)
    Environment note for THIS machine (not for any doc): Docker may be unavailable here; the native devkitPro path is the proven route — run via `/c/devkitPro/msys2/usr/bin/bash.exe -lc 'cd <repo> && DEVKITPRO=/c/devkitPro bash scripts/build-switch.sh'` (or the repo's established native invocation). Use whichever path (Docker or native) is available; either satisfies the gate as long as build_switch/thomaz.nro is produced.
  </read_first>
  <action>
    Run the Switch build gate: `scripts/build-switch.sh`. By default it builds inside
    the devkitPro Docker image; if Docker is unavailable on this machine, run the
    proven native devkitPro path (set DEVKITPRO and invoke through the MSYS2 login
    shell — see read_first note). Confirm the build completes end-to-end and produces
    `build_switch/thomaz.nro`. Do NOT modify any source, CMake, or script file — this
    is a verification run only. Record which path (Docker vs native) was used and the
    resulting artifact size in the SUMMARY.
  </action>
  <verify>
    <automated>test -f build_switch/thomaz.nro</automated>
  </verify>
  <acceptance_criteria>
    - The build completes end-to-end with no errors.
    - `build_switch/thomaz.nro` exists after the run (`test -f build_switch/thomaz.nro` exits 0).
    - No source, CMake, or script file is modified by this task (`git status` shows no tracked-file changes).
    - The SUMMARY records the build path used (Docker or native devkitPro) and the artifact size.
  </acceptance_criteria>
  <done>scripts/build-switch.sh produces build_switch/thomaz.nro; both single-target gates are confirmed green together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build toolchain | scripts/build-switch.sh may pull the `devkitpro/devkita64` Docker image and runs `git submodule update --init --recursive`. These are established, pre-existing project mechanisms (unchanged by this milestone); no new inputs, network endpoints, or privilege boundaries are introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | Docker image / submodules pulled by build-switch.sh | accept | Pre-existing, unchanged build mechanism (devkitpro/devkita64 is the official CI image; submodules are pinned in the repo). This plan only runs the existing script — it does not add or alter dependencies. |
| T-07-04 | Denial of Service | local build resources (CPU/disk during compile) | accept | Local developer-machine build only; transient resource use, no external impact. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan; the Switch build uses the pre-bundled devkitPro portlibs from the official image. |

No high-severity threats. Realistic surface is minimal: running the project's
existing, unchanged verification gates locally — no new inputs, network endpoints,
or privilege boundaries.
</threat_model>

<verification>
- Host doctest gate green: `make -C tests test` exits 0, all cases pass, and the
  retained `saves/fake_cloud_save_client.*` double is compiled (per tests/Makefile SRCS).
- Switch build gate green: `scripts/build-switch.sh` produces `build_switch/thomaz.nro`.
- Both gates confirmed together as the v1.1 verification flow (Phase 7 success
  criteria #2 and #3).
</verification>

<success_criteria>
- The host doctest suite passes unchanged, still compiling the retained test double (VERIF-01, success criterion #2).
- The Switch build is clean and produces build_switch/thomaz.nro (success criterion #3).
- The two single-target gates stand together as the milestone's verification flow.
- VERIF-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/07-docs-cleanup-final-verification-gate/07-02-SUMMARY.md` when done.
</output>
