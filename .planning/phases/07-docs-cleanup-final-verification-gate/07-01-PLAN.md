---
phase: 07-docs-cleanup-final-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "README.md no longer instructs or implies a desktop PC build"
    - "README.md points the Switch build at scripts/build-switch.sh (Docker default, native when DEVKITPRO set)"
    - "README.md documents the two single-target verification gates: host doctest (make -C tests test) and Switch build (scripts/build-switch.sh -> build_switch/thomaz.nro)"
    - "README.md mentions deploying thomaz.nro to a CFW Switch (copy to /switch/thomaz.nro or push via nxlink)"
    - "The living-doc desktop-build grep gate returns nothing"
    - "CMakeLists.txt header comment is Switch-only (verified, unchanged)"
  artifacts:
    - path: "README.md"
      provides: "Switch-only build + two-gate verification documentation in pt-BR"
      contains: "scripts/build-switch.sh"
  key_links:
    - from: "README.md build section"
      to: "scripts/build-switch.sh"
      via: "documented build entrypoint"
      pattern: "scripts/build-switch\\.sh"
    - from: "README.md verification section"
      to: "tests/Makefile"
      via: "documented host doctest gate"
      pattern: "make -C tests test"
---

<objective>
Clean the repository's living docs so they describe a Switch-only tree with the
milestone's two single-target verification gates, and remove every stale
desktop-build instruction. README.md is the sole edit target; CMakeLists.txt's
header comment is already Switch-only (Plan 06-01) and is only verified.

Purpose: Satisfies DOC-01 and Phase 7 success criterion #1 — no living doc may
instruct or imply a desktop PC build, and the docs must describe the
host-doctest + build-switch.sh flow. This is documentation only; no application
behavior changes.

Output: A rewritten README.md "🛠️ Compilar do código-fonte" section (pt-BR),
with the "Desktop (PC)" section removed, the Switch build pointed at
scripts/build-switch.sh, a deploy note, and the two-gate verification flow
documented. The canonical living-doc grep gate returns empty.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-docs-cleanup-final-verification-gate/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@README.md
@scripts/build-switch.sh
@tests/Makefile
@CMakeLists.txt
</context>

<artifacts_this_phase_produces>
This is a docs-only plan. No new code symbols, files, types, or build targets are
created. Modified docs/sections:
- `README.md` — "🛠️ Compilar do código-fonte" section rewritten:
  - "### Nintendo Switch (.nro)" subsection updated to recommend `scripts/build-switch.sh`
  - "### Desktop (PC) — para iterar na UI sem hardware" subsection removed entirely
  - deploy note added (copy `build_switch/thomaz.nro` to `/switch/thomaz.nro`, or `nxlink`)
  - "### Testes (lógica pura, no host)" subsection expanded into the two-gate verification flow
  - Status blurb (line ~7) updated to drop "e desktop"
- `CMakeLists.txt` — header comment verified Switch-only; expected NO change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Desktop (PC) section and de-desktop the Status blurb in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full — the file being edited; focus on line ~7 Status blurb and the "🛠️ Compilar do código-fonte" section ≈ lines 74-115)
    - .planning/phases/07-docs-cleanup-final-verification-gate/07-CONTEXT.md (D-01, D-03, D-04 — scope and exact section to remove)
    Environment note (do NOT put in README — README stays platform-neutral): on THIS machine the verification gates run via the devkitPro MSYS2 login shell; host doctest needs msys2 gcc + -DDOCTEST_CONFIG_NO_POSIX_SIGNALS; native Switch build runs through /c/devkitPro/msys2/usr/bin/bash.exe -lc 'cd <repo> && bash scripts/build-switch.sh'. The README must describe the generic Docker/CI + native devkitPro flow only.
  </read_first>
  <action>
    Edit README.md (pt-BR; preserve the document's existing voice and markdown style):
    1. Remove the entire "### Desktop (PC) — para iterar na UI sem hardware" subsection
       — the heading, the fenced bash block containing `sudo apt install ... && ./scripts/build-desktop.sh && ./build_desktop/thomaz`, and the trailing sentence "No desktop os jogos são exemplos fictícios (FakeTitleService)...". Per D-04 the desktop target no longer exists.
    2. In the Status blurb (line ~7), change "O app compila e roda (Switch e desktop)" so it no longer claims a desktop target — e.g. "O app compila e roda na Switch". Keep the rest of the warning (hardware-validation caveat) intact.
    Do NOT touch the "### Backend local (feed / auth)" subsection, the "🧱 Estrutura" section, or any link into docs/superpowers/ (those are historical archive references and out of scope per D-02/D-03).
    Do NOT add desktop-build wording anywhere. No fenced code blocks containing build-desktop/run-desktop/build_desktop may remain.
  </action>
  <verify>
    <automated>git grep -nE 'build-desktop|run-desktop|build_desktop|-DUSE_SDL2|-DPLATFORM_DESKTOP|desktop PC' -- 'README.md'; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - README.md no longer contains the heading "### Desktop (PC) — para iterar na UI sem hardware".
    - README.md no longer contains the strings "build-desktop", "build_desktop", or "FakeTitleService".
    - `git grep -nE 'build-desktop|run-desktop|build_desktop|-DUSE_SDL2|-DPLATFORM_DESKTOP|desktop PC' -- 'README.md'` returns no output (exit code 1).
    - The Status blurb no longer claims a desktop run target (no "(Switch e desktop)").
    - The "### Backend local (feed / auth)" subsection and all docs/superpowers/ links are unchanged.
  </acceptance_criteria>
  <done>The desktop section is gone, the Status blurb is Switch-only, and the README-scoped desktop grep returns nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Switch build entrypoint, deploy, and the two-gate verification flow; verify the living-doc grep gate</name>
  <files>README.md</files>
  <read_first>
    - README.md (the "🛠️ Compilar do código-fonte" section, post-Task-1 state)
    - scripts/build-switch.sh (source of truth for build wording: Docker `devkitpro/devkita64` default, native when `DEVKITPRO` is set; output `build_switch/thomaz.nro`; copy to `/switch/thomaz.nro`)
    - tests/Makefile (source of truth for the host doctest gate wording: `make -C tests test`; still compiles the retained `source/platform/saves/fake_cloud_save_client.cpp` double)
    - CMakeLists.txt (header lines 1-4 — verify already Switch-only)
    - .planning/phases/07-docs-cleanup-final-verification-gate/07-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Edit README.md (pt-BR):
    1. In the "### Nintendo Switch (.nro)" subsection, make `scripts/build-switch.sh` the recommended local build path (per D-05): note it builds via Docker (`devkitpro/devkita64`, the CI path) by default and natively when `DEVKITPRO` is set, producing `build_switch/thomaz.nro`. Keep the existing raw `cmake -B build_switch -DPLATFORM_SWITCH=ON -DUSE_DEKO3D=ON` snippet only if useful as an alternative (Claude's discretion per D-05); the script is the headline path. The GitHub Actions note may stay.
    2. Add a brief deploy note (per D-05): copy `build_switch/thomaz.nro` to the SD card at `/switch/thomaz.nro`, or push it to a running hbmenu via `nxlink`. (The "🚀 Instalação" section already covers SD copy for end users; this deploy note is for the from-source flow — keep it short, no duplication of the full install steps.)
    3. Replace the old "### Testes (lógica pura, no host)" content with the milestone's two single-target verification gates (per D-06), keeping it in pt-BR:
       - Gate 1 — host doctest: `make -C tests test` (lógica pura no host; ainda compila o test double `source/platform/saves/fake_cloud_save_client.*`).
       - Gate 2 — Switch build: `scripts/build-switch.sh` produz `build_switch/thomaz.nro`.
       State these two together are the v1.1 verification flow (no desktop smoke run).
    4. Verify CMakeLists.txt header (lines 1-4) already describes a Switch-only tree (Plan 06-01). It should already say "targeting Nintendo Switch (devkitPro) only" with no dual-target/desktop wording — do NOT churn it; only edit if a stale desktop/dual-target phrase is actually present.
    Then run the canonical living-doc grep gate and confirm it is empty.
    Do NOT inline machine-specific MSYS2 quirks into README (it stays platform-neutral).
  </action>
  <verify>
    <automated>git grep -nE 'build-desktop|run-desktop|PLATFORM_DESKTOP|build_desktop|-DUSE_SDL2|-DPLATFORM_DESKTOP|desktop PC' -- '*.md' 'CMakeLists.txt' ':!docs/superpowers/' ':!.planning/'; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "scripts/build-switch.sh".
    - README.md contains "make -C tests test".
    - README.md contains "build_switch/thomaz.nro".
    - README.md mentions deploying to the SD `/switch/thomaz.nro` or `nxlink` in the from-source flow.
    - README.md documents both verification gates (host doctest + Switch build) as the verification flow, with no desktop smoke run.
    - CMakeLists.txt header (lines 1-4) contains no desktop/dual-target wording (e.g. "PLATFORM_DESKTOP", "desktop", "two targets").
    - Canonical living-doc gate `git grep -nE 'build-desktop|run-desktop|PLATFORM_DESKTOP|build_desktop|-DUSE_SDL2|-DPLATFORM_DESKTOP|desktop PC' -- '*.md' 'CMakeLists.txt' ':!docs/superpowers/' ':!.planning/'` returns no output (exit code 1).
    - All rewritten README sections remain in pt-BR.
  </acceptance_criteria>
  <done>README documents the build-switch.sh entrypoint, the deploy step, and the two-gate verification flow in pt-BR; CMakeLists header is confirmed Switch-only; the living-doc desktop grep gate is empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits a markdown doc (README.md) and reads CMakeLists.txt. No new inputs, network calls, parsing of untrusted data, or privilege boundaries are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | README.md content | accept | Docs-only edit, version-controlled in git; any unintended change is visible in diff/review. No runtime impact. |
| T-07-02 | Information Disclosure | README.md (public doc) | accept | README must stay platform-neutral; machine-specific MSYS2 paths are kept out of the README (documented only in read_first notes). No secrets are added. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan. |

No high-severity threats. Realistic surface is minimal: documentation edits with no new inputs, network, or privilege boundaries.
</threat_model>

<verification>
- Living-doc desktop-build grep gate (success criterion #1) is empty:
  `git grep -nE 'build-desktop|run-desktop|PLATFORM_DESKTOP|build_desktop|-DUSE_SDL2|-DPLATFORM_DESKTOP|desktop PC' -- '*.md' 'CMakeLists.txt' ':!docs/superpowers/' ':!.planning/'` returns no output.
  (Note: `.planning/**` and `docs/superpowers/**` are working-history / historical archives that intentionally document the desktop-removal effort — both are excluded from the living-doc gate per D-02/D-03 intent.)
- README.md "🛠️ Compilar do código-fonte" section describes a Switch-only build via `scripts/build-switch.sh`, a deploy step, and the two-gate verification flow (host doctest + Switch build), all in pt-BR.
- CMakeLists.txt header comment confirmed Switch-only (no edit expected).
</verification>

<success_criteria>
- No living doc instructs or implies a desktop PC build (Phase 7 success criterion #1).
- README documents `scripts/build-switch.sh`, deploy to `/switch/thomaz.nro` (or `nxlink`), and the host-doctest + Switch-build verification flow.
- CMakeLists.txt header is Switch-only.
- DOC-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/07-docs-cleanup-final-verification-gate/07-01-SUMMARY.md` when done.
</output>
