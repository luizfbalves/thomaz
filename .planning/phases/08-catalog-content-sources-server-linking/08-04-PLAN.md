---
phase: 08-catalog-content-sources-server-linking
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - resources/i18n/en-US/thomaz.json
  - resources/i18n/pt-BR/thomaz.json
  - source/app/theme.cpp
  - resources/xml/activity/home.xml
  - source/app/home_activity.hpp
  - source/app/home_activity.cpp
  - source/app/source_list_activity.hpp
  - source/platform/games/cover_art.hpp
  - source/platform/games/cover_art.cpp
autonomous: true
requirements: [CAT-01, SRC-04]
must_haves:
  truths:
    - "Every new Phase 8 copy string exists under a NEW catalog/sources i18n namespace in BOTH en-US and pt-BR (never reusing the existing games namespace)"
    - "A new thomaz/tile_games theme color token is registered in theme.cpp with an indigo/blue hue not used by cheats/settings/saves/mods/backup"
    - "A new focusable gamesCard tile in home.xml uses tile_games and pushes SourceListActivity when clicked"
    - "cover_art resolves art in three tiers: titledb iconUrl (fetched once + SD-cached, capped at 4 MiB) -> installed libnx NACP icon -> name+kind placeholder, so the catalog is fully usable with zero art"
  artifacts:
    - path: "resources/i18n/en-US/thomaz.json"
      provides: "English Phase 8 catalog/sources copy"
      contains: "catalog"
    - path: "resources/i18n/pt-BR/thomaz.json"
      provides: "Portuguese Phase 8 catalog/sources copy"
      contains: "catalog"
    - path: "source/app/theme.cpp"
      provides: "tile_games color token"
      contains: "tile_games"
    - path: "resources/xml/activity/home.xml"
      provides: "Games Home card"
      contains: "gamesCard"
    - path: "source/platform/games/cover_art.cpp"
      provides: "3-tier cover-art resolution"
      contains: "resolve_cover"
  key_links:
    - from: "source/app/home_activity.cpp"
      to: "SourceListActivity"
      via: "gamesCard click -> pushActivity"
      pattern: "gamesCard"
    - from: "resources/xml/activity/home.xml"
      to: "thomaz/tile_games"
      via: "gamesCard backgroundColor"
      pattern: "tile_games"
    - from: "source/platform/games/cover_art.cpp"
      to: "title_service_switch / NsApplicationControlData"
      via: "libnx icon fallback (tier 2)"
      pattern: "nsGetApplicationControlData|InstalledTitle"
---

<objective>
Add the shared UI scaffolding the catalog screens depend on — all new i18n copy (both locales,
new `catalog`/`sources` namespace), a `thomaz/tile_games` theme token, the Home "Games" entry
card wired to push `SourceListActivity`, and the 3-tier cover-art platform service (titledb ->
libnx icon -> placeholder). Owning these shared resource files in one plan lets the two catalog
Activities (Plans 05 and 06) be built in parallel without file contention.

Purpose: CAT-01 needs cover art for every entry (the headline "cover-art-rich" value) via the
3-tier fallback so the grid is never a wall of blank squares; the Home card is the entry point
(SRC-04's empty source list lives one push away); the i18n namespace is shared by both catalog
and source-list screens. A forward-declared `SourceListActivity` header lets `home_activity.cpp`
wire the push before Plan 06 implements the activity body.

Output: Updated i18n (both locales), `theme.cpp` token, `home.xml` + `home_activity.{hpp,cpp}`
wiring, a `source_list_activity.hpp` declaration, and `cover_art.{hpp,cpp}`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-catalog-content-sources-server-linking/08-CONTEXT.md
@.planning/phases/08-catalog-content-sources-server-linking/08-RESEARCH.md
@.planning/phases/08-catalog-content-sources-server-linking/08-PATTERNS.md
@.planning/phases/08-catalog-content-sources-server-linking/08-UI-SPEC.md
@.planning/phases/08-catalog-content-sources-server-linking/08-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (downstream plans consume them; do not flag as drift):

- i18n namespace `catalog` and `sources` keys in BOTH `resources/i18n/en-US/thomaz.json` and
  `resources/i18n/pt-BR/thomaz.json` — the full copy table from 08-UI-SPEC.md lines 137-161
  (home card label, Add Source, Sync to Account, Refresh, empty heading/body, local-source label,
  catalog empty, no-matches, offline note, truncation note, detail kind rows, all error strings,
  sync success/expired, remove-source confirm).
- Theme token `thomaz/tile_games` in `source/app/theme.cpp` (indigo/blue, e.g. nvgRGB(0x49,0x5C,0xD6)).
- `home.xml` `gamesCard` AnimatedBox (focusable, backgroundColor `@theme/thomaz/tile_games`).
- `home_activity.cpp` wiring: `gamesCard` click -> `pushActivity(new SourceListActivity(...))`.
- `source_list_activity.hpp`: a declaration of `class SourceListActivity` with the ctor
  signature `SourceListActivity(ITitleService* titleService, IHttpClient* http)` (body in Plan 06).
- `cover_art.hpp`: `enum class ArtTier { Titledb, LibnxIcon, Placeholder };`
  `struct CoverArt { std::vector<std::uint8_t> bytes; ArtTier tier; bool ok; };`
  `CoverArt resolve_cover(IHttpClient* http, ITitleService* titles, std::uint64_t titleId, thomaz::core::TitleKind kind, std::shared_ptr<std::atomic<bool>> cancelled);`
  plus a titledb-index fetch+cache helper (`std::optional<std::string> titledb_icon_url(std::uint64_t titleId);`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: i18n catalog/sources namespace (both locales) + tile_games token + Home card</name>
  <files>resources/i18n/en-US/thomaz.json, resources/i18n/pt-BR/thomaz.json, source/app/theme.cpp, resources/xml/activity/home.xml, source/app/home_activity.hpp, source/app/home_activity.cpp, source/app/source_list_activity.hpp</files>
  <read_first>
    - .planning/phases/08-catalog-content-sources-server-linking/08-UI-SPEC.md (Copywriting Contract lines 130-171 exact en/pt strings; Color lines 107-111 tile_games; Interaction lines 181-182 entry)
    - resources/i18n/en-US/thomaz.json (target — existing namespace structure; do NOT collide with the games namespace)
    - resources/i18n/pt-BR/thomaz.json (target — must stay key-for-key parallel with en-US)
    - source/app/theme.cpp (target + analog: tile_* registrations lines 48-52 — pick an indigo/blue hue unused by the five existing tiles)
    - resources/xml/activity/home.xml (target + analog: cheatsCard AnimatedBox lines 97-105)
    - source/app/home_activity.cpp (target + analog: cheatsCard click->pushActivity lines 34-43)
    - source/app/home_activity.hpp (target — add the source_list include; ctor unchanged)
  </read_first>
  <action>
    Add the full Phase 8 copy table from 08-UI-SPEC.md (lines 137-161) to BOTH
    resources/i18n/en-US/thomaz.json and resources/i18n/pt-BR/thomaz.json under a NEW top-level
    namespace — use `catalog` and `sources` (UI-SPEC: do NOT reuse the existing `games` namespace,
    which is the installed-titles "My Games" feature). Keep the two locale files key-for-key
    identical in structure. Copy the en-US and pt-BR strings verbatim from the Copywriting
    Contract, including the home card label (Games / Jogos), CTAs (Add Source / Adicionar fonte,
    Sync to Account / Sincronizar com a conta, Refresh / Atualizar), empty heading/body
    (SRC-04 "Nothing is enabled by default."), local-source label, catalog empty, no-matches,
    offline + truncation notes, detail kind rows (Base / Update v{n} / DLC), all five error
    strings, sync success/expired, and the remove-source confirm string.

    Register `thomaz/tile_games` in theme.cpp alongside the existing tile_* colors, using an
    indigo/blue hue NOT used by cheats(violet)/settings(teal)/saves(amber)/mods(rose)/backup(emerald)
    — e.g. nvgRGB(0x49, 0x5C, 0xD6).

    Add a `gamesCard` AnimatedBox to home.xml modeled on cheatsCard (focusable,
    highlightCornerRadius=16, hideHighlightBackground, an entranceDelay continuing the cascade),
    with backgroundColor `@theme/thomaz/tile_games` and a brls:Label bound to the chosen home-card
    i18n key at fontSize 17, textColor #FFFFFF. Place it in a sensible column slot (e.g. reveal the
    hidden systemCard slot, or add to column B) without breaking the existing tile layout.

    Create source/app/source_list_activity.hpp declaring `class SourceListActivity` deriving the
    same base the other activities use (ThomazActivity/brls::Activity — match theme_browser),
    with ctor `SourceListActivity(ITitleService* titleService, IHttpClient* http)`,
    `CONTENT_FROM_XML_RES("activity/source_list.xml")`, and `onContentAvailable()` (the .cpp body
    is Plan 06). In home_activity.hpp add `#include "app/source_list_activity.hpp"`. In
    home_activity.cpp wire `gamesCard` -> `pushActivity(new SourceListActivity(this->titleService,
    this->http), brls::TransitionAnimation::NONE)` plus a TapGestureRecognizer, mirroring the
    cheatsCard block. Plan 06 supplies the .cpp so the full Switch build links; this plan delivers
    the declaration + wiring so home compiles against the header.
  </action>
  <verify>
    <automated>node $HOME/.claude/gsd-core/bin/i18n-parity.cjs resources/i18n/en-US/thomaz.json resources/i18n/pt-BR/thomaz.json catalog sources 2>/dev/null || python3 -c "import json;en=json.load(open('resources/i18n/en-US/thomaz.json'));pt=json.load(open('resources/i18n/pt-BR/thomaz.json'));ek=sorted(list(en.get('catalog',{}))+list(en.get('sources',{})));pk=sorted(list(pt.get('catalog',{}))+list(pt.get('sources',{})));assert ek==pk and len(ek)>0, (ek,pk);print('i18n parity OK',len(ek))"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: en-US and pt-BR have identical, non-empty catalog/sources key sets.
    - `grep -n "tile_games" source/app/theme.cpp` shows the registered token with an indigo/blue nvgRGB value distinct from the five existing tile hues.
    - `grep -n "gamesCard" resources/xml/activity/home.xml` shows a focusable AnimatedBox using `@theme/thomaz/tile_games`.
    - `grep -n "SourceListActivity" source/app/home_activity.cpp` shows the gamesCard click pushing it.
    - source/app/source_list_activity.hpp declares the ctor `SourceListActivity(ITitleService*, IHttpClient*)` and CONTENT_FROM_XML_RES("activity/source_list.xml").
    - No new key is added under the existing `games` namespace (verify by reading — the My Games namespace is untouched).
  </acceptance_criteria>
  <done>All Phase 8 copy exists in both locales under catalog/sources, the tile_games token is registered, the Home Games card is wired to SourceListActivity, and the source-list header is declared for Plan 06.</done>
</task>

<task type="auto">
  <name>Task 2: cover_art 3-tier resolution (titledb -> libnx icon -> placeholder)</name>
  <files>source/platform/games/cover_art.hpp, source/platform/games/cover_art.cpp</files>
  <read_first>
    - source/app/theme_browser_activity.cpp (analog: loadThumb lines 124-163 — client->request, r.ok(), to_decodable_image(r.body), setImageFromMem)
    - source/platform/title_service_switch.cpp (analog: libnx icon extraction lines 48-90 — nsGetApplicationControlData, t.icon.assign(control->icon, ...))
    - source/platform/title.hpp (InstalledTitle.icon — tier-2 source)
    - source/platform/image_transcode.hpp (to_decodable_image for non-PNG art)
    - source/core/games/title_id.hpp (Plan 01 — TitleKind for the placeholder label; titleId for the titledb lookup)
    - source/platform/games/catalog_cache.cpp (Plan 02 — the SD cache helper pattern to reuse for art cache + the titledb regional file cache)
    - .planning/phases/08-catalog-content-sources-server-linking/08-RESEARCH.md (D-02 lines 24; Pitfall 5 art-optional lines 354-358; A1 titledb iconUrl lines 440; titledb regional file note lines 426-434)
    - .planning/phases/08-catalog-content-sources-server-linking/08-PATTERNS.md (cover_art three-tier section lines 126-131)
  </read_first>
  <action>
    Create platform/games/cover_art.{hpp,cpp} exposing `resolve_cover(http, titles, titleId, kind,
    cancelled)` returning a `CoverArt{ bytes, tier, ok }`. Tier 1 (titledb): fetch the regional
    titledb JSON ONCE per session and cache it on SD (it is large — do NOT fetch per card,
    Pitfall 5); look up the title's `iconUrl`; fetch the icon over the existing IHttpClient with
    fail-closed TLS, cap the art response at 4 MiB (Pitfall 2), and transcode via
    `to_decodable_image` for non-PNG. Cache a successful icon on SD (reuse the Plan 02 cache helper
    pattern). Tier 2 (libnx icon): when titledb misses/unreachable AND the title is installed, pull
    the JPEG icon from `NsApplicationControlData` via the title service (mirror
    title_service_switch lines 84-87). Tier 3 (placeholder): return ok=true with empty bytes and
    `tier=Placeholder` so the caller renders a name+kind card — the catalog MUST be fully usable
    with zero art (never a grid of blank squares). Keep the titledb/eShop endpoints metadata-only
    (does not change the "thomaz hosts no content" posture). The exact titledb endpoint
    (raw.githubusercontent.com/blawar/titledb regional file) is configurable in one place — confirm
    reachability + fail-closed TLS at execution time; if unreachable, the 3-tier fallback keeps the
    catalog working (record reachability in the SUMMARY).
  </action>
  <verify>
    <automated>cd tests &amp;&amp; make test</automated>
  </verify>
  <acceptance_criteria>
    - `cd tests && make test` exits 0 (the host suite still compiles; cover_art's pure helpers, if added to SRCS, build clean).
    - `grep -n "resolve_cover" source/platform/games/cover_art.hpp` shows the 3-tier entry point with the ArtTier enum.
    - `grep -n "ArtTier::Placeholder" source/platform/games/cover_art.cpp` confirms tier-3 returns ok with a placeholder (catalog usable with zero art).
    - `grep -n "to_decodable_image" source/platform/games/cover_art.cpp` confirms non-PNG transcode on the titledb path.
    - The titledb regional file is fetched/cached once (verify by reading — no per-card titledb fetch), and art is capped at 4 MiB.
    - A single configurable constant/function holds the titledb endpoint URL (verify by reading).
  </acceptance_criteria>
  <done>cover_art resolves cover art across titledb -> libnx icon -> placeholder with the regional titledb cached once, art capped at 4 MiB, and a guaranteed usable catalog when art is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device -> titledb / eShop CDN | metadata-only art fetch over public HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering / Spoofing | MITM serving forged art over downgraded TLS | mitigate | art fetch uses the existing fail-closed `apply_curl_tls` path; art is metadata-only and never executed (Security Domain V7). |
| T-08-14 | DoS | oversized titledb regional file / art exhausting console memory | mitigate | art capped at 4 MiB (Pitfall 2); titledb regional file fetched/cached once, not per card (Pitfall 5). |
| T-08-15 | Availability | titledb/eShop unreachable -> broken-looking catalog | mitigate | 3-tier fallback (libnx icon -> name+kind placeholder) keeps the catalog fully usable with zero art (Pitfall 5). |
</threat_model>

<verification>
- i18n parity: en-US and pt-BR have identical, non-empty catalog/sources key sets.
- `cd tests && make test` is green.
- The Home Games card pushes SourceListActivity; tile_games is a distinct hue.
- cover_art guarantees a usable catalog with zero art and caps art at 4 MiB.
- On-hardware (deferred to phase gate via nxlink): the Games card appears with its indigo tile and
  pushes the (Plan 06) source list; cover art renders for installed titles and a placeholder shows
  when titledb is unreachable.
</verification>

<success_criteria>
- Shared i18n (both locales), tile token, Home card wiring, and the 3-tier cover-art service exist.
- CAT-01 (cover art for every entry, art-optional) and the SRC-04 entry point are satisfied at the shared-resource layer.
- Plans 05 and 06 can build their Activities in parallel against this shared scaffolding with no file contention.
</success_criteria>

<output>
Create `.planning/phases/08-catalog-content-sources-server-linking/08-04-SUMMARY.md` when done.
</output>
