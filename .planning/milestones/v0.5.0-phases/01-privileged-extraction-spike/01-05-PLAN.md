---
phase: 01-privileged-extraction-spike
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - docs/title-takeover.md
  - THIRD_PARTY.md
autonomous: false
requirements:
  - TAKEOVER-02
must_haves:
  truths:
    - "A user-facing doc explains the hbloader title-override (hold-R) launch path to run thomaz in Application mode for extraction"
    - "The Atmosphère SPL key-source release/commit AND the firmware version the spike actually ran against are recorded in both the doc and THIRD_PARTY.md"
  artifacts:
    - path: "docs/title-takeover.md"
      provides: "User-facing title-takeover launch instructions + recorded provenance"
      contains: "hold"
    - path: "THIRD_PARTY.md"
      provides: "SPL key-source + spike firmware-version provenance block"
      contains: "Atmos"
  key_links:
    - from: "docs/title-takeover.md"
      to: "the on-hardware spike run (plan 04)"
      via: "records the firmware major.minor.micro the spike validated"
      pattern: "firmware|[0-9]+\\.[0-9]+\\.[0-9]+"
---

<objective>
Write the user-facing title-takeover launch doc (TAKEOVER-02) and record the SPL key-source + firmware
provenance (D-07, Success Criterion #4) from the actual on-hardware spike run. This is the documentation
deliverable that turns the validated mechanism into something a user can reproduce and that records
exactly which Atmosphère key sources + which firmware the spike proved out.

Purpose: TAKEOVER-02 — the required title-takeover launch path for privileged FS/SPL access must be
documented for users (hold-R title override → Application mode). Success Criterion #4 — SPL key-source
provenance recorded against the firmware the spike ran on. This plan depends on plan 04's hardware run
because the firmware version is only known AFTER the spike actually runs (`setsysGetFirmwareVersion`).

Output: `docs/title-takeover.md` (launch instructions + provenance) and an extended `THIRD_PARTY.md`
(SPL key-source release/commit + spike firmware version). This plan adds NO code and does NOT build the
Phase-4 forwarder (deferred / TAKEOVER-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-privileged-extraction-spike/01-CONTEXT.md
@.planning/phases/01-privileged-extraction-spike/01-RESEARCH.md
@.planning/phases/01-privileged-extraction-spike/01-PATTERNS.md
@THIRD_PARTY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the title-takeover user doc and record SPL key-source + firmware provenance</name>
  <files>docs/title-takeover.md, THIRD_PARTY.md</files>
  <read_first>
    - .planning/phases/01-privileged-extraction-spike/01-RESEARCH.md (Title-Takeover Doc Skeleton in Code Examples — the verbatim hold-R steps; Pitfall/Assumption A4 on override_config.ini being documentation-only; the firmware-version recording snippet `setsysGetFirmwareVersion`)
    - .planning/phases/01-privileged-extraction-spike/01-PATTERNS.md (Title-takeover doc — use the RESEARCH skeleton verbatim as the base; must record Atmosphère key-source version/commit + validated firmware version, same provenance written into THIRD_PARTY.md)
    - THIRD_PARTY.md (existing `## <name>` attribution block shape to mirror for the SPL key-source + firmware block)
    - The plan-04 SUMMARY and/or plan-02 key_loader source (the pinned Atmosphère release/commit recorded in code) and the firmware version surfaced by the spike run
  </read_first>
  <action>
    Create `docs/title-takeover.md` from the RESEARCH "Title-Takeover Doc Skeleton" (D-04 hbloader title
    override): the numbered hold-R steps (from Home menu, HOLD R, launch an installed GAME — not the
    Album/Gallery — keep holding through the Nintendo logo → hbloader opens in Application mode → launch
    thomaz → run extraction), plus the note that Album-launched / applet mode lacks FS/SPL permissions and
    shows the relaunch prompt (ties to TAKEOVER-01). Add a Provenance section recording: the Atmosphère
    release version + commit the PUBLIC SPL key sources came from (from the plan-02 key_loader constants /
    plan-01 vendoring; D-07), and the firmware `major.minor.micro` the spike actually ran against (from
    the plan-04 spike run via `setsysGetFirmwareVersion`). If A4 turns out true (the target Atmosphère
    config blocked default title override), add a documentation-only note on the `override_config.ini`
    adjustment. Then extend `THIRD_PARTY.md` with a `## SPL key sources` block mirroring the existing
    attribution shape: the Atmosphère release+commit AND the validated firmware version — satisfying
    Success Criterion #4. Keep the doc under `docs/` (CI ignores `docs/**`, so this plan does not affect
    the build).
  </action>
  <verify>
    <automated>test -f docs/title-takeover.md && grep -qi 'hold' docs/title-takeover.md && grep -Eqi 'applet|album' docs/title-takeover.md && grep -Eqi 'firmware|[0-9]+\.[0-9]+\.[0-9]+' docs/title-takeover.md && grep -qi 'atmos' docs/title-takeover.md && grep -qi 'atmos' THIRD_PARTY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/title-takeover.md` documents the hold-R title-override launch path to Application mode (TAKEOVER-02) and notes that applet/Album mode shows the relaunch prompt.
    - The doc records BOTH the Atmosphère SPL key-source release/commit AND the firmware `major.minor.micro` the spike validated (D-07, Success Criterion #4).
    - `THIRD_PARTY.md` has a `## SPL key sources` block with the same Atmosphère release/commit + firmware version.
    - The provenance values are real (taken from the plan-04 hardware run + plan-02 pinned constants), not placeholders.
  </acceptance_criteria>
  <done>The title-takeover launch path is documented for users and the SPL key-source + spike firmware provenance is recorded in both the doc and `THIRD_PARTY.md`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the launch doc steps and recorded provenance match the real hardware run</name>
  <files>docs/title-takeover.md, THIRD_PARTY.md</files>
  <action>
    PAUSE for the developer to confirm the documented hold-R steps are reproducible on their console and
    that the recorded firmware version + Atmosphère key-source release/commit match the actual plan-04
    spike run. This is verification of already-written docs, not new work — no automation precedes it
    because the values can only be confirmed by the human who ran the spike.
  </action>
  <what-built>The title-takeover doc (`docs/title-takeover.md`) and the `## SPL key sources` provenance block in `THIRD_PARTY.md`, both populated with the Atmosphère key-source release/commit and the firmware version the spike ran against.</what-built>
  <how-to-verify>
    1. Open `docs/title-takeover.md`. Confirm the hold-R steps are correct and reproducible for your console, and that following them launches thomaz in Application mode (you already validated this in the plan-04 spike run).
    2. Confirm the recorded firmware `major.minor.micro` matches the firmware you actually ran the spike on.
    3. Confirm the recorded Atmosphère release/commit matches the key sources compiled into the build.
    4. Open `THIRD_PARTY.md` and confirm the `## SPL key sources` block carries the same provenance.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the doc steps are reproducible and the recorded firmware + Atmosphère provenance match the actual spike run.</human-check>
  </verify>
  <done>Developer has approved the doc and confirmed the recorded provenance matches the hardware run.</done>
  <resume-signal>Type "approved" or describe what to correct (e.g. wrong firmware version, missing override_config note).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented launch path → user action | Incorrect instructions could leave users in applet mode (extraction silently unavailable) — accuracy is the control. |
| recorded provenance → reproducibility/attribution | Wrong/missing key-source or firmware provenance breaks GPLv2 attribution and the ability to diagnose a future key-derivation failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | Repudiation | SPL key-source / firmware provenance | mitigate | Record the exact Atmosphère release+commit and the validated firmware version in both the doc and `THIRD_PARTY.md` (D-07, Success Criterion #4); human-verify the values match the actual run. |
| T-01-20 | Information Disclosure | Doc contents | accept | Only PUBLIC info (launch steps, public key-source release, firmware version) is documented; no secret, no derived key, no `prod.keys`. Low risk. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Docs-only plan — no packages installed, no code changed. slopcheck N/A. |
</threat_model>

<verification>
- TAKEOVER-02: `docs/title-takeover.md` documents the hold-R title-override path to Application mode and the applet-mode caveat.
- Provenance: both `docs/title-takeover.md` and `THIRD_PARTY.md` record the Atmosphère key-source release/commit AND the spike firmware version (Success Criterion #4 / D-07).
- Human-verify: the recorded firmware/key-source values match the actual plan-04 hardware run.
</verification>

<success_criteria>
- TAKEOVER-02: the required title-takeover launch path is documented for users.
- Success Criterion #4: SPL key-source provenance is recorded against the firmware the spike ran on, in both the user doc and `THIRD_PARTY.md`.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

- `docs/title-takeover.md` — user-facing hold-R title-override launch instructions (TAKEOVER-02) + applet-mode caveat + Provenance section (Atmosphère key-source release/commit + validated firmware version).
- `THIRD_PARTY.md` — `## SPL key sources` provenance block (Atmosphère release/commit + spike firmware version) satisfying Success Criterion #4 / D-07.
</artifacts_produced>

<output>
Create `.planning/phases/01-privileged-extraction-spike/01-05-SUMMARY.md` when done.
</output>
