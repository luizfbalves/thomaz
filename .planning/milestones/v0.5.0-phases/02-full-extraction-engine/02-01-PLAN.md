---
phase: 02-full-extraction-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/platform/themes/cfw_paths.cpp
  - source/platform/themes/firmware_extract.hpp
  - source/platform/themes/firmware_extract_fake.cpp
  - tests/test_cfw_paths.cpp
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02, EXTRACT-03]
must_haves:
  truths:
    - "target_map(\"common\") resolves to title 0100000000001000 and szs common.szs"
    - "All eight qlaunch parts (ResidentMenu, Entrance, Flaunch, Set, Notification, common) plus Psl and MyPage resolve through target_map() to the correct title-ID and szs"
    - "base_szs_path(\"common\") is the flat path base_layout_dir()/common.szs (no nesting)"
    - "The neutral firmware_extract.hpp declares ExtractAllResult and extract_all_base_layouts() using only bool/std::string/std::vector<std::string> (no libnx, no SarcLib)"
    - "The desktop build links because firmware_extract_fake.cpp provides a no-op extract_all_base_layouts() with zero Switch symbols"
    - "Host doctest test_cfw_paths.cpp passes including the new common assertions"
  artifacts:
    - path: "source/platform/themes/cfw_paths.cpp"
      provides: "target_map() with the common arm added (8 entries)"
      contains: "if (target == \"common\")"
    - path: "source/platform/themes/firmware_extract.hpp"
      provides: "ExtractAllResult struct + extract_all_base_layouts() declaration"
      contains: "struct ExtractAllResult"
    - path: "source/platform/themes/firmware_extract_fake.cpp"
      provides: "desktop no-op extract_all_base_layouts()"
      contains: "extract_all_base_layouts"
    - path: "tests/test_cfw_paths.cpp"
      provides: "host coverage of common mapping + flat base path"
      contains: "common"
  key_links:
    - from: "tests/test_cfw_paths.cpp"
      to: "source/platform/themes/cfw_paths.cpp"
      via: "doctest CHECK on target_map/base_szs_path"
      pattern: "target_map\\(\"common\"\\)"
    - from: "source/platform/themes/cfw_paths.cpp"
      to: "base_present_for (apply path)"
      via: "target_map() shared contract — additive common arm"
      pattern: "target_map\\("
---

<objective>
Establish the pure-logic contracts for Phase 2's multi-title extraction engine and
reconcile the title-ID/szs mapping table.

Two threads, both host-testable and libnx-free:
1. Add the missing `common` arm to `cfw_paths::target_map()` (D-01a) so the apply
   path (`base_present_for`) can resolve `common.szs`, and extend the existing
   host doctest to lock the mapping/path logic (success criterion 4).
2. Declare the new `ExtractAllResult` struct and `extract_all_base_layouts()`
   entry point in the neutral `firmware_extract.hpp`, and add the matching desktop
   no-op in `firmware_extract_fake.cpp`. This defines the interface contract that
   Plan 03's Switch driver implements — interface-first ordering so the executor
   never has to reverse-engineer the result shape.

Purpose: ship the apply-path table reconciliation and the engine's public
contract before the privileged implementation is written. EXTRACT-01/02/03 mapping
is the host-testable slice of those requirements (success criterion 4).
Output: reconciled `target_map()`, new `ExtractAllResult`/`extract_all_base_layouts()`
declarations + desktop no-op, extended `test_cfw_paths.cpp`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-extraction-engine/02-CONTEXT.md
@.planning/phases/02-full-extraction-engine/02-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols this plan creates)

- `cfw_paths.cpp` `target_map()` — new `common` arm returning `TargetMap{"0100000000001000", "common.szs"}` (the symbol `target_map` already exists; this plan adds the `common` case).
- `firmware_extract.hpp` — new `struct ExtractAllResult { bool ok; std::string systemic_error; std::vector<std::string> failed_parts; std::vector<std::string> written_parts; };`
- `firmware_extract.hpp` — new free function declaration `ExtractAllResult extract_all_base_layouts();`
- `firmware_extract_fake.cpp` — new desktop no-op definition of `extract_all_base_layouts()`.
- `tests/test_cfw_paths.cpp` — new doctest `TEST_CASE` assertions covering `common` and the flat `base_szs_path`.

These symbols are created in this phase. Downstream drift verification must treat
`ExtractAllResult` and `extract_all_base_layouts` as newly-introduced (not pre-existing).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the common arm to target_map() and lock it with host doctest</name>
  <files>source/platform/themes/cfw_paths.cpp, tests/test_cfw_paths.cpp</files>
  <read_first>
    - source/platform/themes/cfw_paths.cpp (current target_map has 7 arms; common is missing — D-01a)
    - source/platform/themes/cfw_paths.hpp (TargetMap struct, base_szs_path/base_layout_dir signatures)
    - tests/test_cfw_paths.cpp (existing doctest to EXTEND, not replace — already wired into tests/Makefile SRCS via cfw_paths.cpp)
    - .planning/phases/02-full-extraction-engine/02-PATTERNS.md (cfw_paths section + Common.cpp reconciliation facts: QlaunchCommon = {QlaunchID 0x0100000000001000, "/lyt/common.szs"})
    - lib/switchthemes/Common.cpp (authoritative ThemeTargetInfo source of truth for the reconciliation — QlaunchCommon field)
  </read_first>
  <action>
    In cfw_paths.cpp, add exactly one arm to target_map() immediately before the
    final `return std::nullopt;`: when target equals the literal string `common`,
    return `TargetMap{"0100000000001000", "common.szs"}`. The title-ID is qlaunch
    (0100000000001000) and the szs filename is `common.szs`, matching
    ThemeTargetInfo::QlaunchCommon in lib/switchthemes/Common.cpp. This is purely
    additive (D-01a): the other 7 arms and base_szs_path/output_szs_path/
    base_present_for are unchanged and derive from target_map() automatically.

    In tests/test_cfw_paths.cpp, ADD new doctest assertions (do not delete the
    existing TEST_CASEs). Cover: target_map("common") resolves to title_id
    0100000000001000 and szs common.szs; base_szs_path("common") equals
    base_layout_dir() + "/common.szs" (flat, success criterion 3); and that the
    Psl/MyPage title-IDs (0100000000001007 / 0100000000001013) still resolve.
    Use the existing `using namespace thomaz;` and the doctest CHECK macros already
    present in the file. Do NOT introduce libnx or SarcLib includes — this TU is
    pure-logic and host-compiled (-std=c++17).

    Per D-01a, this reconciles target_map() with the apply path so base_present_for
    can resolve common; do not also widen the extraction filter here (that is D-01,
    Plan 03's /lyt/ prefix — the filter no longer enumerates target_map()).
  </action>
  <verify>
    <automated>cd tests && make run && ./run --test-case="*common*" && ./run --test-case="*target_map*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'target == "common"' source/platform/themes/cfw_paths.cpp` returns exactly one line returning TargetMap{"0100000000001000", "common.szs"}.
    - Running the host doctest binary (`cd tests && make run && ./run`) exits 0 with all cfw_paths cases passing.
    - The doctest output contains a passing assertion for target_map("common")->title_id == "0100000000001000" and ->szs == "common.szs".
    - The doctest output contains a passing assertion for base_szs_path("common") == base_layout_dir() + "/common.szs".
    - `grep -c 'if (target ==' source/platform/themes/cfw_paths.cpp` returns 8 (seven original arms + common).
  </acceptance_criteria>
  <done>target_map() has 8 arms including common; test_cfw_paths.cpp asserts the common mapping and flat path; the host doctest binary passes.</done>
</task>

<task type="auto">
  <name>Task 2: Declare ExtractAllResult + extract_all_base_layouts() and add the desktop no-op</name>
  <files>source/platform/themes/firmware_extract.hpp, source/platform/themes/firmware_extract_fake.cpp</files>
  <read_first>
    - source/platform/themes/firmware_extract.hpp (current neutral header — only ExtractResult + extract_base_layout; pragma once, &lt;string&gt; only, no libnx)
    - source/platform/themes/firmware_extract_fake.cpp (current desktop no-op for extract_base_layout — the pattern to mirror)
    - .planning/phases/02-full-extraction-engine/02-PATTERNS.md (firmware_extract.hpp + firmware_extract_fake.cpp sections; ExtractAllResult member shape)
    - .planning/phases/02-full-extraction-engine/02-RESEARCH.md ("Code Examples" — ExtractAllResult + driver shape; "Desktop no-op")
    - source/app/theme_detail_activity.cpp lines 407-444 (existing caller of extract_base_layout — proves extract_base_layout MUST be kept; the new entry point is additive)
  </read_first>
  <action>
    In firmware_extract.hpp: KEEP the existing ExtractResult struct and
    extract_base_layout() declaration unchanged — they have a live caller in
    theme_detail_activity.cpp:430 (do NOT remove; Open Question 1 resolves to keep).
    Add `#include <vector>` next to the existing `#include <string>`. Declare a new
    struct `ExtractAllResult` with four members in this exact order and types:
    `bool ok;` (false only on systemic abort — D-02a), `std::string systemic_error;`,
    `std::vector<std::string> failed_parts;` (per-part warnings — D-02), and
    `std::vector<std::string> written_parts;` (canonical paths written — D-03).
    Use ONLY bool/std::string/std::vector<std::string> — no libnx, no SarcLib, no
    cstdint types — so the header stays neutral (Pitfall 5 / D-08). Declare the free
    function `ExtractAllResult extract_all_base_layouts();` inside namespace thomaz.
    Add a doc comment describing the systemic-vs-per-part contract (D-02/D-02a) and
    that ok=false implies written_parts is empty.

    In firmware_extract_fake.cpp: KEEP the existing extract_base_layout no-op. Add a
    second no-op `ExtractAllResult extract_all_base_layouts()` that returns
    `{false, "Firmware extraction is only available on Switch.", {}, {}}` —
    matching the existing fake's message style and carrying zero Switch symbols
    (the whole file stays under `#ifndef __SWITCH__`).
  </action>
  <verify>
    <automated>cd tests && make run 2>&1 | tail -5; grep -q "struct ExtractAllResult" source/platform/themes/firmware_extract.hpp && grep -q "extract_all_base_layouts" source/platform/themes/firmware_extract_fake.cpp && echo CONTRACT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'struct ExtractAllResult' source/platform/themes/firmware_extract.hpp` returns one line; the struct body contains `bool ok`, `std::string systemic_error`, `std::vector<std::string> failed_parts`, `std::vector<std::string> written_parts` in that order.
    - `grep -n 'ExtractAllResult extract_all_base_layouts();' source/platform/themes/firmware_extract.hpp` returns one line inside namespace thomaz.
    - firmware_extract.hpp contains no `#include <switch.h>`, no `SarcLib`, no libnx type names — only `<string>` and `<vector>` standard includes (verify: `grep -E 'include' source/platform/themes/firmware_extract.hpp` shows only string and vector).
    - `grep -n 'extract_all_base_layouts' source/platform/themes/firmware_extract_fake.cpp` returns the no-op definition returning `{false, ..., {}, {}}`.
    - The existing `extract_base_layout` declaration and fake definition are still present (grep both files).
  </acceptance_criteria>
  <done>The neutral header declares ExtractAllResult + extract_all_base_layouts() with libnx-free members; the desktop fake provides a matching no-op; extract_base_layout is preserved for its live caller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware_extract.hpp neutral header → desktop TU | The interface header must carry no Switch/SarcLib symbols; a leak breaks the desktop build (D-08 invariant). |
| target_map() table → apply path (base_present_for) | A shared pure-logic contract consumed by the theme-apply path; an incorrect mapping silently breaks "Aplicar Tema". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | target_map() common arm | mitigate | Host doctest asserts the exact title-ID/szs for common; wrong mapping fails the test before merge (Task 1 acceptance). |
| T-02-02 | Information Disclosure | ExtractAllResult members | accept | Members are bool + path strings only; no key material, no PII. Result carries only SD paths the user already owns. |
| T-02-03 | Denial of Service | desktop build break via header leak | mitigate | Neutral-header invariant enforced: header limited to `<string>`/`<vector>`; acceptance grep blocks libnx/SarcLib includes. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; RESEARCH.md Package Legitimacy Audit = "Not applicable, zero external packages." |
</threat_model>

<verification>
- Host doctest passes: `cd tests && make clean && make run && ./run`
- `target_map()` resolves all eight qlaunch parts (incl. common) + Psl + MyPage.
- Neutral header carries only `<string>` and `<vector>`; no libnx/SarcLib leak.
- The desktop no-op for extract_all_base_layouts() exists and the existing
  extract_base_layout caller path is untouched.
</verification>

<success_criteria>
- target_map() reconciled with common (D-01a) and asserted by host doctest (success criterion 4 mapping coverage for EXTRACT-01/02/03).
- ExtractAllResult + extract_all_base_layouts() declared in the neutral header with libnx-free members (D-02/D-02a/D-03 result contract).
- Desktop build contract preserved: fake no-op present, no Switch symbols in header/fake.
</success_criteria>

<output>
Create `.planning/phases/02-full-extraction-engine/02-01-SUMMARY.md` when done.
</output>
