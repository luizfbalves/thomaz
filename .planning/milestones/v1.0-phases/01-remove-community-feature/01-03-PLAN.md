---
phase: 01-remove-community-feature
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - api/prisma/schema.prisma
  # generated (new migration file):
  - api/prisma/migrations/
autonomous: true
requirements:
  - RM-02
  - RM-04

must_haves:
  truths:
    - "api/prisma/schema.prisma contains no Post, Like, or Comment model definitions"
    - "User, RefreshToken, and SaveSlot models remain intact in schema.prisma"
    - "User model has no posts, likes, or comments relation fields"
    - "A new migration file exists under api/prisma/migrations/ that drops the Post, Like, and Comment tables"
    - "npx prisma migrate dev exits 0 locally (migration applied)"
    - "cd api && npx vitest run exits 0 — auth and saves tests pass, no dead references"
    - "Desktop build -DUSE_SDL2=ON still exits 0 after API-side changes"
  artifacts:
    - path: "api/prisma/schema.prisma"
      provides: "Schema with only User, RefreshToken, SaveSlot models; no Post/Like/Comment"
      contains: "model User\|model RefreshToken\|model SaveSlot"
    - path: "api/prisma/migrations/"
      provides: "Migration SQL file named remove_community_models that drops Post, Like, Comment tables and their FK constraints"
  key_links:
    - from: "api/prisma/schema.prisma"
      to: "api/prisma/migrations/"
      via: "npx prisma migrate dev --name remove_community_models"
      pattern: "remove_community_models"
    - from: "api/src/routes/saves.ts"
      to: "api/prisma/schema.prisma SaveSlot"
      via: "app.prisma.saveSlot.findMany"
      pattern: "saveSlot"
---

<objective>
Drop the Post, Like, and Comment Prisma models from the schema and apply the migration locally. This is the [BLOCKING] database step required to complete RM-02 — the schema edit regenerates the Prisma client types (removing the generated Post/Like/Comment query methods), and the migration SQL drops the tables and their FK constraints from the local dev database.

After the migration, run the full Vitest suite to confirm RM-04: auth and cloud-saves tests pass, no references to the deleted community code remain.

This plan depends on Plan 01 (routes and test cleanup) and Plan 02 (C++ client removal) completing first, though schema.prisma itself has no dependency on those — the ordering ensures the test suite is already clean before we run it here.

Purpose: Complete the data-layer removal and establish the regression-green baseline for the hardening phases.
Output: Updated schema.prisma, a new migration file, and a green Vitest run confirming auth + saves are intact.
</objective>

<execution_context>
@/home/solid/www/personal/playground/thomas/.planning/phases/01-remove-community-feature/01-01-SUMMARY.md
@/home/solid/www/personal/playground/thomas/.planning/phases/01-remove-community-feature/01-02-SUMMARY.md
</execution_context>

<context>
@/home/solid/www/personal/playground/thomas/.planning/ROADMAP.md
@/home/solid/www/personal/playground/thomas/.planning/REQUIREMENTS.md
@/home/solid/www/personal/playground/thomas/api/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Edit schema.prisma to drop Post/Like/Comment; generate and apply the migration</name>

  <read_first>
    api/prisma/schema.prisma — current state: User has fields posts Post[], likes Like[], comments Comment[]; Post model with FK authorId -> User; Like model with composite PK [userId, postId] and FKs to both User and Post; Comment model with FKs to Post and User; SaveSlot, RefreshToken, and User (core fields) must be preserved exactly.
    FK cascade order matters for the migration: Like and Comment reference Post; Post references User. Prisma migration tooling handles FK drop order automatically when models are removed together in a single migration — do not attempt to hand-write SQL.
    The migration must run against the local dev database (DATABASE_URL from api/.env or environment). Do NOT run against production (Lightsail). Production gets the migration on the next deploy via the normal PM2/GitHub Actions workflow.
    api/prisma/migrations/ — check for the most recent migration to understand the naming prefix convention (timestamp_name format).
  </read_first>

  <files>api/prisma/schema.prisma, api/prisma/migrations/</files>

  <action>
    Step 1 — Edit api/prisma/schema.prisma:

    From the User model, remove the three relation fields:
      posts         Post[]
      likes         Like[]
      comments      Comment[]

    Delete the entire Post model block (all fields, indexes, and the closing brace).
    Delete the entire Like model block.
    Delete the entire Comment model block.

    The resulting schema must contain exactly: generator client, datasource db, model User (without community relation fields), model RefreshToken, model SaveSlot.

    Verify the schema is syntactically valid before running migrate:
    cd /home/solid/www/personal/playground/thomas/api && npx prisma validate

    Step 2 — Generate and apply the migration locally:
    cd /home/solid/www/personal/playground/thomas/api && npx prisma migrate dev --name remove_community_models

    This command:
    - Detects the diff between the current schema and last migration state
    - Generates SQL that drops Comment, Like, Post tables and their FK/index constraints
    - Applies the migration to the local DATABASE_URL database
    - Regenerates the Prisma client (removes Post/Like/Comment query methods from @prisma/client)

    If the local database is not running, start it before running the command (the project uses postgres:16-alpine locally per STACK.md). Do not use --create-only (that would skip applying).

    Step 3 — Verify schema state:
    grep -c "model Post\|model Like\|model Comment" api/prisma/schema.prisma — must return 0
    grep -c "model User\|model RefreshToken\|model SaveSlot" api/prisma/schema.prisma — must return 3
    grep -c "posts\s*Post\[\]\|likes\s*Like\[\]\|comments\s*Comment\[\]" api/prisma/schema.prisma — must return 0

    Commit: "feat(01-03): drop Post/Like/Comment Prisma models; apply remove_community_models migration (RM-02)"
  </action>

  <verify>
    <automated>grep -c "model Post\|model Like\|model Comment" /home/solid/www/personal/playground/thomas/api/prisma/schema.prisma | grep "^0$"</automated>
  </verify>

  <acceptance_criteria>
    - api/prisma/schema.prisma contains no "model Post", "model Like", or "model Comment" blocks
    - api/prisma/schema.prisma contains "model User", "model RefreshToken", and "model SaveSlot" (3 preserved models)
    - User model in schema.prisma has no "posts Post[]", "likes Like[]", or "comments Comment[]" fields
    - A new directory exists under api/prisma/migrations/ named with timestamp + remove_community_models
    - npx prisma validate exits 0 (no schema errors)
    - npx prisma migrate dev exits 0 (migration applied, no pending migrations)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run Vitest suite and confirm full phase success criteria are met</name>

  <read_first>
    api/test/api.test.ts — after Plan 01, this file contains only: GET /health, auth register+login+refresh tests, auth refresh and logout test (with /users/me assertion removed), saves list/upload/download with revision test, and rate-limit tests. All community tests (feed, posts, users) are gone.
    The Vitest suite needs a live DATABASE_URL to run (integration tests using app.inject() with Prisma). Ensure the local database has the migration applied (from Task 1) before running.
    api/package.json — Vitest run command: "test": "vitest run" or check the scripts field for the exact command.
    Plan 02 produced a clean desktop build (build-desktop/thomaz ELF). No further C++ changes in this plan. Confirm the build is still passing by re-running it (it should be; nothing changed in C++ since Plan 02).
  </read_first>

  <files>(verification only — no new file edits in this task)</files>

  <action>
    Step 1 — Run the API Vitest suite:
    cd /home/solid/www/personal/playground/thomas/api && npx vitest run 2>&1 | tail -30

    Expected: all tests pass. The suite should include tests for /health, /auth/*, /saves/* and rate-limiting, with no references to /feed, /posts, or /users/*.

    If any test fails:
    - A "relation does not exist" Prisma error means the migration was not applied — re-run npx prisma migrate dev from Task 1.
    - A TypeScript import error for a deleted module means a stale import survived in the test or a route file — fix the import and re-run.
    - A "cannot find module" error for feed-page.ts or serializers community export means Plan 01 left a stale import — fix it.

    Step 2 — Confirm desktop build still passes (no regressions from Plan 02):
    cmake --build /home/solid/www/personal/playground/thomas/build-desktop -- -j$(nproc) 2>&1 | tail -10

    Step 3 — Final grep sweep to confirm no dead community references anywhere:
    grep -r "IFeedClient\|feed_client\.hpp\|feed_types\.hpp\|HttpFeedClient\|FakeFeedClient" /home/solid/www/personal/playground/thomas/source/ 2>/dev/null | grep -v "^Binary" | wc -l
    (expect 0)

    grep -r "routes/feed\|routes/posts\|routes/users\|@fastify/multipart\|@fastify/static" /home/solid/www/personal/playground/thomas/api/src/ 2>/dev/null | wc -l
    (expect 0)

    Commit: "chore(01-03): verify phase 1 complete — Vitest green, desktop build green, no dead community references"
  </action>

  <verify>
    <automated>cd /home/solid/www/personal/playground/thomas/api && npx vitest run 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - npx vitest run exits 0 — all tests pass, none fail
    - Test output shows tests for /auth/* and /saves/* passing; no test for /feed, /posts, /users/* appears
    - cmake --build build-desktop exits 0 (binary still compiles cleanly after Plan 02 changes)
    - grep -r "IFeedClient" source/ returns 0 matches
    - grep -r "model Post\|model Like\|model Comment" api/prisma/schema.prisma returns 0 matches
    - A migration directory named *_remove_community_models exists under api/prisma/migrations/
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma migrate → local dev DB | Migration is applied locally only; production DB gets the migration via the normal GitHub Actions deploy workflow |
| API routes → Prisma client | After client regeneration, prisma.post / prisma.like / prisma.comment no longer exist as typed query methods — any surviving reference is a compile-time error caught by typecheck |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Prisma migration modifying User/RefreshToken/SaveSlot | mitigate | Schema edit removes ONLY Post, Like, Comment models and the three relation fields on User. RefreshToken and SaveSlot are untouched. npx prisma validate confirms schema integrity before migrate runs. |
| T-03-02 | Information Disclosure | Residual Post/Like/Comment data in DB after migration | accept | The migration drops the tables. Any residual data in the post/like/comment tables is deleted by the DROP TABLE cascade. This is intentional — the community feature is being fully removed per D-01. |
| T-03-03 | Denial of Service | Migration failure leaving DB in inconsistent state | mitigate | Prisma migrations run in a transaction; a failure rolls back. The local dev DB can be reset if needed (prisma migrate reset); production is not touched in this plan. |
| T-03-SC | Tampering | npm install (npx prisma) | accept | prisma is already in api/package.json devDependencies; no new package is installed. npx prisma resolves from node_modules, not from the registry. |
</threat_model>

<verification>
After both tasks complete:
- api/prisma/schema.prisma: grep -c "model Post\|model Like\|model Comment" returns 0; grep -c "model User\|model SaveSlot\|model RefreshToken" returns 3
- api/prisma/migrations/ contains a directory named *_remove_community_models with migration.sql
- cd api && npx vitest run exits 0
- cmake --build build-desktop exits 0
- grep -r "IFeedClient\|feed_client\.hpp" source/ returns 0 matches
- grep -r "@fastify/static\|@fastify/multipart\|feedRoutes\|postsRoutes\|usersRoutes" api/src/app.ts returns 0 matches
</verification>

<success_criteria>
- RM-02: Post/Like/Comment Prisma models dropped via a clean migration; User/RefreshToken/SaveSlot intact
- RM-04: API Vitest suite exits 0 (auth + saves green, no dead community references); desktop build exits 0
- Phase 1 complete: all four requirements RM-01, RM-02, RM-03, RM-04 satisfied
</success_criteria>

<output>
Create .planning/phases/01-remove-community-feature/01-03-SUMMARY.md when done.

## Artifacts this phase plan produces

Net-new artifact: api/prisma/migrations/{timestamp}_remove_community_models/ directory with migration.sql.
Modified: api/prisma/schema.prisma (Post/Like/Comment models removed).
No C++ files changed in this plan.

This plan's migration SQL is the only truly net-new artifact of the entire Phase 1 removal. All other changes in Plans 01 and 02 are deletions and edits to existing files.
</output>
