---
phase: 03-c-platform-hardening
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - source/app/save_detail_activity.hpp
  - source/app/save_detail_activity.cpp
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "save_detail_activity.hpp declares cloudBusy as std::atomic of bool, initialized false"
    - "Every read of cloudBusy uses .load(); every write uses .store()"
    - "The concurrent-operation guard behaves exactly as before (check-then-set preserved, no compare_exchange)"
    - "The alive member is left untouched (S2 cross-phase boundary — Phase 4 owns it)"
  artifacts:
    - path: "source/app/save_detail_activity.hpp"
      provides: "atomic cloudBusy declaration with documented threading contract"
      contains: "atomic"
  key_links:
    - from: "source/app/save_detail_activity.cpp"
      to: "cloudBusy.load / cloudBusy.store"
      via: "atomic accessors at all guard/set sites"
      pattern: "cloudBusy\\.(load|store)"
---

<objective>
Make save_detail_activity's cloudBusy guard a std::atomic of bool with a documented threading contract (CONC-01). All read sites use .load(), all write sites use .store(). Semantics are preserved exactly — the existing check, return-if-busy, then set busy pattern stays a plain load+store (NO compare_exchange — a CAS would change the check-then-set semantics; RESEARCH Anti-Pattern).

S2 CROSS-PHASE CONSTRAINT (LOCKED): touch ONLY cloudBusy. Do NOT remove or modify the alive member in save_detail_activity.hpp — Phase 4's CONC-02 owns that. This plan depends on Plan 03 because both edit save_detail_activity.cpp (Plan 03 adds the banner call); serializing avoids a write conflict.

Purpose: Document and enforce the main-thread-only contract for cloudBusy so a future off-thread refactor (Phase 4 runAsync) is race-safe.
Output: cloudBusy atomicized in save_detail_activity.{hpp,cpp}.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-c-platform-hardening/03-CONTEXT.md
@.planning/phases/03-c-platform-hardening/03-RESEARCH.md
@.planning/phases/03-c-platform-hardening/03-PATTERNS.md
@.planning/phases/03-c-platform-hardening/03-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan MODIFIES an existing symbol — no new public symbols. The drift pass should note cloudBusy changes type from bool to std::atomic of bool; no new files or globals are introduced.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Atomicize cloudBusy (load/store at all 11 sites), document the contract</name>
  <files>source/app/save_detail_activity.hpp, source/app/save_detail_activity.cpp</files>
  <read_first>
    - source/app/save_detail_activity.hpp:4 (already includes the atomic header), :47 (the bool cloudBusy = false declaration to change), :53 (the alive member — MUST NOT be touched per S2)
    - source/app/save_detail_activity.cpp:249 and :342 (the two read guards in doUpload/doDownload — become .load()), :251,:296,:344 (set true — become .store(true)), :266,:275,:313,:322,:381 (set false — become .store(false))
    - source/app/save_detail_activity.cpp:51 (the install_header_username + Plan-03 banner call — confirm Plan 03's edit is present and leave it untouched)
  </read_first>
  <action>
    In save_detail_activity.hpp: change line 47 from the plain bool cloudBusy initialized to false into a std::atomic of bool initialized to false (brace-init). Add a brief doc comment documenting the threading contract (main-thread-only today; atomic enforces the contract and makes a future off-thread write — Phase 4 runAsync — race-safe). Leave the alive member (.hpp:53) EXACTLY as-is — do not touch it (S2).

    In save_detail_activity.cpp: replace the two read guards at :249 and :342 (the bare if this->cloudBusy return;) with the .load() form. Replace the set-true sites (:251,:296,:344) with this->cloudBusy.store(true); and the set-false sites (:266,:275,:313,:322,:381) with this->cloudBusy.store(false);. Do NOT introduce compare_exchange — keep the existing check-then-set structure verbatim, only swapping the bare read/assign for load()/store(). Change no other logic.
  </action>
  <verify>
    <automated>cd tests && make clean && make test</automated>
  </verify>
  <acceptance_criteria>
    - cloudBusy is atomic in the header: grep -q 'atomic' source/app/save_detail_activity.hpp AND grep -q 'cloudBusy' source/app/save_detail_activity.hpp
    - No bare assignment remains: grep -nE 'cloudBusy *= *(true|false)' source/app/save_detail_activity.cpp returns nothing (all writes go through .store())
    - All accesses use atomic accessors: grep -cE 'cloudBusy\.(load|store)' source/app/save_detail_activity.cpp returns 11 (2 loads + 3 store true + 5 store false + the count tolerance for the exact site set; at minimum >= 10)
    - No compare_exchange introduced: grep -c 'compare_exchange' source/app/save_detail_activity.cpp returns 0
    - alive member preserved (S2): grep -q 'alive' source/app/save_detail_activity.hpp AND git diff -- source/app/save_detail_activity.hpp shows the alive line unchanged
    - Plan-03 banner wiring preserved: grep -q 'install_tls_warning_banner' source/app/save_detail_activity.cpp
    - Host suite green: cd tests && make clean && make test exits 0
  </acceptance_criteria>
  <done>cloudBusy is std::atomic of bool with a documented contract; all 11 sites use load/store; no compare_exchange; alive untouched; banner wiring intact; make test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI event handlers ↔ async cloud-save callbacks | cloudBusy guards against concurrent upload/download; a torn read could allow a double operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering (state corruption) | Data race on cloudBusy enabling a double upload/download if a future refactor moves a write off the main thread | mitigate | std::atomic of bool (CONC-01) gives a well-defined memory model for every read/write; load/store preserves the current check-then-set semantics exactly (all 11 sites are main-thread today per RESEARCH, so no behavior change now, but the contract is enforced for Phase 4's runAsync). |
| T-03-08 | Tampering | Accidentally widening scope to the alive member and colliding with Phase 4 | mitigate | Acceptance criterion asserts the alive line is unchanged in git diff (S2 boundary); plan scoped to cloudBusy only. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs — single-symbol type change; RESEARCH Package Legitimacy Audit confirms zero external packages. |
</threat_model>

<verification>
- cloudBusy is std::atomic of bool in the header with a documented contract (ROADMAP SC-2).
- All read/write sites use load()/store(); no compare_exchange (semantics preserved).
- alive member unchanged (S2 cross-phase boundary intact for Phase 4 CONC-02).
- Plan-03 banner wiring in save_detail_activity.cpp preserved.
- cd tests && make test green.
</verification>

<success_criteria>
- save_detail_activity.hpp declares cloudBusy as std::atomic of bool, init false, with a documented threading contract (CONC-01 / ROADMAP SC-2).
- Every access uses load()/store(); guard behavior unchanged; no compare_exchange.
- alive member untouched (S2).
- Host test suite green.
</success_criteria>

<output>
Create `.planning/phases/03-c-platform-hardening/03-04-SUMMARY.md` when done.
</output>
