---
phase: 04-c-activity-hardening
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - source/app/mod_detail_activity.hpp
  - source/app/mod_detail_activity.cpp
  - source/app/clear_cheats_activity.hpp
  - source/app/clear_cheats_activity.cpp
  - source/app/auth_activity.hpp
  - source/app/auth_activity.cpp
  - source/app/theme_browser_activity.hpp
  - source/app/theme_browser_activity.cpp
  - source/app/settings_activity.hpp
  - source/app/settings_activity.cpp
  - source/app/theme_detail_activity.hpp
  - source/app/theme_detail_activity.cpp
  - source/app/cheat_detail_activity.hpp
  - source/app/cheat_detail_activity.cpp
  - source/app/mod_manager_activity.hpp
  - source/app/mod_manager_activity.cpp
autonomous: true
requirements: [CONC-02]
must_haves:
  truths:
    - "All nine remaining alive-guarded activities inherit ThomazActivity (not brls::Activity directly)"
    - "The per-activity alive member is removed from all nine headers; the base owns it"
    - "Every direct brls::async call-site in these files is migrated to this->runAsync(...)"
    - "No DEBT-03 cast edits are made in these nine files (cast scope is the four files in Plan 02 only)"
    - "The desktop build compiles clean with zero new warnings after the inheritance changes"
  artifacts:
    - path: "source/app/theme_detail_activity.cpp"
      provides: "runAsync migration of the 5 async sites"
      contains: "runAsync"
    - path: "source/app/mod_manager_activity.cpp"
      provides: "base-swap + alive-member removal (no direct brls::async to migrate)"
      contains: "ThomazActivity"
  key_links:
    - from: "source/app/theme_detail_activity.hpp"
      to: "source/app/thomaz_activity.hpp"
      via: "class : public ThomazActivity"
      pattern: "public ThomazActivity"
    - from: "source/app/auth_activity.cpp"
      to: "ThomazActivity::runAsync"
      via: "this->runAsync(worker, onSync)"
      pattern: "runAsync"
---

<objective>
Complete the CONC-02 migration (D-01: ALL direct `brls::async` activity call-sites) by migrating the nine remaining activities that carry an `alive` guard but are NOT in the DEBT-03 cast scope: `mod_detail`, `clear_cheats`, `auth`, `theme_browser`, `settings`, `theme_detail`, `cheat_detail`, and `mod_manager`. Per file: `.hpp` base `brls::Activity` → `ThomazActivity` + remove the `alive` member; `.cpp` dtor drop `*this->alive = false;`; migrate each direct `brls::async(...)` guarded site to `this->runAsync(...)`. These files get NO cast changes (DEBT-03 is scoped to the four files in Plan 02 — RESEARCH Pitfall 4).

Purpose: D-01 mandates migrating ALL 13 `brls::async` activity files (mirroring the Phase 3 D-04 "all call-sites" precedent) so the alive guard is genuinely impossible to forget app-wide. Plan 02 covered the four DEBT/CONC shared files; this plan covers the rest. Runs in parallel with Plan 02 (zero file overlap — different activities), both depending only on the Plan 01 base.

Special case — `mod_manager_activity`: it declares `alive` + a dtor setting it, but has NO direct `brls::async` line (verified at plan time — its `brls::sync([this]{...})` calls at 105/176/275/298 are NOT alive-guarded today). It needs the base-swap + `alive`-member removal + dtor cleanup ONLY; do NOT invent a runAsync wrapping for its unguarded `brls::sync` calls (out of D-01 scope, which targets `brls::async` sites).

Output: 9 migrated activities building clean on desktop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-c-activity-hardening/04-CONTEXT.md
@.planning/phases/04-c-activity-hardening/04-RESEARCH.md
@.planning/phases/04-c-activity-hardening/04-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@source/app/thomaz_activity.hpp
@.planning/phases/04-c-activity-hardening/04-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts This Phase Produces (this plan's contribution)

- **Migration** of 8 activities with direct `brls::async` to `: public ThomazActivity` + `runAsync`: `mod_detail`, `clear_cheats`, `auth`, `theme_browser`, `settings`, `theme_detail`, `cheat_detail`.
- **Base-swap-only migration** of `mod_manager_activity` (`: public ThomazActivity`, `alive` member removed, dtor `*alive=false;` removed) — it has no direct `brls::async` to migrate.
- **Removal** of the per-activity `alive` member + the `*this->alive = false;` dtor line from all nine headers/dtors.
- **No** DEBT-03 cast changes in these files (scope discipline — Pitfall 4).

(Base class + runAsync from Plan 01; the four DEBT-03 files from Plan 02; CONC-03 curl cancellation from Plan 04.)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 8 async-bearing activities to ThomazActivity + runAsync</name>
  <files>source/app/mod_detail_activity.hpp, source/app/mod_detail_activity.cpp, source/app/clear_cheats_activity.hpp, source/app/clear_cheats_activity.cpp, source/app/auth_activity.hpp, source/app/auth_activity.cpp, source/app/theme_browser_activity.hpp, source/app/theme_browser_activity.cpp, source/app/cheat_detail_activity.hpp, source/app/cheat_detail_activity.cpp</files>
  <read_first>
    - source/app/thomaz_activity.hpp + 04-01-SUMMARY.md (the exact runAsync signature + cancelled accessor name)
    - source/app/mod_browser_activity.cpp (the canonical capture+guard async idiom 53-72 that runAsync replaces — reference shape)
    - source/app/mod_detail_activity.cpp (async sites 75, 206)
    - source/app/clear_cheats_activity.cpp (async site 41)
    - source/app/auth_activity.cpp (async site 98)
    - source/app/theme_browser_activity.cpp (async sites 94, 115)
    - source/app/cheat_detail_activity.cpp (async site 47)
    - .planning/phases/04-c-activity-hardening/04-RESEARCH.md §"13-file inventory" and Pitfall 5 (per-file dtor handling)
    - .planning/codebase/CONVENTIONS.md (snake_case, no exceptions)
  </read_first>
  <action>
    For each of `mod_detail`, `clear_cheats`, `auth`, `theme_browser`, `cheat_detail`: (1) `.hpp` — change base `public brls::Activity` → `public ThomazActivity`, add `#include "thomaz_activity.hpp"`, remove the `alive` member declaration; (2) `.cpp` dtor — remove `*this->alive = false;` (drop the dtor + its header declaration if the body becomes empty; keep any other cleanup); (3) migrate every direct `brls::async([...alive...]{ worker; brls::sync([...alive...]{ if(!alive->load())return; onSync; }); })` to `this->runAsync([worker captures, no this]{ worker; }, [onSync captures]{ onSync; })`. Async sites: mod_detail 75 & 206; clear_cheats 41; auth 98; theme_browser 94 & 115; cheat_detail 47. Keep the existing worker→onSync data handoff (results captured into the sync lambda). Do NOT add a manual `alive` capture/guard (runAsync owns it). Make NO cast edits in any of these files (DEBT-03 is Plan 02 scope only — Pitfall 4). Note: mod_detail:206 captures `alive` but no `this` (`[alive, f, dest, mod_name, tid]`) — its onSync still routes through runAsync's onSync slot.
  </action>
  <verify>
    <automated>for f in mod_detail clear_cheats auth theme_browser cheat_detail; do grep -Eq "class .*:\s*public\s+ThomazActivity" source/app/${f}_activity.hpp || { echo "FAIL base $f"; exit 1; }; done; [ "$(grep -c 'brls::async' source/app/mod_detail_activity.cpp source/app/clear_cheats_activity.cpp source/app/auth_activity.cpp source/app/theme_browser_activity.cpp source/app/cheat_detail_activity.cpp | awk -F: '{s+=$2} END{print s}')" = "0" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 headers (`mod_detail`, `clear_cheats`, `auth`, `theme_browser`, `cheat_detail`) declares `: public ThomazActivity` and no longer declares its own `alive` member.
    - `grep -c "brls::async"` across these 5 `.cpp` files totals 0 (all migrated).
    - Each migrated site uses `this->runAsync(` and no longer hand-captures/guards `alive`.
    - No C-style `(brls::T*)getView` cast was added or changed in any of these files (DEBT-03 scope discipline).
  </acceptance_criteria>
  <done>The five activities (mod_detail, clear_cheats, auth, theme_browser, cheat_detail) inherit ThomazActivity, own no `alive` member, and route every direct async through `runAsync`, with no cast edits.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate settings + theme_detail + mod_manager, then verify clean desktop build</name>
  <files>source/app/settings_activity.hpp, source/app/settings_activity.cpp, source/app/theme_detail_activity.hpp, source/app/theme_detail_activity.cpp, source/app/mod_manager_activity.hpp, source/app/mod_manager_activity.cpp</files>
  <read_first>
    - source/app/thomaz_activity.hpp + 04-01-SUMMARY.md (runAsync signature)
    - source/app/settings_activity.cpp (async sites 145, 195, 217)
    - source/app/theme_detail_activity.cpp (async sites 57, 74, 141, 179, 203)
    - source/app/mod_manager_activity.cpp (lines 61 dtor `*this->alive = false;`; 105/176/275/298 are bare `brls::sync([this]{...})` — NOT alive-guarded, NOT brls::async; leave them as-is)
    - source/app/mod_manager_activity.hpp (base + alive member to remove)
    - .planning/phases/04-c-activity-hardening/04-RESEARCH.md Open Question 1 + Assumption A1 (mod_manager has alive+dtor but no direct brls::async — base-swap only)
    - memory note: desktop build needs BOTH -DPLATFORM_DESKTOP=ON AND -DUSE_SDL2=ON (else GLFW/wayland-scanner fails); exit-124 smoke timeout is a healthy run
  </read_first>
  <action>
    For `settings` and `theme_detail`: apply the standard per-file migration — `.hpp` base → `ThomazActivity` + include + remove `alive` member; `.cpp` dtor drop `*this->alive=false;`; migrate async sites (settings 145,195,217; theme_detail 57,74,141,179,203) to `this->runAsync(...)`. For `mod_manager_activity`: it has NO direct `brls::async` — do ONLY the base-swap (`public brls::Activity` → `public ThomazActivity` + include), remove the `alive` member from the header, and remove the `*this->alive = false;` line at :61 from the dtor (drop the dtor if empty). Leave its bare `brls::sync([this]{...})` calls at 105/176/275/298 UNCHANGED — they are not `brls::async` sites and are out of D-01 scope. After ALL nine activities in this plan are migrated, run the clean desktop build: configure + build with `-DPLATFORM_DESKTOP=ON -DUSE_SDL2=ON`; confirm zero errors and zero NEW warnings from the inheritance changes. Record the exact commands and warning delta.
  </action>
  <verify>
    <automated>for f in settings theme_detail mod_manager; do grep -Eq "class .*:\s*public\s+ThomazActivity" source/app/${f}_activity.hpp || { echo "FAIL base $f"; exit 1; }; grep -q "shared_ptr<std::atomic<bool>> alive" source/app/${f}_activity.hpp && { echo "FAIL alive remains $f"; exit 1; }; done; [ "$(grep -c 'brls::async' source/app/settings_activity.cpp source/app/theme_detail_activity.cpp source/app/mod_manager_activity.cpp | awk -F: '{s+=$2} END{print s}')" = "0" ] && echo GREP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `settings`, `theme_detail`, `mod_manager` headers declare `: public ThomazActivity` and declare no `alive` member.
    - `grep -c "brls::async"` across `settings_activity.cpp`, `theme_detail_activity.cpp`, `mod_manager_activity.cpp` totals 0.
    - `mod_manager_activity.cpp` still contains its four `brls::sync([this]` calls unchanged (they were never `brls::async` and are out of scope).
    - `mod_manager_activity.cpp` no longer references `this->alive` (dtor line removed).
    - Whole-phase grep: `grep -rc "brls::async" source/app/ | awk -F: '{s+=$2} END{print s}'` returns 0 across ALL activities (Plans 02 + 03 combined fully migrate D-01's 13 files).
    - Desktop build configured AND built with `-DPLATFORM_DESKTOP=ON -DUSE_SDL2=ON` compiles all nine migrated activities with zero errors and zero NEW warnings (commands + delta recorded in SUMMARY).
  </acceptance_criteria>
  <done>settings and theme_detail route async through runAsync; mod_manager is base-swapped with its alive member removed and its unguarded brls::sync calls untouched; the whole app has zero remaining direct `brls::async`; the desktop build is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async pool thread → UI thread | Worker runs while the activity may be popped; the base-owned `alive` guard mediates the `onSync` continuation across all nine migrated activities |

## STRIDE Threat Register

Internal robustness refactor; no network/auth/input-validation surface change. No DEBT-03 cast surface in this plan (those are Plan 02). The threat class is use-after-free during the per-activity `alive` removal.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (memory) / use-after-free | nine `alive` members removed; a stale dtor reference or un-migrated async site would dangle | mitigate | Each migration removes the member AND its dtor line in the same per-file pass (Pitfall 5); every `brls::async` site routes through `runAsync` (base-owned guard captured by value). For `mod_manager` (no async), the base dtor still sets `alive=false` so any future async path inherits the guard. Verified by the whole-app `grep -c brls::async == 0` gate + clean desktop build. |
| T-04-06 | Denial of service (race) | concurrent guard access across pool/UI threads | accept | `alive` is `std::atomic<bool>` (lock-free); no torn reads. Unchanged from pre-refactor semantics. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure in-repo refactor (RESEARCH: zero new dependencies). N/A. |

No high-severity (remotely exploitable) threat in this plan; stated explicitly per the security contract.
</threat_model>

<verification>
- All nine headers inherit `ThomazActivity` and declare no `alive` member.
- Zero `brls::async` remains anywhere under `source/app/` (combined with Plan 02).
- `mod_manager` unguarded `brls::sync` calls are untouched; no cast edits in any of the nine files.
- Desktop build (`-DPLATFORM_DESKTOP=ON -DUSE_SDL2=ON`) is clean, zero new warnings.
</verification>

<success_criteria>
- All activities that previously used `brls::async` directly now call `this->runAsync(...)` and the per-activity `alive` member is gone app-wide (ROADMAP Phase 4 success criterion 1; CONC-02, D-01 "all call-sites").
- DEBT-03 cast scope was NOT touched in these nine files (Pitfall 4 discipline).
- Desktop build is clean with zero new warnings (ROADMAP success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/04-c-activity-hardening/04-03-SUMMARY.md` when done. Record: per-file dtor kept/dropped, async sites migrated per file, confirmation that mod_manager was base-swap-only, the whole-app `brls::async` count (must be 0), and the desktop build command + warning delta.
</output>
