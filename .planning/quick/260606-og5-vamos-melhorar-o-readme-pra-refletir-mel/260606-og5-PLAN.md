---
phase: quick-260606-og5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-README-01
must_haves:
  truths:
    - "The README headline describes thomaz as a multi-feature Switch homebrew hub (cheats + mods + themes + cloud saves), not a cheats-only tool"
    - "Each user-visible feature surfaced on the home screen (Themes, Cheats, Mods, Saves, Settings) has at least one description line in the Funcionalidades section"
    - "Every technical claim in the README (build command, output path, two-gate verification, dependencies, backend) matches the actual codebase"
    - "No feature is advertised that is not actually reachable by a user (the hidden sysmodules card is not sold as a feature)"
  artifacts:
    - path: "README.md"
      provides: "Accurate project description, full feature list, verified build/install instructions, current structure"
      contains: "Mods"
  key_links:
    - from: "README.md Funcionalidades section"
      to: "source/app/home_activity.cpp home cards"
      via: "feature parity — every active card has README coverage"
      pattern: "Mods|Temas|Saves|Cheats"
    - from: "README.md build section"
      to: "scripts/build-switch.sh"
      via: "documented build command matches the real script"
      pattern: "build-switch\\.sh"
---

<objective>
Rewrite README.md so it accurately reflects what thomaz actually is: a multi-feature
Nintendo Switch homebrew hub (Borealis UI, Switch-only NRO) covering **cheats, mods,
themes, and cloud saves**, backed by a Fastify/Postgres cloud API — not the
cheats-only manager the current README describes.

The current README documents only the Cheats feature. The actual home screen
(`source/app/home_activity.cpp` + `resources/xml/activity/home.xml`) surfaces five
active feature cards — Themes (hero), Cheats, Mods, Saves, Settings — plus account
login, title-visibility filtering, and self-update. The README must close this gap
without inventing anything: every claim is grounded in the codebase verified during
planning.

Purpose: The README is the front door of the repo and is currently misleading —
it undersells the project by ~4 of its 5 features and leaves its structure/feature
docs stale relative to the shipped v1.1 Switch-only tree.
Output: A rewritten README.md (Portuguese, matching the existing tone) that is
feature-complete, technically accurate, and internally consistent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# The artifact being improved
@README.md

# Ground-truth sources for the feature list — DO NOT invent beyond these
@source/app/home_activity.cpp
@resources/xml/activity/home.xml

# Ground-truth for the build/verification claims
@CMakeLists.txt
@scripts/build-switch.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the headline, Funcionalidades, and "Como usar" to cover all active features</name>
  <files>README.md</files>
  <action>
Rewrite the top of README.md (headline through "Como usar") so it describes thomaz
as a multi-feature Switch homebrew hub, not a cheats-only manager.

GROUND TRUTH (verified during planning — do not add features beyond this list):
The home screen wires six cards in `source/app/home_activity.cpp`. FIVE are active
and user-visible (per `resources/xml/activity/home.xml`): **Temas (hero), Cheats,
Mods, Saves, Configurações**. The SIXTH — System/sysmodules (`systemCard`) — is
`visibility="gone"` in home.xml: it is wired in code but NOT shown to users. Do NOT
advertise sysmodules as a feature; it is not reachable from the menu.

Headline (line ~3): change from "Gerenciador de trapaças (cheats)" to a hub framing.
Keep it bilingual-friendly and in Portuguese. Convey: thomaz is a modern homebrew
hub for the Nintendo Switch that manages **cheats, mods, temas e backups/saves na
nuvem** for your games, with a touch/mouse/controller UI. Keep the existing
Atmosphère + switch-cheats-db references where they belong (now scoped to the cheats
feature, not the whole app). Preserve the existing "⚠️ Status: em desenvolvimento /
não validado 100% em hardware real" caveat (still accurate per PROJECT.md / STATE.md
deferred hardware-UAT items).

Funcionalidades section (lines ~11-22): restructure into one short block per active
feature. Required coverage, each grounded in the codebase:
  - **Cheats** — keep the existing accurate description (lista jogos instalados com
    badges "tem cheat"/"ativo"; tela de toggles por jogo; baixa o cheat certo para o
    build_id da versão instalada; salva na SD; Limpar trapaças com seleção múltipla).
    Source: `core/cheat_*`, `platform/cheat_store`, clear_cheats_activity.
  - **Mods** — browse/install de mods do **GameBanana** por jogo, com extração de
    arquivos (zip/7z/etc). Source: `core/mods/*` (gamebanana_*), `platform/mods/*`
    (libarchive_extractor, mod_download, mod_store), mod_browser/detail/manager
    activities.
  - **Temas** — navegar e aplicar temas da nuvem via **Themezer**, MAIS extração
    on-device dos layouts do firmware (engine exelix vendorizado + hactool) para
    desbloquear a aplicação de temas sem precisar de prod.keys do usuário. Source:
    `core/themes/themezer_*`, `platform/themes/*` (theme_install, firmware_extract,
    nca_extract, key_loader), theme_browser/detail activities.
  - **Saves (backup + nuvem)** — backup de saves locais e sincronização na nuvem com
    locking otimista, via a API HTTP do projeto (login necessário). Source:
    `core/saves/*` (save_sync, cloud_save_json), `platform/saves/*`, save_manager/
    detail activities.
  - **Configurações** — keep existing: Idioma (Automático/Português/English),
    Buscar atualizações (auto-update via GitHub Releases), Atualizar base de cheats.
  - Cross-cutting line — conta/login (tela de boot: login ou convidado) necessária
    para saves na nuvem; filtro de visibilidade de títulos (oculta forwarders que não
    são jogos); UI bilíngue + toque/mouse/controle; tema escuro com acento violeta.

Languages claim: the current README says only "Bilíngue PT-BR / EN". Verified locale
dirs: `resources/i18n/` has pt-BR and en-US fully translated (thomaz/mods/themes/
system), plus PARTIAL fr, ru, zh-Hans (only thomaz.json). State this accurately:
"Totalmente bilíngue PT-BR / EN, com traduções parciais em outros idiomas (fr, ru,
zh)." Do not claim full support for fr/ru/zh.

"Como usar" section (lines ~57-70): keep the cheats walkthrough (it is accurate),
but reframe its intro so it's clearly "como usar os cheats" rather than "como usar o
app" — the app now does more than cheats. The "Onde os cheats são gravados" subsection
(sd:/atmosphere/contents/<TITLE_ID>/cheats/<BUILD_ID>.txt + nearest-version fallback)
is accurate — keep it. Optionally add one short line each pointing at how to reach
Mods (home → Mods → escolher jogo), Temas (home → Temas), and Saves (home → Saves,
requer login) — short, factual, no invented step-by-step.

Do NOT touch the Requisitos, Instalação, Capturas de tela, Compilar, Estrutura,
Roadmap, Créditos, Aviso, or Licença sections in this task — Task 2 handles those.
  </action>
  <verify>
    <automated>grep -qi "Mods" README.md && grep -qi "Temas" README.md && grep -qi "Saves\|nuvem" README.md && grep -qi "GameBanana" README.md && grep -qi "Themezer" README.md && echo PASS</automated>
  </verify>
  <done>
    Headline frames thomaz as a multi-feature hub. Funcionalidades has a distinct
    block for each of Cheats, Mods, Temas, Saves, Configurações, each grounded in the
    codebase. Sysmodules is NOT advertised. Language claim accurately reflects
    full PT-BR/EN + partial fr/ru/zh. "Como usar" is scoped clearly. No invented
    features.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and update the build, structure, roadmap, and credits sections against the codebase</name>
  <files>README.md</files>
  <action>
Update the lower half of README.md so every technical claim matches the verified
codebase, and the Estrutura/Créditos sections reflect the real feature set.

BUILD section ("Compilar do código-fonte", lines ~74-116): cross-check against the
verified `scripts/build-switch.sh` and `CMakeLists.txt`. CONFIRMED ACCURATE — keep as
is unless you spot a drift:
  - `./scripts/build-switch.sh` (Docker `devkitpro/devkita64` by default; `DEVKITPRO=`
    for native) — matches the script.
  - Output `build_switch/thomaz.nro` — matches the script's output path.
  - Two-gate verification (`make -C tests test` host doctest + `./scripts/build-switch.sh`
    Switch build) — matches PROJECT.md/STATE.md (host doctest 209/209 + clean Switch build).
  - Backend in `api/` (Fastify + PostgreSQL, Docker compose) — matches PROJECT.md.
  - Borealis fork + CMake + libnx/devkitPro — matches CMakeLists.txt.
Verify the doctest gate count if you cite a number (PROJECT.md says 209/209). Do not
introduce time estimates or invent flags. If any command/path no longer matches the
script, correct it to the script's reality.

ESTRUTURA section (lines ~120-131): the current `source/core` and `source/platform`
descriptions are cheats-centric and stale. Update to reflect the real subdirectories
verified during planning:
  - `source/core/` — pure, testable logic across features: `cheat_*` (parse, build_id,
    índice), `mods/` (gamebanana), `themes/` (themezer), `saves/` (sync), plus update.
  - `source/platform/` — libnx/IO: title listing, HTTP/curl, gravação na SD, settings,
    self-update, `mods/` (extração libarchive), `themes/` (hactool/exelix + firmware
    extract), `saves/` (backup + cloud client).
  - `source/app/` — telas Borealis: home (hub), cheats, mods, temas, saves, configurações,
    boot/login.
  - `api/` — backend Fastify (auth, cloud saves). NOTE: the current README says
    "auth, feed, stubs de saves" — the community **feed was removed in v1.0** (per
    PROJECT.md RM-01..04). Update to "auth + cloud saves" and drop the "feed" mention.
  - `resources/` — i18n + XML das telas + ícone. `tests/` — host doctest. `docs/` — specs.
Keep the tree-diagram style; just make the contents accurate.

ROADMAP section (lines ~135-145): the current roadmap is cheats-only and predates
v1.0/v1.1. Update the "done" checklist to reflect shipped reality per MILESTONES.md /
PROJECT.md without inventing future work:
  - Mark as done: cheats core + build verde; hub bento + lista de jogos; mods
    (GameBanana); temas (Themezer + extração de firmware); saves na nuvem; configurações
    + auto-update; v1.0 hardening (segurança da API, refatoração de concorrência);
    v1.1 Switch-only (target desktop removido, árvore só-Switch).
  - Keep the genuinely-open items that PROJECT.md still lists as deferred/carried:
    validação completa em hardware real (UAT deferida), carregamento lazy de ícones,
    verificação TLS com cacert embarcado (if still accurate), PERF-01/02 (dedup de
    extração de arquivos, cache de CloudStatus).
  Do not invent roadmap items not present in PROJECT.md/MILESTONES.md.

CRÉDITOS section (lines ~148-155): the README credits switch-cheats-db, Borealis,
devkitPro/libnx, Atmosphère. Now that mods/themes/saves are documented features, add
the credits the codebase actually relies on (verified in CMakeLists.txt / source):
  - **Themezer** (fonte dos temas) and the **exelix11/SwitchThemeInjector** theme
    engine (já citado na Licença — também merece crédito aqui).
  - **GameBanana** (fonte dos mods).
  Keep existing credits. Do not credit libraries the tree does not use.

LICENÇA section (lines ~165-170): CONFIRMED ACCURATE (GPLv2, was MIT até v0.4,
mudou ao incorporar a engine exelix) — keep unchanged.

REQUISITOS / INSTALAÇÃO / CAPTURAS / AVISO: verified accurate (only home.png +
games.png exist in docs/screenshots, matching the "mais telas em breve" note; install
via /switch/thomaz.nro is correct). Keep unchanged unless a Task 1 edit created an
inconsistency you must reconcile.

After editing, re-read the full README once to confirm internal consistency: the
feature list (Task 1) and the structure/roadmap/credits (Task 2) must agree on the
same set of features, and no section may still call thomaz a cheats-only tool.
  </action>
  <verify>
    <automated>grep -qi "Themezer" README.md && grep -qi "GameBanana" README.md && grep -q "build_switch/thomaz.nro" README.md && grep -q "build-switch.sh" README.md && ! grep -qi "stubs de saves" README.md && echo PASS</automated>
  </verify>
  <done>
    Build section matches scripts/build-switch.sh and CMakeLists.txt (no drift).
    Estrutura lists the real core/platform/app subdirs (mods, themes, saves) and drops
    the removed "feed" from the api/ description. Roadmap reflects shipped v1.0/v1.1
    and only genuinely-deferred open items. Créditos adds Themezer + exelix +
    GameBanana. Licença unchanged. Full README reads as internally consistent and
    multi-feature.
  </done>
</task>

</tasks>

<verification>
- README headline + Funcionalidades describe a multi-feature hub (cheats, mods, temas, saves), not cheats-only.
- Every active home card (`home_activity.cpp` non-`gone` cards: Themes, Cheats, Mods, Saves, Settings) has README coverage; the hidden sysmodules card is NOT advertised.
- Build commands/paths match `scripts/build-switch.sh` + `CMakeLists.txt`; output is `build_switch/thomaz.nro`; two-gate verification described correctly.
- Estrutura, Roadmap, Créditos updated; the removed community "feed" no longer appears in the api/ description.
- No invented features, libraries, or roadmap items — everything traces to the codebase or PROJECT.md/MILESTONES.md.
</verification>

<success_criteria>
- README.md accurately reflects thomaz as a Switch-only, multi-feature homebrew hub.
- A reader who has never seen the code understands it does cheats, mods, themes, and cloud saves, and can build/install it correctly.
- No technical claim in the README contradicts the codebase as verified during planning.
- Document tone, language (Portuguese), and section structure remain consistent with the original.
</success_criteria>

<output>
Create `.planning/quick/260606-og5-vamos-melhorar-o-readme-pra-refletir-mel/260606-og5-SUMMARY.md` when done
</output>
