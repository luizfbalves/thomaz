/*
    thomaz — app theme registration.
    Call registerThomazTheme() in main() after brls::Application::init().
*/

#pragma once

namespace thomaz {

// Registers the dark theme colors and forces dark mode.
void registerThomazTheme();

} // namespace thomaz
