#pragma once

// Current app version. Keep in sync with CMakeLists (VERSION_*) and the git tag
// used when cutting a GitHub Release (the updater compares against tag_name).
namespace thomaz {
constexpr const char* THOMAZ_VERSION = "0.3.2";
}
